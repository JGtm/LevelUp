# PLAN — T0 dérivé du film (premier mouvement) — 2026-09-02

Branche : `wt/t0-film` (worktree `.claude/worktrees/t0-film`, base feat/v75).
Contrat d'exécution : skill `plan-execution` (défaut). Revue : skill `plan-review` passée.
Recherche fondatrice : `internal/analysis/replay/t0_mouvement_research_test.go` (commit
`a5a468a16`) + entrée thought_log du 2026-09-02.

## Objectif et critère de succès

Le T0 (coup d'envoi du gameplay) est aujourd'hui estimé des `first_joined_time` API —
dégénéré (~0 ms) sur ~10-15 % des matchs, cassé à ±1 h/2 h sur 41/101 (bug TZ historique).
La mesure a établi que le film le porte directement : premier mouvement détecté par match
(CV 1,3 % de marge interne). Succès = (1) le rejeu de `1b2d9e08` démarre sur le coup
d'envoi (gate visuel user), (2) les charts T0-dépendants (premier frag...) lisent le T0
film sur tout match doté d'un artefact, (3) aucun match sans artefact n'est dégradé
(fallback API inchangé).

## Décisions tranchées (fermes — ne pas re-débattre en exécution)

- **D1 (user 02/09)** : détecteur PAR MATCH, aucune constante absolue en dur. La constante
  22 700 ms ne sert que de témoin de plausibilité documentaire, jamais de valeur.
- **D2** : le T0-film PRIME partout où il existe (σ 9,8 s contre 12,8 s, aucune valeur
  invraisemblable) ; `t0_quality = "film_movement"`. L'API reste la source des matchs sans
  artefact.
- **D3 (user 02/09)** : préambule de 1 s = confort de LECTURE uniquement (`leadInFrame`
  côté web). L'origine de l'horloge affichée, la frise et les bornes d'export ne bougent
  pas ; le T0 stocké reste la mesure pure.
- **D4** : réparation de l'historique en base SANS recuisson (les artefacts portent déjà
  les positions). La recuisson `backfill-replay --only-existing` ne sert qu'à publier le
  champ dans les artefacts et NE SE LANCE PAS sans accord explicite du user (règle
  post-sinistre du 31/08, verrou `AcquireSolo`).
- **D5** : côté web, `replayWindow` préfère `doc.t0FilmMs` (axe du match) à
  `header.t0_ms` pour la borne basse et l'origine d'horloge ; la borne de FIN garde sa
  formule compensée actuelle (documentée dans replayWindow.ts).
- **D6** : refus explicite à la `origin.go` : pas de mouvement détectable, témoin rafale
  < 2 xuid dans la seconde, ou t0 − originMs > 120 000 ms → champ ABSENT (pointeur nil),
  WARN journalisé, verdict publié en couverture. Jamais de zéro ambigu.

## Lot A — Helper canonique + champ d'artefact (Go)

- [x] A1. `internal/analysis/replay/t0_film.go` : détecteur de production (fonctions
      pures ; doc de tête chiffrée ; seuils nommés 5 m / 0,5 m / 1 000 ms justifiés par la
      mesure ; refus D6). Entrée = pistes/points déjà en main dans `BuildFromPositions`.
      324 L. Entrée neutre `T0FilmTrack{XUID, Points}` (et non `[]Track`) pour que la CLI
      du lot C, qui décode l'artefact au minimum, appelle le MÊME détecteur ; l'adaptateur
      `t0FilmTracksOf([]Track)` fait le pont côté build. Verdict publié :
      `Coverage.T0Film` (`T0FilmCoverage` : detected / reason / tracks / moving / burst /
      marginMs), publié MÊME EN CAS DE REFUS — trois refus rendent le même champ absent,
      seule la raison les distingue. Quatrième refus ajouté au-delà de D6 :
      `noFrameInterval` (sans échelle de temps, ni fenêtre ni marge n'ont de sens).
- [x] A2. `t0_film_test.go` : unitaires à fixtures synthétiques (rafale nominale, joueur
      unique, AFK complet, téléportation, trou de réplication, refus rafale < 2, refus
      > 120 s, piste 1 point). 13 cas, dont deux ajoutés : cumul EXACTEMENT sur le seuil
      (0,50 m — le seuil est strict, refus) et dérive lente évincée par la fenêtre.
- [x] A3. Le research test réutilise le détecteur de production (zéro copie de la logique
      de fenêtre glissante — règle ≤ 2 copies) sans changer ses verdicts publiés.
      `t0mPasDeLaPiste`, `t0mPas`, `t0mDepart` et les 3 constantes de seuil SUPPRIMÉS ;
      `t0mAnalysePiste` appelle `t0FilmFirstMovement` et emprunte `t0FilmWindow` pour le
      seul plancher de bruit ; `rafale()` délègue à `t0FilmBurst`. ISO-VERDICT prouvé sur
      le corpus (chiffres au journal ci-dessous).
- [x] A4. `document.go` : `T0FilmMs *int64` `json:"t0FilmMs,omitempty"` (piège omitempty
      documenté sur OriginMs) + chronique + bump `SchemaVersion` 35 → 36 (note collision
      inter-worktrees : renuméroter au merge si un autre lot a pris 36). Le cliquet
      `structure_test.go:603` exige une raison ÉCRITE par version : entrée v36 rédigée.
- [x] A5. Câblage `build.go` à côté de la pose d'`OriginMs` (~l. 528) + verdict dans
      `doc.Coverage` (patron existant). POSÉ APRÈS `buildCoverage` (l. 578) et non à
      l. 528 : le verdict vit dans `doc.Coverage`, qui n'existe qu'à partir de là, et le
      détecteur lit les pistes PUBLIÉES. Garde : rien si `doc.OriginMs == nil`.
- [x] A6. `contracttest/replay_contract_test.go` : `wantReplayDocumentFields` 48 → 49 +
      chronique + champ optionnel non requis. `T0FilmCoverage` ajouté à `replaySchemas`
      dans le même geste (leçon P2-3 du 2026-09-01, appliquée tout de suite).
- [x] A7. Régénérations : `go run ./cmd/openapi-gen` ; `npm run generate-types` (ou le
      script openapi-typescript du package.json web) ; `replaySchemaLogic.ts` 35 → 36.
      `npm ci` a dû tourner d'abord (worktree neuf). Les deux régénérations sont
      PUREMENT ADDITIVES (+31 L yaml, +15 L generated.ts) : aucune dérive préexistante.
- [x] A8. Goldens : `go test ./internal/analysis/replay/ -run GoldenAssembly -update` puis
      run nu vert. Le rendu du golden gagne UNE LIGNE (`renderT0Film`) : un golden qui
      n'aurait bougé que sur le numéro de schéma ne verrouillerait pas le nouveau calque.
      Sur 000d5950 : `coup d envoi date par le film : 26304 ms · 98/99 piste(s) en
      mouvement · rafale 4 · marge depuis la frame 0 : 22700 ms` — soit EXACTEMENT la
      valeur que le research test tire du même match par un chemin disjoint (artefact
      relu, pistes non décimées), et la marge documentaire de 22 700 ms.

Gate A : `go test ./internal/analysis/replay/ ./contracttest/` vert ; test golden openapi
(`TestOpenAPIYAMLIsUpToDate`, tag cgo) vert ; `go vet` propre sur les paquets touchés ;
grep : une seule implémentation de la fenêtre glissante. **PASSÉ le 2026-09-02** — les
quatre, codes de sortie vérifiés (détail au journal).

## Lot B — Lecteur web (fenêtre + préambule)

- [ ] B1. `replayWindow.ts` : borne basse/origine depuis `doc.t0FilmMs` si présent, sinon
      `header.t0_ms` (D5) ; doc de tête mise à jour (la hiérarchie des sources y est dite).
- [ ] B2. `ReplayWindowBounds.leadInFrame` (constante nommée 1 000 ms, D3), calculé avec
      clamp à l'image 0.
- [ ] B3. `useReplayPlayback.ts` : pose initiale, `rewind()` et borne basse de `seekTo`
      passent à `leadInFrame` ; l'horloge (`useReplayClock`), la frise
      (`replayTimelineTracksLogic`) et l'export (`replayExportPlan`) restent sur
      `startFrame`/`startMs` — vérifier sur pièces qu'aucun ne lit la borne via playback.
- [ ] B4. Tests : `replayWindow.test.ts` (préférence doc>header, leadIn, absence de champ),
      `useReplayPlayback.test.tsx` (pose/rewind/seek au leadIn), non-régression
      `useReplayClock.test.tsx` + `replayExportPlan.test.ts`.

Gate B : purge `node_modules\.tmp` puis `npm run typecheck` ; vitest des fichiers touchés ;
`npm run lint` sur le périmètre.

## Lot C — Écritures base (CLI + fil de l'eau)

- [ ] C1. `cmd/backfill_t0_film/main.go` (patron `backfill_t0`) : balaye
      `ReplayArtifactsDir` (PathResolver, jamais de chemin à la main), décode minimal
      (schemaVersion/originMs/frameIntervalMs/tracks), calcule via le détecteur A1,
      dry-run par défaut, `--commit` = UPDATE row-by-row paramétré par match_id
      (`real_start_time`, `t0_quality='film_movement'`) en transaction single-connection ;
      `SQLStartTimeCanonical` pour le start (ratchet no_raw_start_time) ; compte rendu
      complet (réparés / refus D6 / sans artefact exploitable / inchangés).
- [ ] C2. Fil de l'eau : après la cuisson post-sync d'un artefact, reporter son
      `t0FilmMs` dans `match_registry` via un segment WRITER court du sync (UPDATE
      row-by-row, forme autorisée par `no_art_patterns_test`), avec garde : ne jamais
      écraser un `film_movement` existant. Si la plomberie writer du chemin cuisson se
      révèle disproportionnée (> 2x l'estimation), arrêt propre et proposition — pas de
      contournement silencieux.
- [ ] C3. Vérifier sur pièces si un re-sync d'un match existant réécrit
      `real_start_time` (transforms.go:116 + persister) ; si oui, garde « film_movement
      ne s'écrase pas » sur ce chemin aussi.

Gate C : `go test ./internal/sync/... ./cmd/...` ciblés verts ; `no_art_patterns_test` et
`check_lease_enforcement` verts ; **`go test -tags=integration -p 1 ./...` complet**
(persist/sync touchés — obligatoire) avec code de sortie vérifié.

## Lot D — Réparation historique + clôture

- [ ] D1. CLI C1 en dry-run sur le corpus local : compte rendu chiffré au user.
- [ ] D2. `--commit` local (serveur air ARRÊTÉ — un seul writer par DB ; le redémarrer
      ensuite et vérifier le port 8000).
- [ ] D3. Gate visuel user sur témoins nommés : `1b2d9e08` (dégénéré, régime 1) +
      `72b0a25e` (frame 0 sur le coup d'envoi) + 1 match sain au choix du user.
- [ ] D4. Recuisson `backfill-replay --only-existing` pour publier `t0FilmMs` dans les
      artefacts : DEMANDER l'accord user avant lancement (D4 des décisions).
- [ ] D5. Registre (`.ai/V7.5/REGISTRE_REPORTS.md`) : passe de réparation à rejouer en
      PROD au déploiement v7.5.0 ; dette TZ `first_joined_time` (41/101 constatés) déjà
      connue — référencer, ne pas traiter.
- [ ] D6. Entrée thought_log de clôture + delivery-checklist + CI de branche verte au
      niveau job avant de déclarer le lot clos.

## Découvertes (à consigner, ne pas traiter)

- 41/101 matchs du corpus ont un `real_start_time` à ±1 h/2 h exactement (dette TZ
  `first_joined_time` — déjà au registre des dettes).
- `FlagReturnZone` (schéma 35, mergé sur feat/v75 le 2026-09-02) est ABSENT de
  `replaySchemas` dans `contracttest/replay_contract_test.go` : c'est exactement le trou que
  la revue P2-3 du 2026-09-01 a bouché pour `Pickup`/`PickupCoverage`, et il se rouvre au
  champ suivant. Les deux gardes champ↔contrat et le garde omitempty↔required ne voient donc
  pas ce type. NON TRAITÉ (hors périmètre du lot A) — une ligne à ajouter à la table.
- `golangci-lint run --new-from-merge-base=origin/main` rend 2 issues `unparam` sur
  `internal/analysis/filmdec/weapon_hit_distance_resolver.go:178` et
  `internal/sync/killcollector/hits.go:171`. Aucun des deux fichiers n'est touché par ce
  lot : c'est de la dette introduite ailleurs sur `feat/v75` depuis `main`, que le ratchet
  CI rougira au merge. NON TRAITÉ (hors périmètre).

## Reprise de session

Source de vérité = ce fichier (cases + journal ci-dessous). Reprendre à la première case
non statuée du lot courant. Décisions D1-D6 fermes.

## Journal

- 2026-09-02 : plan créé après research test (commit `a5a468a16`) et repérage complet des
  ancrages (schéma 35, précédent originMs `0d5e9ffd5`, patron backfill_t0, consommateurs
  real_start_time exhaustifs, points d'insertion web).
- 2026-09-02 : **LOT A CLOS**, A1-A8 tous `[x]`, aucun report. Gate A passé, codes de
  sortie relevés dans un log persistant (jamais un filtre `FAIL` nu) :
  - `go test ./internal/analysis/replay/ ./contracttest/ -count=1` → `EXIT_GATE=0`
    (`ok ...replay 24,3 s` · `ok ...contracttest 0,5 s`). Premier passage ROUGE sur
    `TestStructureIsOptionalInDocument` — le cliquet de schéma exige une raison écrite
    avant tout bump ; entrée v36 rédigée, aucun garde-rail affaibli.
  - `go test ./internal/api/ -run TestOpenAPIYAMLIsUpToDate` → vert (`ok`), joué APRÈS
    `go run ./cmd/openapi-gen`.
  - `go vet ./internal/analysis/replay/ ./contracttest/ ./internal/api/` → `EXIT_VET=0`.
  - grep fenêtre glissante : `newT0FilmWindow` / `w.sum -= w.dists[0]` n'existent qu'en
    `t0_film.go`, appelés depuis la production ET depuis le research test. (La somme
    glissante de `weapon_burst_wave_test.go` est un autre sujet, pas une copie.)
  - Web (au-delà du gate A, parce que A7 touche `replaySchemaLogic.ts`) : `npm ci` puis
    `npm run typecheck` → `EXIT_TSC=0` ; `vitest replaySchemaLogic.guard.test.ts` → 1/1.
- 2026-09-02 : **ISO-VERDICT du research test PROUVÉ** (A3). Relancé avec `REPLAY_CORPUS`
  + `T0_API_TSV` après refonte, il rend au chiffre près ce que son en-tête documentait
  avant : population 106 artefacts / 106 décodés / 5 sans originMs, RETENUS 49 sains +
  11 dégénérés + 41 aberrants ; RAFALE médiane 6 (min 2, max 20) ; ACCORD 1er→3e 100 ms
  médian, 500 ms au p95 ; **MARGE censure retirée n=83 · médiane 22 700 ms · écart-type
  299 ms · CV 0,013 · min 21 500 · max 23 000** ; CENSURE 18/101 = 17,8 % ; plancher de
  bruit cumul médian 0,060 m ; corpus sain delta 1er joueur médiane +4 523 ms écart-type
  5 949 ms, variante médiane-joueur toujours réfutée à +241 663 ms ; dispersion des deux
  horloges 9 752 ms (film) contre 12 764 ms (API) ; dégénérés 10/11 plausibles dont 4
  censurés, `1b2d9e08` à 31 862 ms, `72b0a25e` à 38 951 ms. Verdicts INCHANGÉS.
- 2026-09-02 : contrôle croisé non prévu au plan et qui vaut d'être noté — le chemin de
  PRODUCTION (build complet, pistes décimées, `DetectT0Film`) et le chemin de RECHERCHE
  (artefact relu, pistes brutes) donnent le MÊME instant sur 000d5950 : 26 304 ms. La
  décimation à 100 ms ne déplace pas le premier mouvement.
