# P0-2 — la fiche du rejeu ne lit que la vie en cours du slot (2026-09-06/07)

Branche `feat/v2-web-vies`, worktree `LevelUp-wt-v2-web-vies`, base `a059caefc` (= `feat/v75`).
Constat P0-2 du registre `.ai/AUDIT_LECTEURS_VIES_ANONYMES_2026-09-06.md` (cherry-pick
`370955a35` -> `40be890cd` sur cette branche, sans conflit). Correctif : commit `61fb96a60`.

## Constat

`apps/web/src/lib/replay/rosterLogic.ts:580-598` (`nearestReading`) cherchait « la dernière
lecture du SLOT » sans jamais regarder la frontière de vie : `best` (la lecture passée la plus
proche) l'emportait TOUJOURS sur `ahead` (le repli protecteur documenté), quelle que soit la vie
à laquelle elle appartenait. Sur un slot recyclé — réapparition normale, ou remplaçant/multi-manche
qui réattribue le slot à un AUTRE joueur — la fiche affichait donc les armes, les munitions et la
capacité de la vie PRÉCÉDENTE, sous une infobulle « Lu il y a X s » qui affirmait une lecture
passée plutôt qu'un « à venir ». Le remède existait déjà dans le dépôt et n'était pas appelé :
`lifeOfSlotAt` (`features/match-replay/model/livesPosition.ts:121`).

Site frère, même défaut : `changeRefine.ts:131-149` (`refineAbilityReading`) ne bornait le
changement d'équipement retenu que par `c.t <= frame`, jamais par le début de la vie — un `spent`
de la vie précédente pouvait faire disparaître la vignette d'une vie neuve qui n'avait pourtant
rien consommé.

## Correctif

- `currentLifeOf(doc, slot, frame)` — nouvelle fonction dans `rosterLogic.ts`, JUMELLE de
  `lifeOfSlotAt` et non un import de celle-ci : `lib/replay/` ne dépend jamais de `features/`
  (sens unique du dépôt, aucun fichier de production ne l'inverse), et les appelants n'ont qu'un
  slot et une image, jamais la map par-slot déjà construite — un balayage direct de `doc.tracks`
  suffit, au même coût que `nearestReading` lui-même. Précédent déjà accepté par l'audit
  (`abilityChargeLogic.ts:133`, écarté comme non fautif pour le même patron).
- `nearestReading<T>(samples, slot, frame, life)` — 4e paramètre `life: {start, end} | undefined`
  (fenêtre de `trackWindow`). Sans vie couvrante : `null` immédiat. Avec vie : `best` et `ahead`
  ne considèrent plus que les échantillons dans `[life.start, life.end]`.
- `loadoutAt`, `abilityAt` (`rosterLogic.ts`) et `inventoryAt`, `grenadeReadingAt`
  (`inventoryReading.ts`) résolvent la vie en cours via `currentLifeOf` avant tout accès aux
  échantillons, et propagent la fenêtre à `nearestReading`.
- `refineAbilityReading` (`changeRefine.ts`) — 5e paramètre `lifeStart: number` ; un changement
  antérieur au début de la vie est écarté même s'il précède `frame`.
- `lastFullBefore` (`inventoryReading.ts`) — 4e paramètre `lifeStart`, même garde : la lecture
  PLEINE de repli ne remonte plus au-delà du début de la vie courante.

**Décision produit reçue en cours de lot (2026-09-06)** : « les vies anonymes n'existent pas ;
une vie est un humain ou un bot » — le nommage des vies se corrige à la SOURCE côté Go (autre
lot), pas ici. Conséquence sur ce lot : le correctif logique (borner à la vie en cours) reste
inchangé, mais l'état de repli affiché n'est PAS « joueur/vie inconnu(e) ». Seul repli légitime :
« aucune lecture encore observée depuis le début de cette vie » — neutre et discret, jamais le
mot « inconnu ». Traduit par :
- ability : même glyphe que le rang lu-mais-non-identifié (`AbilityUnknownMark`, vignette
  pointillée), tooltip `abilityUnread` (« capacité non lue sur cette vie » / « ability not read
  on this life »).
- munitions : un tiret neutre (`—`), tooltip `ammoUnread` (« munitions non lues sur cette vie »
  / « ammo not read on this life »).
- armes : couvert par le libellé PRÉEXISTANT `loadoutUnread` (« armes non lues sur cette vie »),
  déjà sobre, inchangé.

Les deux nouveaux repli sont GARDÉS par la présence documentaire de l'axe (`doc.abilities.length
> 0` / `doc.inventory.length > 0`), même doctrine que `VitalityPresence` (`rosterLogic.ts`) :
un artefact/schéma qui ne porte JAMAIS cet axe reste muet plutôt que d'afficher une lacune
permanente sur chaque fiche. Sans cette garde, deux tests préexistants seraient tombés
(`ReplayTeams.test.tsx` « ni Moi ni Ami… colonne muette », `equipmentUsage`-like scenarios) —
détecté par la suite vitest complète, corrigé avant commit.

## Test par mutation

Deux vies sur le même slot (512), lecture SEULEMENT dans la vie 1, instant demandé dans la vie 2
(slot recyclé, joueur B) :

```ts
const recycled = doc({
  tracks: [track(512, 'A', 0, 50), track(512, 'B', 60, 150)],
  loadouts: [{ t: 10, slot: 512, w: ['0xAAAA'] }],
})
expect(loadoutAt(recycled, 512, 100)).toBeNull()
```

Vérifié ROUGE en neutralisant temporairement la borne (`if (false && (s.t < life.start || s.t >
life.end)) continue`) : le test échoue alors en retrouvant `{ weapons: ['0xAAAA'], age: 90 }` —
exactement la fuite décrite par l'audit. Restauré, le test repasse VERT. Même protocole appliqué
et vérifié pour :
- `abilityAt` (rosterLogic.test.ts) — capacité de la vie précédente.
- `inventoryAt` (inventoryReading.test.ts) — inventaire ET `lastFullBefore` (repli plein).
- `grenadeReadingAt` (inventoryReading.test.ts) — axe grenades.
- `refineAbilityReading` (changeRefine.test.ts) — `lifeStart` neutralisé (`(false && c.t <
  lifeStart)`), un changement d'équipement de la vie précédente redevient candidat, test rouge ;
  restauré, vert.

## Appels vérifiés — aucun ne contourne

`grep -rn "nearestReading(\|refineAbilityReading(\|loadoutAt(\|abilityAt(\|inventoryAt(\|grenadeReadingAt(\|currentLifeOf("`
sur tout `apps/web/src` : tous les appelants de production recensés et mis à jour ou inchangés à
bon droit —
- `loadoutAt`/`inventoryAt` : seul appelant de production `equippedLogic.ts` (`equippedWeapons`),
  signature inchangée (3 args), bénéficie du correctif sans modification.
- `abilityAt` : seul appelant `ReplayAbilityCell.tsx`, signature inchangée.
- `inventoryAt` (ligne) : seul appelant `ReplayInventoryRow.tsx`, signature inchangée.
- `nearestReading`/`refineAbilityReading`/`currentLifeOf` : tous les appelants sont les fonctions
  ci-dessus (rosterLogic.ts, inventoryReading.ts) ou des tests ; aucun appelant tiers.
- `ReplayTeams.tsx:243/257/272` confirmé : appelle déjà avec `state.life.slot`, la vie couvrante
  (la fuite était dans la LECTURE, pas dans le choix de la vie — conforme à la vérification
  adverse de l'audit).

**Hors périmètre, noté et NON traité** (règle « zéro fix opportuniste ») :
- `drawnSwapAt` (`equippedLogic.ts:108-126`) — scanne `doc.inventory` filtré par slot sans borne
  de vie pour détecter la dernière BASCULE du sélecteur d'emplacement ; sur un slot recyclé, la
  dernière lecture de la vie précédente et la première de la vie courante pourraient être lues
  comme un échange et déclencher l'animation à tort. Motif de non-traitement : absent du
  périmètre nommé par l'audit (ni `nearestReading`, ni un « lecteur de la dernière lecture du
  slot » au même patron — c'est une détection de TRANSITION, pas un report de valeur), impact
  visuel mineur (une animation, pas une donnée affichée), et zéro mesure sur le parc. À signaler
  au superviseur pour arbitrage si un futur audit du même axe l'atteint.
- P1-9 (dessin des vies anonymes) : décision utilisateur en attente, non traité (hors mandat de
  ce lot).

## Gates

- `npx tsc --noEmit` (racine `apps/web`) : propre.
- `npx eslint src/lib/replay src/features/match-replay` : 0 erreur, 8 warnings préexistants (hors
  périmètre : `ReplayExportDialog.tsx`, `useReplayVehicles.ts`, `useReplaySound.ts`,
  `ReplayFeedName.tsx` — react-refresh/react-hooks, non touchés par ce lot).
- `npx vitest run src/lib/replay` : 9 fichiers / 203 tests verts.
- `npx vitest run src/lib/replay src/features/match-replay` : 170 fichiers / 2529 tests verts.

## Fichiers touchés

`apps/web/src/lib/replay/rosterLogic.ts`, `rosterLogic.test.ts`, `changeRefine.ts`,
`changeRefine.test.ts` ; `apps/web/src/features/match-replay/model/inventoryReading.ts`,
`inventoryReading.test.ts`, `equippedLogic.test.ts` ; `apps/web/src/features/match-replay/ui/
ReplayAbilityCell.tsx`, `ReplayInventoryRow.tsx` ; `apps/web/src/features/match-replay/i18n/
i18n.ts`, `i18nContract.ts`.

Registre `.ai/AUDIT_LECTEURS_VIES_ANONYMES_2026-09-06.md` : ligne d'état P0-2 ajoutée (« CORRIGÉ,
branche `feat/v2-web-vies`, commit `61fb96a60` »).

## Corrections R1 (revue adversariale WEB-R1, 2026-09-07)

Verdict complet : voir la revue WEB-R1 (constats C1/C2, 6/8 invariants tenus sans réserve).
Diagnostic : correctif logique EXACT (rejoué et confirmé), mais l'invariant 8 du contrat
(« chaque borne ajoutée a un test qui rougit si on la retire ») ne tenait pas intégralement —
deux trous de test P1, tous deux réparables sans toucher au code de production livré.

### C1 — la moitié HAUTE de la borne de vie (`s.t > life.end`) n'était protégée par aucun test

Tous les tests « slot recyclé » du lot initial plaçaient la lecture parasite AVANT la vie
interrogée (vie 1 lue, requête en vie 2) — ils n'exerçaient que la moitié BASSE
(`s.t < life.start`). Le cas symétrique (lecture d'une vie ULTÉRIEURE, requête depuis une vie
ANTÉRIEURE sans lecture propre) n'était testé nulle part : retirer `|| s.t > life.end` seul
laissait les 2529 tests du lot initial verts.

**Correctif** : un test symétrique par lecteur borné —
- `loadoutAt`, `abilityAt` (`rosterLogic.test.ts`), `inventoryAt`, `grenadeReadingAt`
  (`inventoryReading.test.ts`) : vie 1 [0,50] sans lecture propre, seule lecture disponible en
  vie 2 [60,150] (t=70), requête à frame=20 (dans la vie 1) → attendu `null`.
- `refineAbilityReading` (`changeRefine.test.ts`) : cette fonction n'a pas de paramètre
  `life.end` séparé — `c.t > frame` (préexistant, jamais retiré par ce lot) joue ce rôle de
  fait, `frame` étant toujours `<= life.end` par construction de l'appelant. Testé
  symétriquement (changement en vie ultérieure, requête en vie antérieure).
- `lastFullBefore` (`inventoryReading.test.ts`) : **pas de borne haute qui lui soit propre, et
  c'est une preuve vérifiée, pas une lacune** — quand `frame` couvre une vie V, toute lecture
  d'une vie ultérieure a `s.t >= vie_suivante.start > V.end >= t`, donc son propre filtre
  `s.t < t` l'exclut structurellement avant même `lifeStart`. Documenté en commentaire de test
  (pas dans le code de production) ; un test verrouille le comportement sans revendiquer une
  ligne de mutation qui n'existe pas.

**Mutation avant/après** (`if (s.t < life.start || s.t > life.end) continue` →
`if (s.t < life.start || (false && s.t > life.end)) continue`, restauré ensuite — `git diff`
vide confirmé) :

| Lecteur | Rouge (borne haute neutralisée) | Vert (restaurée) |
|---|---|---|
| `loadoutAt` | `{ weapons: ['0xFFFF'], age: -50 }` au lieu de `null` | `null` |
| `abilityAt` | `{ rank: 9, age: -50, src: 'kf' }` au lieu de `null` | `null` |
| `inventoryAt` | lecture de la vie 2 (âge -50) au lieu de `null` | `null` |
| `grenadeReadingAt` | lecture de la vie 2 (âge -50) au lieu de `null` | `null` |
| `lastFullBefore` (test dédié) | **reste VERT** (preuve empirique du finding ci-dessus : `best`, propre à la vie courante, l'emporte déjà) | VERT |

Mutation séparée pour `refineAbilityReading` (`c.t > frame` → `(false && c.t > frame)`,
restauré ensuite) : le nouveau test ET le test préexistant « ignore les changements à venir et
ceux d'un autre slot » rougissent TOUS LES DEUX (même mécanisme partagé, confirmé) —
`{ rank: 7, age: -30, src: 'chg' }` / `{ rank: 20, age: -20, src: 'chg' }` au lieu de leurs
valeurs attendues.

### C2 — ce lot avait lui-même neutralisé un test préexistant (« une lecture passée prime toujours une lecture à venir »)

La fixture partagée `d` du bloc `describe('loadoutAt', ...)` (ajoutée par ce lot : `tracks:
[track(512, 'A', 0, 100), ...]`) faisait tomber le candidat `ahead` légitime (`t=200`) hors des
bornes de vie (`life.end=100`) AVANT même que `best`/`ahead` ne soient comparés. Le test
préexistant « une lecture PASSÉE prime toujours la lecture à venir » ne voyait donc plus qu'UN
SEUL candidat et passait pour une raison différente de celle qu'il prétendait vérifier — le
commentaire ajouté par ce lot (« le seul facteur testé reste la présence/absence d'une lecture
— pas la vie elle-même ») était FAUX pour ce test précis.

**Correctif** : vie du slot 512 étendue à `[0, 250]` (couvre `t=200`) dans la fixture partagée ;
commentaire faux remplacé par l'explication du gap et du correctif ; test préexistant conservé
tel quel (assertion inchangée, `age === 50`) mais redevenu réellement protecteur. Ajout d'un
test dédié et ISOLÉ (« lecture PASSÉE et lecture À VENIR toutes deux dans la MÊME vie »,
`rosterLogic.test.ts`), indépendant de toute fixture partagée, pour que ce verrou ne puisse
plus être neutralisé par un futur ajustement de fixture sans qu'un test named échoue
explicitement.

**Mutation avant/après** (`return best ?? ahead` → `return ahead ?? best`, restauré ensuite —
`git diff` vide confirmé) :

| Test | Rouge (best/ahead inversés) | Vert (restauré) |
|---|---|---|
| `loadoutAt` — « rend la dernière lecture du SLOT » (effet de bord, fixture partagée) | âge -140 au lieu de 50 | âge 50 |
| `loadoutAt` — « une lecture PASSÉE prime toujours la lecture à venir » (réparé) | âge -140 au lieu de 50 | âge 50 |
| `loadoutAt` — test dédié isolé (nouveau) | `{ weapons: ['0xFFFF'], age: -20 }` au lieu de `{ weapons: ['0xAAAA'], age: 50 }` | valeur attendue |

### Gates rejoués après R1

- `npx tsc --noEmit` : propre.
- `npx eslint src/lib/replay src/features/match-replay` : 0 erreur, mêmes 8 warnings
  préexistants hors périmètre.
- `npx vitest run src/lib/replay src/features/match-replay` : **170 fichiers / 2536 tests
  verts** (2529 + 7 tests ajoutés par R1).
- `git diff` sur `rosterLogic.ts` et `changeRefine.ts` : **vide** — aucun code de production
  changé par R1, conformément au périmètre STRICT de la revue (tests seuls).

Commit R1 : voir hash dans le rapport de session / `git log`.
