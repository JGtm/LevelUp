# P-décodeur E2 — le lien DIRECT slot de bipède ↔ index de joueur, intégré

> Lot **P-décodeur E2** du plan d'orchestration `.ai/PLAN_ORCHESTRATION_2026-09-07.md` §4
> (vague 3, décisions D12/D13). Contrat d'exécution : skill `plan-execution`.
> Source du lien : `.ai/V7.5/film_re/SONDAGE_E2_BIPEDE_INDEX_2026-09-08.md` (spécification
> bit-exacte §2.2, mesures `mesures_e2_2026-09-08/`, plan d'intégration §4).
>
> Base : `feat/v2-p2-registre-joueurs` (`2654e5e04`, registre P2 + P2-bis) fusionnée avec
> `feat/v2-sondage-e2` (docs seulement). Branche `feat/v2-decodeur-e2`, worktree
> `LevelUp-wt-e2`. **Bump 50 inchangé** : P n'est pas fusionné, le contenu cuit change encore
> sous ce numéro (cf. la chronique 50, amendée par ce lot).

---

## I0 — LE CAS DES BOTS : instruit sur pièces AVANT toute ligne de code

Le sondage laissait la question ouverte (§2.2, « Cas des bots : à VÉRIFIER avant intégration »)
et posait la règle d'attente : « tant qu'il n'est pas tranché, l'intégration doit COMPTER les
index hors table plutôt que les publier ». Voici ce que les octets soutiennent.

### I0.1 Le fait, sur `c75f33b8` (le seul film à bot du matériau)

```
identity.players    humains filmIndex = 0..7, 9, 10   (10 humains, TROU à 8)
                    bot     filmIndex = 11            (« 343 Flippant », BOT_METADATA)
lectures de creation (sondage, 89 records)
                    ref5 = 0:4 1:14 2:11 3:9 4:12 5:9 6:3 7:14  8:2  9:8 10:3
                    index 11 : JAMAIS lu ; index 8 : lu 2 fois, absent d'identity.players
```

Les DEUX vies que ces deux lectures désignent, dans l'artefact cuit (schéma 50) :

```
532  [783..801]    xuid vide   source non_resolu   method (aucune)
591  [3910..4009]  xuid vide   source non_resolu   method (aucune)
```

### I0.2 Quatre pièces qui tranchent, et elles vont toutes dans le même sens

1. **Sur les quatre films SANS bot, l'ensemble des index lus est EXACTEMENT l'ensemble des index
   humains** — `d9781168`, `bf15f7ab`, `64e8adfa`, `3372e7eb` : `ref5 ∈ {0..7}`, huit humains,
   rien d'autre. Le champ ne porte donc pas un espace parallèle : il porte l'index de
   participant que `PlayerIndexTable` publie.
2. **Sur `c75f33b8`, l'ensemble lu est exactement `{index humains} ∪ {8}`, et 8 est LE TROU** de
   l'ensemble humain dans `[0,10]`. Un espace disjoint n'aurait aucune raison de tomber
   précisément dans le trou de l'autre.
3. **Les deux vies qui lisent 8 sont exactement deux vies que RIEN ne nomme.** Le pont par
   morts ne peut pas les nommer : une mort de bot ne porte aucun xuid. C'est la signature d'un
   participant non humain, pas d'un bruit de décodage.
4. **Le dépôt tenait déjà l'espace pour partagé** : `buildRoster` (`replay/identity.go:182`)
   refuse un bot « dont l'index est tenu par un humain » — règle qui n'a de sens que si le
   `slot` de `BOT_METADATA` et l'index humain vivent dans LE MÊME espace. Et `botmeta.go` le dit
   en toutes lettres : « slot = indice ABSOLU du bot dans le roster de réplication ».

**Corroboration indépendante hors matériau du sondage** : `bcb6d393`, film SANS aucun
`BOT_METADATA`, porte 11 humains d'index `0..7, 9, 10, 11` — **le même trou à 8** — et sa feuille
de match liste quatre bots (`bid(35.0)`, `bid(38.0)`, `bid(41.0)`, `bid(59.0)`, ce dernier avec
une mort). Le trou à 8 n'est donc pas « le siège du bot déclaré » : c'est un siège occupé par un
participant que `PlayerIndexTable` ne nomme pas.

### I0.3 Verdict — (a), avec la conséquence de publication de (b) sur l'index 8

**(a) L'index absolu des bots vit dans la MÊME table que celui des humains.** Le champ
`ECS_ReadEntityRefIndex5` du record de création est un index de participant du film, unique pour
tous les participants, humains et bots confondus.

**Et pourtant l'index 8 ne se publie pas comme un lien.** La feuille de `c75f33b8` compte
**14 participants** (10 humains + `bid(21.0)`, `bid(26.0)`, `bid(34.0)`, `bid(4.0)`) alors que le
film ne DÉCLARE qu'un seul bot, au siège 11. L'occupant du siège 8 est donc l'un des trois bots
non déclarés, et **rien ne permet de l'identifier** — l'équivaloir au bot déclaré au siège 11
serait une supposition publiée comme un lien, ce que la doctrine interdit. Règle implémentée,
uniforme et sans cas particulier de titre :

| Index lu | Présence dans `identity.players` | Publication |
|---|---|---|
| trouvé, humain (xuid) | oui | **lien `direct`**, méthode `creation_bipede` |
| trouvé, bot déclaré (pas de xuid) | oui | pas de xuid à poser : la vie reste nommée par ses voies actuelles, la lecture est COMPTÉE à part (`indexBot`) — jamais une alarme |
| absent de la table (ici 8) | non | **`non_resolu`**, cause **`index_hors_table`**, compteur + `slog.Warn`, JAMAIS rattaché |

Ce que le verdict (a) change par rapport au repli (b) du sondage : le jour où un record lira
l'index d'un bot DÉCLARÉ, on saura que c'est lui — on ne le comptera pas comme une anomalie de
lecture. Ce qu'il ne change pas : sur le matériau d'aujourd'hui, les deux lectures de `c75f33b8`
restent `index_hors_table`, et le bot garde ses voies de nommage actuelles.

### I0.4 Découverte annexe, NON traitée (règle 7)

`replaybuild.botIdentities` (`internal/replaybuild/replaybuild.go:454`) construit
`replay.BotIdentity{FilmIndex: b.Slot, Name: ...}` **sans reporter `b.BotID`**. `BotIdentity.Bid()`
rend donc toujours la chaîne vide, et `roster[].bid` / `identity.players[].bid` sont **absents de
tous les artefacts cuits** — vérifié sur `c75f33b8` (le seul film à bot du matériau : la clé
`bid` n'existe pas dans son roster). Le lot P2 annonce ce champ comme publié (§2, changement 2) ;
il est publiable côté `replay` mais son producteur ne le remplit pas. Hors périmètre E2 (le
correctif d'une ligne change le contenu cuit de tous les films à bot).
*Condition de reprise : le lot web de jointure des bots par `bid` (découverte 1 de P2), qui en
dépend et qui ne peut pas aboutir sans lui.*

---

## I1 — Le lecteur `ScanBipedCreations` (commit `67fbbac0c`)

`apps/go-api/internal/analysis/filmdec/biped_creation.go` (310 L). Il rejoue les TROIS premières
feuilles du prologue de `consumeBipedDefaultState` à l'intérieur d'une ancre NEW `ti=35`, et rend
`BipedCreation{Slot, Generation, ParticipantIndex, HasIndex, Chunk, PacketIndex, TimestampUS,
BitPos, Version, Representation}`. Clé de vie : `LifeKey() = slot | gen<<16`.

**Le gate est la SIGNATURE, pas l'ancre i0.** `runCreationWalk` est inatteignable ici : le
default-state du bipède n'est porté qu'à ~120 bits sur ~380, donc l'offset d'i0 calculé après lui
est faux par construction (sondage §2.3 : 1 847 ancres, 181 acceptées, 170 à plus de 10 000 quanta
de la trajectoire réelle — 90 % de bruit). Le gate retenu détermine 43 bits : `g0 = 1`,
`version = 13`, `gRep = 1`, `representation = 0x1876BDA0`, porte inversée du ref OUVERTE.

Les refus sont comptés SÉPARÉMENT, et les catégories ne sont pas décoratives :

| Compteur | Ce qu'il dit |
|---|---|
| `Anchors` | en-têtes NEW `ti=35` reconnus (3 constantes de format + bande de slots) — le dénominateur |
| `ShapeBad` | rejet ORDINAIRE de l'ancrage bit à bit (porte fermée, version différente de 13) — n'alarme rien |
| `SignatureMismatch` + `OtherWord`/`OtherWordCount` | forme complète mais autre mot de 32 bits — LE compteur à surveiller |
| `GateClosed` | signature reconnue, porte de l'index fermée — mesuré à 0 sur 529 records |
| `Truncated`, `Accepted` | débordement de payload, records rendus |

L'alarme que l'appelant arme est `Anchors > 0 && Accepted == 0` (un film dont les bipèdes
porteraient une autre représentation), jamais `SignatureMismatch > 0` seul : ce compteur a un
plancher de bruit d'ancrage, il se juge en ordre de grandeur contre `Accepted`.

`matchWorldObjectNewHeaderIn` : la reconnaissance d'en-tête NEW existante d'`equipment_creation.go`,
sa bande passée en PRÉDICAT plutôt qu'en `map`. La bande du bipède est dense
(`slot_band_dense.go`) parce qu'elle est interrogée une fois par bit candidat ; la reconvertir en
map annulerait le gain. Une seule reconnaissance, pas une copie qui divergerait.

**Mutations jouées** (`biped_creation_test.go`, 382 L) :

| Mutation | Test | Ce qu'il exige |
|---|---|---|
| un bit parasite avant la porte inversée (index à +68 au lieu de +67) | `TestCreationBipedeDecalerLIndexDUnBitLeChange` | le lecteur ne doit PLUS rendre 3 |
| mot de 32 bits `0xDEADBEEF` | `TestCreationBipedeRefuseUneAutreSignature` | 0 record, `SignatureMismatch = 1`, mot alternatif publié |
| version 12 | `TestCreationBipedeRefuseUneAutreVersion` | `ShapeBad = 1`, `SignatureMismatch = 0` |
| porte inversée à 1 | `TestCreationBipedePorteFermeeNEstPasUnIndexNul` | 0 record — une porte fermée n'est PAS l'index 0 |
| `ti=37` sur un slot de la bande | `TestCreationBipedeIgnoreUnAutreArchetype` | 0 ancre |
| 1 000 ancres PARFAITES au prologue pseudo-aléatoire | `TestCreationBipedeTemoinFantome` | 0 passe — le plancher du gate |

Instrument sur film réel derrière `BIPED_CREATION_FILM` (`biped_creation_film_test.go`), témoin
fantôme décalé de 4 096 par le MÊME décodeur. `t.Skipf` sans son film : c'est la forme des vingt
instruments de film du paquet, le dépôt ne versionne aucun film.

---

## I1b — INSTRUCTION DU RÉSIDU : les 53 vies sans record de création lu, une par une

Matériau : les cinq TSV de mesure du sondage (`.ai/V7.5/film_re/mesures_e2_2026-09-08/`, une
ligne par lecture, colonne `vie` = la vie de l'artefact que sa date désigne) confrontés aux
`identity.bipedSlots` des artefacts cuits au schéma 50 (`gate_p2_work/data/cache/replays/`).
Aucune re-lecture de film n'a été nécessaire : la mesure était déjà versionnée.

### I1b.1 Le fait qui commande tout : UN SEUL RECORD PAR SLOT, ET LA GÉNÉRATION NE VARIE PAS

| Film | slots portant des vies | slots portant une lecture | slots à DEUX lectures | générations lues |
|---|---|---|---|---|
| `d9781168` | 160 | **160** | 0 | `1` sur 160 |
| `bf15f7ab` | 83 | **83** | 0 | `1` sur 83 |
| `64e8adfa` | 138 | **138** | 0 | `1` sur 138 |
| `3372e7eb` | 38 | **38** | 0 | `1` sur 38 |
| `c75f33b8` | 80 | **80** | 0 | `1` sur 80 |

La bijection est exacte DANS LES DEUX SENS sur les cinq films : zéro slot avec des vies et sans
lecture, zéro lecture sur un slot sans vie.

Deux conséquences directes, et elles ferment deux des quatre causes du brief :

- **cause (b) « record présent mais non lu » : ZÉRO.** Le rendement de la passe par signature est
  déjà de 100 % des slots. Il n'y a pas de lecteur à améliorer — rien ne lui échappe.
- **cause (d) « record réellement absent du film » : ZÉRO.** Aucun slot porteur de vies n'est sans
  record.

La note du sondage « 146 lectures pour 141 vies sur `64e8adfa` (6 vies portent deux lectures) »
était un artefact du DÉDOUBLEMENT passe A / passe B de la sonde : après déduplication par
`(slot, tsUS)` il reste 138 lectures pour 138 slots. Les « deux lectures d'une même vie » étaient
la même lecture vue deux fois.

**Le corollaire est fort : dans un film, un slot de bipède est UN CORPS, du début à la fin.** Le
pool de handles ne reboucle pas à l'échelle d'un film (la génération reste à 1 partout), et la
« migration du slot aux réapparitions » que documente `lives.go` est exacte au sens où un NOUVEAU
corps prend un NOUVEAU slot — jamais au sens où un slot changerait de corps.

### I1b.2 Le résidu : 53 vies, et leur cause

| Film | vies | vies désignées (`direct`) | résidu | (a) propagation | (b) non lu | (c) index hors table | (d) sans record |
|---|---|---|---|---|---|---|---|
| `d9781168` | 176 | 159 | 17 | **17** | 0 | 0 | 0 |
| `bf15f7ab` | 91 | 79 | 12 | **12** | 0 | 0 | 0 |
| `64e8adfa` | 141 | 134 | 7 | **7** | 0 | 0 | 0 |
| `3372e7eb` | 42 | 38 | 4 | **4** | 0 | 0 | 0 |
| `c75f33b8` | 88 | 75 | 13 | **11** | 0 | **2** | 0 |
| **total** | **538** | **485** | **53** | **51** | **0** | **2** | **0** |

Les deux lignes (c) sont les vies `532 [783..801]` et `591 [3910..4009]` de `c75f33b8`, dont le
record lit `ref5 = 8` — l'index absent d'`identity.players`, tranché à l'item I0 : `non_resolu`,
cause `index_hors_table`, jamais rattaché.

### I1b.3 Les 51 vies de la cause (a), rangées par figure

Chacune est sur un slot dont le record de création EST lu ; la découpe à `lifeGapUS` (5 s) a
séparé un séjour continu du même corps. Trois figures, et elles s'expliquent toutes :

**(a1) Le trou d'ouverture — 24 vies.** Le record est lu à la frame 0 (ou avant l'axe,
`frame = -1`), le corps apparaît quelques frames, disparaît pendant le compte à rebours, et revient
vers la frame 225. Exemple `d9781168` slot 514 : lecture à la frame 0 `ref5 = 2`, vies `514[0..37]`
(désignée) et `514[228..441]` (résidu). Les 24 : `d9781168` slots 514 à 519 (dont 518 en porte 3) ;
`bf15f7ab` 514, 516, 517, 519 puis 512, 513, 515, 518 en lecture `hors_vie` ; `64e8adfa` 512, 514,
517, 519 (`hors_vie`) ; `3372e7eb` 513, 516, 517, 519 ; `c75f33b8` 513, 514, 515, 518, 519 (dont
513 en porte 2) puis 512, 516, 517 (`hors_vie`).

**(a2) La lecture HORS VIE — 12 slots.** Le record est daté AVANT la première position répliquée
du slot (jusqu'à 23 s d'écart sur `d9781168` slot 513 : lecture à la frame 0 contre une vie
`[233..463]`), ou avant l'origine de l'axe (`frame = -1`). Aucune vie ne CONTIENT sa date : le slot
n'a alors AUCUNE vie `direct`, toutes ses vies sont propagées. Cela ne remet pas la lecture en
cause : la création d'une entité précède sa réplication, c'est l'ordre du moteur.

**(a3) La coupure en pleine partie — 15 vies.** Un trou de plus de 5 s dans la réplication d'un
corps vivant : embarquement en véhicule, occultation, perte de paquets. Exemples : `d9781168`
`558[2256..2629]` (création frame 2143, vie désignée `558[2168..2168]`), `587[3482..3492]`,
`603[4583..4772]`, `637[5878..5884]`, `637[5969..6040]` ; `64e8adfa` `534[2236..2629]`,
`527[765..765]` ; `bf15f7ab` `557[3225..3389]`, `568[3796..4549]`, `575[3895..4046]` ;
`c75f33b8` `528[804..882]`, `582[3643..3751]`, `513[1113..1113]`.

### I1b.4 Ce que la propagation CORRIGE, et qui n'est pas un gain de couverture

Le slot 637 de `d9781168` porte quatre vies, un seul record de création (`ref5 = 6`, frame 5660),
et le pont par morts y nomme `[5660..5827]` d'un joueur et `[6112..6192]` d'un AUTRE — c'est la
`slotCollision` instruite au §B.8 de P2-bis, celle qui rendait le slot ambigu, donc muet pour
`XUIDAt`, `PontDeSlot` et `PontEpure`. **Le film dit qu'il n'y a qu'un corps sur ce slot** : la
collision est une erreur du pont, pas une propriété du film. Le direct l'emporte, la collision
disparaît, et le slot redevient lisible. Même figure sur `fb1a1a72` (hors matériau de mesure E2).

De même, la contradiction du slot 641 (`d9781168`, découverte 2 de P2-bis : zéro occupant possible,
les huit joueurs ayant une vie nommée qui chevauche) vient d'une vie nommée à tort par le pont ; le
direct la retire du problème sans qu'on ait à instruire le pont.

### I1b.5 Couverture projetée — le chiffre du brief

| Film | vies | `direct` | `direct_propage` | `non_resolu` | `direct` total |
|---|---|---|---|---|---|
| `d9781168` | 176 | 159 | 17 | 0 | **100,0 %** |
| `bf15f7ab` | 91 | 79 | 12 | 0 | **100,0 %** |
| `64e8adfa` | 141 | 134 | 7 | 0 | **100,0 %** |
| `3372e7eb` | 42 | 38 | 4 | 0 | **100,0 %** |
| `c75f33b8` | 88 | 73 | 13 | 2 (`index_hors_table`) | **97,7 %** |

Objectif du brief (au moins 95 % de vies en `direct`, propagés compris, `non_resolu` entièrement
ventilé par cause) : **atteint sur les cinq films**, et le `non_resolu` restant tient en une cause
unique, déjà tranchée à I0.

### I1b.6 La règle de propagation, et ce qu'elle REFUSE

Ce n'est pas une déduction : c'est le MÊME record appliqué au MÊME corps. Formulée pour tenir
au-delà du matériau mesuré :

> Les records de création d'un slot sont groupés. **S'ils portent tous le MÊME index**, cet index
> nomme toutes les vies du slot : `direct` pour la vie qui contient la date d'un record,
> `direct_propage` pour les autres. **S'ils divergent**, seules les vies contenant une date de
> record sont nommées (par leur propre record) ; les autres vies du slot deviennent `non_resolu`,
> cause `lectures_divergentes`, comptée et alarmée.

Le refus est mesuré à zéro aujourd'hui (un seul record par slot sur 538 vies), et c'est
précisément pourquoi il doit exister : le jour où le pool de handles rebouclerait dans un film plus
long, la propagation par slot deviendrait fausse sans lui.

---

## I2 / I3 / I4 — le registre, le pont déclassé, le collecteur

### I2.1 L'ordre des étapes, amendé

`BuildIdentityRegistry` compose désormais dans cet ordre, et il n'est pas négociable :

1. **le lien DIRECT corps ↔ joueur** (`identity_registry_creation.go`), posé d'abord et à 100 % ;
2. **le pont par morts en TÉMOIN** (`identity_registry_bridge.go`) : il pose la cause de fin et
   confronte, il ne nomme plus ;
3. l'élimination sur le roster (P2), inchangée ;
4. l'exclusion temporelle (P2-bis), inchangée ;
5. ce qui résiste est publié `non_resolu` **avec sa cause**.

**« Direct » est la vie que le record OUVRE.** La première re-cuisson a rendu `direct = 0` et
`propage = 176` : aucun record de création ne tombe dans l'intervalle d'une vie, parce que le
moteur crée l'entité puis réplique ses positions dans un paquet ULTÉRIEUR (jusqu'à 23 s plus tard
quand la réplication du slot démarre en retard). La règle retenue est donc « la première vie du
corps qui n'est pas déjà terminée à la date du record, et qu'aucun record antérieur n'a déjà
ouverte » : exactement un `direct` par record, et `direct_propage` compte précisément les vies que
la découpe à `lifeGapUS` a ajoutées.

### I2.2 Ce que la couverture publie

`canonical.BipedLinkCounts` **embarque** `LinkCounts` : `direct` / `deduit` / `non_resolu` restent
au même niveau dans le JSON, et le gate corpus continue de les lire sans rien savoir du type. S'y
ajoutent `direct_propage` (sous-compte de `direct` — le brief dit « direct dont propagés ») et
`non_resolu_par_cause{index_hors_table, sans_record, lectures_divergentes}`. La cause voyage dans
`Link.Method`, comme `conteste` le fait déjà. **Invariant vérifié par test** : la somme des causes
ÉGALE `non_resolu`.

`ownersFromCreations` complète le pont slot → index pour les corps que les vies nommées ne peuvent
pas porter : un bot n'a pas de xuid, donc `ownersFromLives` ignorait son slot. Le record, lui,
porte l'index quel que soit le participant — le nommage des pistes de bot gagne une source DIRECTE
là où il vivait d'une fermeture.

### I3 — La seule branche où le pont nomme encore, et pourquoi elle est datée

`nameLivesByDeaths` est SUPPRIMÉ. Reste `nommerParLesMorts`, appelée dans un seul cas : le registre
n'a reçu **aucun** record de création. Ce n'est pas un repli par vie — c'est la dégradation
complète et publiée (`coverage.bridge.bridgeNamedLives`) d'un producteur sans ce canal.

Elle existe parce que `testdata/inputs_000d5950.bin.gz`, le fixture d'assemblage, est **figé avant
ce canal** et que son film (`000d5950`) n'est plus au parc — vérifié : 466 films, celui-là absent,
donc le fixture ne peut pas être régénéré aujourd'hui. Sans cette branche, le golden d'assemblage
tomberait de 483/519 tirs à 0 et cesserait de tester l'assemblage. Bascule 2026-09-08, critère de
retrait mesurable écrit dans `identity_registry_bridge.go` (`bridgeNamedLives == 0` sur toute
cuisson de film du parc), condition de reprise : régénérer le fixture, ou le repointer sur un film
du parc.

**Sur les cinq films mesurés, `bridgeNamedLives = 0`.** L'objectif « 0 vie nommée par le pont » est
tenu partout où le film porte ses records.

### I4 — Le collecteur

`buildPositionRows` lit les créations sur le MÊME film et sous le MÊME verrou de décodage que les
positions, et les passe au registre via `lecturesDuFilm` (les quatre lectures groupées : le dépôt
borne à cinq paramètres). Couture verrouillée par
`TestEntreeDuRegistrePorteLesCreationsDeBipede` — son retrait ne cassait AUCUN autre test.

`IsolationDecoderRev` : `isolement-2026-09-08-registre` (posée par P2, **jamais livrée**) →
`isolement-2026-09-08-creation-bipede`. Elle ne pouvait pas être conservée : un poste qui aurait
joué le backfill sur la branche P2 porterait déjà l'ancienne valeur, et ses lignes seraient exclues
à vie du redécodage alors que leur contenu change de nouveau, à la racine.

### I2-I4 — mutations jouées (sorties collées)

| Mutation | Test qui rougit | Sortie |
|---|---|---|
| le pont nomme sans condition ET écrase le direct | `TestPontParMortsNEcrasePasLeLienDirect` | `slot 100 : xuid = 222, attendu 111 — le pont a echange les deux noms que le film ecrit` |
| idem | `TestPontParMortsNeNommeAucuneVieQuandLeFilmNomme` | `vies nommees par le pont = 3, attendu 0` |
| propagation retirée (`indexPour` sans repli) | `TestCreationSePropageAuxAutresViesDuMemeCorps` | `seconde vie du slot 100 : xuid = 0, attendu 111 (meme corps)` |
| lectures divergentes départagées par le premier index | `TestCreationSeTaitSurDesLecturesDivergentes` | `une vie a ete nommee (111) malgre deux lectures divergentes sur son corps` |
| index hors table rattaché au premier xuid connu | `TestCreationNeRattachePasUnIndexHorsTable` | `le slot 200 porte le xuid 222 alors que son index (8) n'est pas publie` |
| cause du non-résolu non ventilée | `TestCouvertureBipedeVentileChaqueNonResolu` | `causes = 0, non_resolu = 1` sur les TROIS cas |
| `closedBy` retiré de `prefixesMethode` | `TestFermeturesSontDesVoiesPasDesRichesses` | `coverage.bridge.closedByShot n'est pas lu comme une voie` |
| `noBridge` retiré des compteurs d'échec | `TestNoBridgeEstUnCompteurDEchec` | `coverage.bombCarries.noBridge n'est pas lu comme un echec` |

### Structure et garde-rail

`identity_registry.go` passait à 505 L pour un seuil de 500 : la CONSTRUCTION du pont brut sort
dans `identity_registry_pont.go` — **troisième moitié du même producteur**, troisième entrée datée
de l'allowlist `archlint`. Les TROIS décideurs (`_creation.go`, `_elimination.go`, `_exclusion.go`)
et le témoin `_bridge.go` restent **hors** allowlist : ils travaillent sur `[]lifeSpan`, jamais sur
les tables brutes.

Le garde-rail de contrat `jsonFieldsOf` (contracttest) **aplatit désormais les structs embarqués** :
sans cela il accusait le contrat exact d'oublier un champ « LinkCounts » qui n'existe dans aucun
JSON, et de promettre cinq propriétés que le Go publie pourtant. Premier type embarqué du contrat,
premier révélateur.

---

## I5 — MESURE : re-cuisson locale des cinq films

Binaire compilé depuis ce worktree (`CGO_ENABLED=0 go build ./cmd/replay-build`), cuisson dans la
racine de travail du gate P2 (`LEVELUP_REPO_ROOT=<scratchpad>/gate_p2_work/cuisson-base`), mêmes
films, mêmes feuilles, **même schéma 50** (P n'est pas fusionné). « Avant » = les artefacts P2-bis
(`gate_p2_work/data/cache/replays/`), « après » = cette cuisson.

### I5.1 Identité des vies

| Film | `bridge.unnamedLives` | `bipedSlot` avant `{deduit / non_resolu}` | `bipedSlot` après `{direct (dont propagés) / non_resolu}` | couverture directe |
|---|---|---|---|---|
| `d9781168` | **15 → 0** | 152 / 24 | **176 (16)** / 0 | **100 %** |
| `bf15f7ab` | 0 → 0 | 84 / 7 | **91 (8)** / 0 | **100 %** |
| `64e8adfa` | **7 → 0** | 132 / 9 | **141 (3)** / 0 | **100 %** |
| `3372e7eb` | **8 → 0** | 32 / 10 | **42 (4)** / 0 | **100 %** |
| `c75f33b8` | **14 → 1** | 62 / 26 | **86 (8)** / **2** | **97,7 %** |

Les deux `non_resolu` de `c75f33b8` sont ventilés `index_hors_table = 2` — les vies `532` et `591`,
dont le record lit `ref5 = 8`, l'index que `identity.players` ne publie pas (verdict I0). **Aucun
`sans_record`, aucune `lectures_divergentes` sur les cinq films.**

Ces chiffres sont **exactement** ceux que l'instruction du résidu (I1b) prédisait à partir des TSV
du sondage, sans relire un seul film.

### I5.2 Le pont, en témoin

| Film | `concordant` | `discordant` | `bridgeNamedLives` |
|---|---|---|---|
| `d9781168` | 127 | **13** | **0** |
| `bf15f7ab` | 76 | 0 | **0** |
| `64e8adfa` | 118 | **4** | **0** |
| `3372e7eb` | 29 | 0 | **0** |
| `c75f33b8` | 50 | **10** | **0** |

27 vies portaient le joueur que le film écrit sur une AUTRE vie. Le sondage en annonçait 13 sur
`d9781168` et 5 sur `64e8adfa` — le compte diffère de 1 sur `64e8adfa` et de 4 sur `c75f33b8` parce
que la sonde comparait au nommage du pont SEUL, alors que la mesure ici compare au nommage
d'aujourd'hui (pont + fermetures + élimination + exclusion temporelle).

### I5.3 Ce que les calques y gagnent — et aucun ne perd

| Film | tirs rattachés | tirs `noSlot` | tirs `ambiguous` | `slotCollisions` | `unnamedLivesContested` |
|---|---|---|---|---|---|
| `d9781168` | 2 863 → **3 337** | 513 → **64** | 25 → **0** | 1 → **0** | 2 → **0** |
| `bf15f7ab` | 1 522 → 1 522 | 12 → 12 | 0 | 0 | 0 |
| `64e8adfa` | 2 663 → **2 865** | 213 → **13** | 3 → **1** | 0 | 0 |
| `3372e7eb` | 345 → **624** | 289 → **10** | 0 | 0 | 0 |
| `c75f33b8` | 930 → **1 321** | 450 → **119** | 60 → **0** | 0 | 0 |

Et sur `3372e7eb`, les portages de drapeau : `carries` **9 → 22**, `noTrack` **13 → 0** — les treize
portages qu'aucune trajectoire ne portait ont maintenant leur porteur.

**Balayage exhaustif des compteurs d'échec** (tous les nombres de `coverage`, les cinq films) :
**zéro compteur d'échec ne MONTE**. Les deux hausses que P2-bis avait dû faire accepter
(`slotCollisions` +1, `unnamedLivesContested` +2 sur `d9781168`) sont **effacées** : le film ne
porte qu'un corps par slot, la collision était une erreur du pont.

### I5.4 Les compteurs de la feuille sont intacts

`scoreTimeline` et `identity.statborgSlots` sont **identiques octet pour octet** sur les cinq films
(SHA-256 des objets triés). L'écart cumulé K/D/A contre la feuille (somme des manches) ne bouge
donc pas :

| Film | écart K/D/A avant | après |
|---|---|---|
| `d9781168` | 0 | **0** |
| `bf15f7ab` | 0 | **0** |
| `3372e7eb` | 0 | **0** |
| `64e8adfa` | 16 | **16** — non traité par ce lot (pont STATBORG, 5 couples restants) |
| `c75f33b8` | 21 | **21** — idem, sur un film à 14 participants pour 10 humains publiés |

### I5.5 Le portage du crâne — la SEULE grandeur qui baisse, et elle est instruite

```
d9781168  temps de portage par equipe   avant 172,5 / 158,8 s   apres 172,4 / 158,8 s
          portages publies              avant 36               apres 36
```

**Une seule frame, sur un seul portage.** Le portage `…6950 [5115..5165]` finit désormais à
**5164**, la dernière frame de la vie de son porteur (slot 618, `[4891..5164]`).

Le mécanisme est exactement celui que P2-bis avait écrit : `carrierPresenceOf` fait entrer une vie
DÉDUITE dans `unnamed` — « une identité déduite ajoute une présence, elle n'en retire jamais une ».
Avant, la vie 618 était nommée par `exclusion_temporelle`, donc déduite, donc le cas d'abstention
n° 2 s'appliquait et le portage n'était pas rogné. Elle est maintenant une **LECTURE** : le cas 3
s'applique et le portage est rogné à la vie qui le recouvre.

**C'est un gain d'exactitude, pas une perte** : un portage ne survit plus d'une frame à son porteur.
Le seuil du brief (≥ 172,5 s) est manqué de 0,1 s pour cette raison, et pour elle seule.

### I5.6 Ce que le gate corpus lira « perte », et l'instruction de chaque ligne

Seules les mesures de `coverage` entrent au gate (`identity` n'est pas aplati par
`replaydiff/empreinte_axes.go`). Après les deux amendements de polarité ci-dessous, il reste **une
seule ligne** :

| Ligne | Films | Instruction |
|---|---|---|
| `coverage.bridge.closedByRespawn` 2/5/5/3/2 → 0 et `closedByShot` 1 → 0 | les cinq | **AMENDÉ** : les fermetures sont une VOIE, pas une richesse — elles ne s'appliquent qu'aux slots que la lecture n'a pas nommés, et il n'y en a plus. `coverage.bridge.closedBy*` rejoint `namedBy*` dans `prefixesMethode` (`replaydiff/polarite.go`, daté, test + mutation) |
| `coverage.bombCarries.noBridge` 2 → 0 | `c75f33b8` | **AMENDÉ** : compteur d'échec dont le nom le dit — plus aucun portage de bombe n'est orphelin. `noBridge` entre à `compteursDEchec`, même défaut que `noTrack` la veille (daté, test + mutation) |
| `coverage.flagCarries.homeByObject` 2 → 0 | `3372e7eb` | **À ACCEPTER, non amendé** : c'est une VOIE de retour au camp, pas une richesse. Les deux retours qu'elle portait sont maintenant confirmés par le marqueur (`markerConfirmed` 2 → 3) grâce aux 13 portages qui ont retrouvé leur porteur. Généraliser la règle aux voies de `flagCarries` demande leur propre inventaire — hors périmètre E2 (règle 7), consigné au registre |

Les **quatre pertes acceptées du 2026-09-08** (P2-bis §B.8) n'ont plus lieu d'être : `slotCollisions`
et `unnamedLivesContested` REDESCENDENT à zéro sur `d9781168`, et le `slotCollisions` de `fb1a1a72`
relève de la même figure (film hors matériau E2, à confirmer par le superviseur au gate complet).

### I5.7 Goldens

`go test ./internal/analysis/replay/ -run Golden -update` puis `git status --short -- testdata/` :
**vide**. Le golden `assembly_000d5950` ne bouge **pas d'un octet** — et c'est le témoin de
neutralité du lot : ce fixture ne porte pas de créations, la branche de dégradation s'y applique, et
l'assemblage y est bit à bit celui d'avant (483/519 tirs, 90/105 vies nommées, 26 lectures d'index).
