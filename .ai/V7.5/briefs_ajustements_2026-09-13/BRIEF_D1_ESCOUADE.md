# LOT D1 — Escouade : retraits, assistances, échange (artefact 4c520da6), heatmap, blocs

Worktree : `C:\Users\Guillaume\Downloads\Scripts\LevelUp-wt-ajust-escouade` · branche `feat/ajust-escouade` · port Vite **5184**.
Lis d'abord `BRIEF_COMMUN.md`. Artefact de référence pour l'échange : `artefacts/4c520da6_echange_escouade.html` (lis le fichier HTML EN ENTIER : CSS + script ; la version texte `artefacts/art_4c520da6.txt` aide à lire le script). Playwright : `E2E_VISUAL_SQUAD_TEAMMATES=Madina97294,Chocoboflor`.
Un lot voisin (D2) ajoute une NOUVELLE section « Les formes retenues » sur la même page ; pour limiter les conflits, tu modifies `SquadSynergiesPage.tsx` avec parcimonie et tu ne réordonnes que ce que le brief demande. Le lot B modifie `features/_shared/usage/EquipmentUsageSection.tsx` (découpage en blocs, « Équipe adverse ») : TU NE TOUCHES PAS ce fichier ni `usageI18n.ts` — tes items D1.5 se limitent à ce qui est propre à la page Escouade (voir D1.5).

## Repérage déjà fait (vérifie sur pièces)

- Shell : `features/squad/SquadLayout.tsx:819-893` — `SessionBriefing :825-848`, `SquadObjectiveStatsPanel :851-855`, **`SquadEchangeKpi :859`**, `SquadFocusStrip :863`, onglets `:866-890`, Outlet `:892`.
- Onglet Synergies `features/squad/SquadSynergiesPage.tsx:116-268` : … `SquadAssistPairsTable :179-190` (« Assistances dans l'escouade », TanStack Table, données `pageData.assist_pairs` → `SquadAssistPairs` `lib/api/types.ts:1346`, Go `domain/assist_pairs.go:167-180`) ; `SquadEchangeMatrixCard :191` (« Qui échange pour qui », `Heatmap2DChart`, `pageData.echange.cellules`) ; `SquadIsolementNuageCard :192-194` (« Isolement et couverture », builder custom `:177-355`, points = (joueur, session) `domain/squad_isolement.go:28-48` + gros point médian par joueur `:244-271`) ; `SquadMapHeatmapChart :195-208` ; … `MedalDigest :253-260` ; `EquipmentUsageSection :267` (mode squad).
- Onglet Dynamique `SquadDynamiquePage.tsx:65-133` : `SquadEchangeDelaiCard :96` (« Délai d'échange », `HistogramChart`, `pageData.echange.delais[]`, 7 intervalles binnés côté Go `service/teammates/teammates_squad_echange.go:39-54`).
- Données : un seul hook `useTeammates` (`features/squad/queries.ts:10-42`, `POST /pages/teammates`), Go `handlers/teammates.go:43`, DTO `domain/teammates.go`, échange `domain/squad_echange.go` (+ `analysis/coordination/trade.go`, `riposte.go`, `isolation.go`, `measure.go`).
- Perf joueur × carte : builder `features/squad/charts/squadMapHeatmapChart.ts:37-126` — AUCUN `xAxis.name`/`yAxis.name` (`:90-102`), pas de `legend` (la légende est un `visualMap` piecewise `:103-116` à `bottom:4`), `grid.bottom:110`, hauteur `SquadMapHeatmapChart.tsx:28` = `max(220, min(640, joueurs*60+160))` → 280 px pour 2 joueurs : le visualMap sort du canvas. Commits utiles : `e1faf4262`, `784580b8f`.
- Match view, forme voulue pour les assistances : `features/match-view/MatchAssistChart.tsx:60-109` (`BarStackedChart`, `componentHexColors`, `tooltipHideZero`, `tooltipComponentNote`) — l'utilisateur veut des barres VERTICALES empilées (`orientation` par défaut).

## Items

### D1.1 — Retirer la KPI « Taux d'échange »
Supprimer `SquadEchangeKpi` du shell (`SquadLayout.tsx:859`) ET le composant, ses tests, ses i18n non réutilisés. Il est remplacé par « Le compte » (D1.3, dans l'onglet Synergies, pas dans le shell).

### D1.2 — « Assistances dans l'escouade » en barres verticales empilées
Remplacer `SquadAssistPairsTable` par un graphe `BarStackedChart` vertical : une barre par ASSISTANT (catégorie = gamertag), segments empilés = bénéficiaires, couleurs = jetons joueurs (`features/squad/colors.ts`, `SQUAD_MAIN_PLAYER_TOKEN` / `SQUAD_TEAMMATE_COLOR_TOKENS`, même joueur = même couleur partout), tooltip « N assistances (part P %) · dont N volées » comme la match view (`tooltipComponentNote`). Bandeau de couverture « N matchs mesurés sur M » AU-DESSUS, comme aujourd'hui. Légende = joueurs. Le tableau et ses tests sont supprimés. Titre/description existants conservés.

### D1.3 — L'échange, selon l'artefact 4c520da6, FOND ET FORME
Six cartes, dans l'ordre de l'artefact ; chaque carte porte son titre, son paragraphe « ce qu'il a trouvé » (le « say », alimenté par les VRAIES données via des gabarits FR/EN — jamais les chiffres de la maquette), sa figure, et sa note de pied (les « foot »). Tags « wrapper / onglet / V1 » de la maquette : NON (c'était de la doc de proposition).
1. **« Le compte »** — `KPIStrip` 4 tuiles : Taux d'échange (« des morts d'équipe, sous 5 s », écart vs habituel en pts, masqué si plein historique — reprends la logique de l'ex-`SquadEchangeKpi`), Délai médian (« quand l'échange a lieu »), Morts sans réponse (« sur N morts d'équipe »), Sans réponse par match (« indépendant du nombre de morts »). Placement : en tête de l'onglet Synergies, à la place de `SquadEchangeConstatCard` si celle-ci fait doublon avec le « Cap du moment » (lis-la ; si elle EST le cap du moment, garde-la juste au-dessus de « Le compte », sinon supprime-la et dis-le).
2. **« Combien, et à quelle vitesse »** — histogramme du délai (7 intervalles servis), barres HORS FENÊTRE HACHURÉES (pattern SVG/`decal` ECharts), ligne verticale pointillée « fenêtre 5 s » (jeton `warning`), valeurs au-dessus des barres, étiquette de figure « Délai des N vengeances survenues — sur M morts d'équipe ». C'est l'évolution de `SquadEchangeDelaiCard` : DÉPLACE-la de Dynamique vers Synergies (à la suite de « Le compte »), transforme-la (hachures, ligne, texte « say ») ; si `HistogramChart` n'expose pas hachure/markLine, étends le wrapper (props optionnelles, tests) plutôt que de bifurquer vers un builder local.
3. **« Qui couvre qui »** — matrice `Heatmap2DChart` ligne = vengeur, colonne = vengé, diagonale « — », cellules `nombre` avec texte (blanc si sombre), sous chaque colonne « reçu N » et une puce « le plus couvert » (jeton `success`) / « le moins couvert » (jeton `warning`), rampe séquentielle 0→max avec légende « 0 … N échanges », figure « Échanges réalisés sur N matchs d'escouade ». Évolution de `SquadEchangeMatrixCard` ; si le wrapper ne sait pas rendre « reçu N » + puces, ajoute-les en DOM sous le chart (rangée alignée sur les colonnes) ou étends le wrapper — dis ce que tu fais.
4. **« Pourquoi la vengeance ne vient pas »** — nuage : UN PETIT POINT PAR SESSION ET PAR JOUEUR (toutes les sessions, toutes visibles, opacité ~0,45, couleur du joueur), UN GROS POINT PAR JOUEUR (taille = nombre de morts, cerclé pointillé sous 30 morts, étiquette gamertag + « · n=N » si faible), X = « morts hors portée de radar → », Y = « morts vengées → », quadrants nommés par les médianes (« proche et couvert », « loin, mais on vient », « proche, et pourtant seul », « loin et sans secours » en `warning`), quadrant à corriger teinté `warning` 0,07, légende de taille (24 morts / 82 morts / une session / moins de 30 morts — valeurs prises des données réelles : min et max des morts par joueur). Tooltip par point comme la maquette. Le composant actuel `SquadIsolementNuageCard` est le point de départ : l'utilisateur ne voit qu'UN gros point — trouve pourquoi (données `nuage_isolement` vides de sessions ? points confondus ? série masquée ?) et prouve-le par une capture AVANT + lecture du JSON de la réponse `/pages/teammates` (Playwright peut intercepter). Toutes les morts doivent être représentées : chaque session est un point.
5. **« Donné et reçu, par coéquipier »** — `BarGroupedChart`, deux séries « Donné » / « Reçu » par joueur, valeurs au-dessus des barres, légende. Dérivable des `cellules` (sommes ligne/colonne) — pas de Go.
6. **« Taux d'échange par session »** — `TimeseriesLineChart` une série, X = sessions (date), Y = %, dernier point appuyé et étiqueté « N % », pas de légende (le titre nomme la série), figure « Part des morts d'équipe vengées sous 5 s, par soirée ». Données : si `pageData.echange` ne porte pas le taux par session, ajoute-le côté Go (`teammates_squad_echange.go`, depuis les mêmes événements, groupés par `session_label`), DTO + openapi + `make generate-types` + tests. Sous le seuil de significativité (règle du « Cap du moment » : moins de N sessions — reprends le seuil déjà défini dans `squadEchange.logic.ts`/`SquadFocusStrip`, sinon 3 sessions), afficher l'état vide propre (« pas encore assez de soirées »).
Ordre final de l'onglet Synergies : Le compte → Combien et à quelle vitesse → Qui couvre qui → Pourquoi la vengeance ne vient pas → Donné et reçu → Taux par session → (le reste dans l'ordre actuel) → blocs d'équipement → Médailles EN DERNIER (D1.6). L'ancien « Isolement et couverture » et l'ancien « Qui échange pour qui » n'existent plus sous leurs anciens titres (remplacés).

### D1.4 — « Performance par joueur × carte » : légende et axes
Ajoute `xAxis.name` (« Carte ») / `yAxis.name` (« Joueur ») FR/EN avec `nameLocation/nameGap/nameTextStyle` (modèle : `SquadIsolementNuageCard.tsx:336-351`), fais rentrer la légende `visualMap` dans le canvas (hauteur suffisante : recalcule `SquadMapHeatmapChart.tsx:28` pour inclure `grid.bottom` + hauteur du visualMap + étiquettes rotées), ET passe une légende DOM `legend` à `ChartCard` (5 paliers `perf-tier-1..5` avec leurs libellés) pour qu'elle existe même si le visualMap est masqué. Vérifie sur capture avec 2 coéquipiers.

### D1.5 — Blocs d'équipement sur Escouade
Le découpage en deux blocs par rangée et « Équipe adverse » sont faits par le lot B dans le composant partagé (tu ne touches pas `EquipmentUsageSection.tsx`/`usageI18n.ts`). Ce qui t'appartient : (a) vérifier que la page Escouade rend bien `mode="squad"` (les titres doivent dire « Notre part … » ; s'il affiche « Ma part … », c'est un bug de la page — corrige côté page) ; (b) retirer la mention « Matchs mesurés 8/8 » — elle vient de `cardTitleAdornment(t.measuredFmt(...))` dans le composant partagé : demande-le au superviseur dans ton rapport SI tu ne peux pas le faire sans toucher au fichier partagé ; sinon, si le composant expose déjà une prop pour la masquer, utilise-la. (Le superviseur arbitrera à la fusion ; note-le explicitement dans ton rapport, pas de contournement.)

### D1.6 — « Médailles — Résumé de l'escouade » toujours en dernier
Déplace la section Médailles (`:253-260`) à la toute fin de l'onglet Synergies, après `EquipmentUsageSection`. Le lot D2 ajoutera sa section AVANT Médailles ; laisse un commentaire d'ancrage `{/* Ancrage lot D2 : section « Les formes retenues » vient ici, avant Médailles */}` juste avant la section Médailles.

Captures APRÈS : Escouade / Synergies (page entière, 1440), Escouade / Dynamique, et un zoom 1024 sur le nuage et la matrice.
