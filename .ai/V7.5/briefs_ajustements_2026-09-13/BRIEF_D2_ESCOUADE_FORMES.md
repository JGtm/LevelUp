# LOT D2 — Escouade : portage intégral de l'artefact « Les formes retenues » (2ec1b8eb)

Worktree : `C:\Users\Guillaume\Downloads\Scripts\LevelUp-wt-ajust-escouade-formes` · branche `feat/ajust-escouade-formes` · port Vite **5185**.
Lis d'abord `BRIEF_COMMUN.md`. Artefact : `artefacts/2ec1b8eb_les_formes_retenues.html` — LIS-LE EN ENTIER (CSS des formes + script : données, six fonctions de forme, 19 cartes avec titres, sous-titres, légendes, notes). La version texte `artefacts/art_2ec1b8eb.txt` aide à lire le script. Playwright : `E2E_VISUAL_SQUAD_TEAMMATES=Madina97294,Chocoboflor`.
Ce lot touche le Go (skills `arch-rules`, `go-features`, `db-schema`, règles ART de CLAUDE.md : lectures via vues `_latest`, aucune écriture nouvelle nécessaire a priori). GOCACHE privé.

## Cadrage utilisateur (verbatim)
« Ni “Usages d'équipement”, ni “Contrôle des armes spéciales” ne décrit les équipements pris ou non. Il faut suivre l'artefact 2ec1b8eb. Ce que je voulais était explicitement listé et n'a pas du tout été implémenté. Tu implémentes tout comme le document le décrit, dans le fond et la forme. Je n'ai pas travaillé des heures pour que tu improvises encore. S'il y a des choses redondantes avec l'existant JE ferais le tri ensuite. »
Conséquences : tu portes LES 19 CARTES des 3 blocs (Usages d'équipements 7, Contrôle des armes spéciales 7, Objectifs 5), les DEUX contextes (« Contexte Solo — moi dans mon équipe, et dans le lobby » = le joueur de la page ; « Contexte Escouade — mon camp contre le leur »), les paragraphes « constat » et « lexique » de chaque bloc (avec gabarits FR/EN alimentés par les données réelles, jamais les chiffres de la maquette), les légendes et notes de chaque carte. Tu ne retires RIEN de l'existant (l'utilisateur triera). Tu n'ajoutes pas de carte qui n'est pas dans l'artefact.

## Où ça se monte
Onglet Synergies de la page Escouade (`features/squad/SquadSynergiesPage.tsx`), dans une NOUVELLE section montée sur UNE ligne, après `EquipmentUsageSection` et avant la section « Médailles — Résumé de l'escouade » (le lot voisin D1 déplace Médailles en dernier et pose un commentaire d'ancrage ; si tu ne le vois pas, monte ta section juste après `EquipmentUsageSection` — le superviseur réconciliera). Tout ton code vit dans `features/squad/formes/` (composants, modèles purs, i18n `Record<Locale, …>`, tests) + `components/charts/` pour les formes réutilisables si elles sont génériques. Tu n'édites dans `SquadSynergiesPage.tsx` que la ligne de montage + l'import.

## Les six formes (CSS de l'artefact = la spec de forme)
`ecart` (Écart à la parité : trait ambre = parité, barre part du trait, droite = plus, gauche = moins, longueur = écart en points, chiffre + « ±N pts » à droite, axe de valeurs sous le bloc) ; `jaugeDouble` (deux pistes sur un axe commun : « dans mon équipe » puis « dans le lobby », chacune avec étendue min-max par match, barre de ma part, trait de parité, texte « P % · k/n au-dessus ») ; `piste100` (barre = 100 % du lobby : segments colorés par joueur allié, « Coéquipier hors escouade », hachuré « équipe adverse », trait de parité à 50 % sauf `sansParite`) ; `bande` (une case par match, teintée par l'écart à la parité avec intensité saturant à 30 pts, « — » si non mesuré, axe = heure + carte des matchs) ; `grille` (lignes alignées, une échelle et un axe PAR colonne, barre + valeur par cellule, séparateur, en-tête avec total) ; `batonMinMax` (étendue min→max par geste + trait de moyenne, chiffres « min … max · moy »). Chaque forme = composant React pur (props typées, jetons sémantiques, infobulle au survol ET au focus clavier comme l'artefact `data-bulle`), testé (vitest, rendu + modèle). Couleurs : `--moi` → `squad-player-1` ; coéquipiers → `SQUAD_TEAMMATE_COLOR_TOKENS` (`features/squad/colors.ts`) ; `--j4` coéquipier hors escouade → `team-ally` ; `--plus`/`--moins` → `divergent-pos`/`divergent-neg` ; parité → `warning` ; familles d'équipement `--fam-*` → jetons `USAGE_METRIC_TOKENS` de `features/_shared/usage/usageMetricKinds.ts` (réutilise, n'invente pas) ; hachure adverse → motif comme `ENEMY_HATCH` dans `features/_shared/usage/UsageForms.tsx:69-73`. Aucun hex.

## Les données (le fond)
L'artefact travaille sur : MATCHS de la portée (id, heure, mode, carte, durée, film décodé oui/non, camp du joueur) ; LOBBY[match].j[gamertag] = { t: camp, camo, over, mur, gra (tractions de grappin), lache (objets lâchés au sol), pad (prises de socle) } pour TOUS les joueurs des deux camps + `anon` (occupations sans ramasseur nommé) ; PADS[match] = armes des socles, occupations par arme, prises [nommées, dont ?] par arme, prises par joueur d'escouade par arme ; PADFAM[match] = par famille (lourde / précision / autre) { moi, mien (mon camp), autre (adverse) } ; OBJ[match] = famille de mode (Bastion, Drapeau…), colonnes réelles du mode avec rôle (prendre / défendre / tenir, `duree` pour les temps), valeurs par joueur des deux camps.
Grenades EXCLUES des usages (« ce ne sont pas des équipements »). Répulseur : pas de ligne. Colonnes pilotées par la donnée (famille absente = pas de colonne).
Avant d'écrire du Go : inventorie ce qui existe (skill `go-features`) — `internal/analysis/sessionusage/` (usage.go, squad.go, usage_families.go : per-match, per-player, lobby, per10min = 600/durée), `internal/domain/equipment_usage.go`, `domain/session_usage.go`, le bloc `equipment_usage` de `TeammatesPageResponse` (`domain/teammates.go:605`), les tables/vues du film (`db-schema` : usages d'équipement, prises de socle `weapon_pad_pickups`/occupations, objectifs par joueur — cherche les vues `_latest`), `objectiveColsFor` côté web (`features/match-view`) et sa table rôle→grandeurs si elle existe (l'artefact dit : « la table rôle → grandeurs est un savoir du titre, à déclarer en donnée » → TOML `config/titles/{slug}/mappings/`, jamais en dur dans un composant ; idem famille d'arme → lourde/précision/autre). Ce qui manque au DTO de la page Escouade, tu l'ajoutes dans un NOUVEAU bloc de réponse (ex. `formes_retenues`) calculé par un service pur dans `internal/analysis/` (lecture seule, `_latest`, portée = les mêmes matchs que le reste de la page Escouade), exposé via `handlers/teammates.go`, openapi régénéré, `make generate-types`, tests Go (unitaires sur fixtures + contrat). Un axe non mesurable pour un match = « non mesuré » (jamais 0). Normalisation « pour dix minutes de jeu » = même règle que `per10Min` (600/durée mesurée).

## Les 19 cartes (titres exacts, forme, contexte) — reprends aussi légendes et notes de l'artefact
Bloc 1 « Usages d'équipements » (constat + lexique) — Solo : « Ma part, dans mon équipe et dans le lobby » (jaugeDouble, 5 axes) ; « Cadence de gestes, match par match » (grille, une ligne par match, colonnes = familles, total en en-tête, hachure match sans film) ; « Étendue et moyenne de la session » (batonMinMax). Escouade : « Régularité match par match » (bande, part de mon camp vs 50 %) ; « Ce que mon camp prend du lobby » (piste100 avec parité) ; « Cadence de chacun sur la session » (grille, une ligne par coéquipier) ; « Qui porte quel geste dans l'escouade » (piste100 sans parité, dénominateur escouade seule).
Bloc 2 « Contrôle des armes spéciales » (constat + lexique : socle, prise de socle, dénominateur = prises NOMMÉES, réserve des occupations sans ramasseur) — Solo : « Écart à la parité, par famille d'arme » (ecart : Toutes armes + 3 familles, parité = 1/taille moyenne du lobby) ; « Ma part des prises de socle, et sa dispersion » (jaugeDouble + sous-titre « Match par match — ma part du lobby » + bande) ; « Taux de rafle par arme » (grille : Taux de rafle %, Mes prises, Occupations du socle ; 12 armes max, les autres repliées). Escouade : « Écart à la parité, par famille d'arme » (ecart, parité 50 %) ; « Les deux frises : quand, et qui » (sous-titres, bande + piste100 + phrase de réserve « Hors de cette barre : N occupations… ») ; « Emprise de l'escouade, match par match » (piste100, une ligne par match, hachure non mesuré) ; « Taux de rafle de chaque coéquipier » (grille, 8 armes, une colonne par coéquipier, échelle par colonne).
Bloc 3 « Objectifs » (constat + lexique) — Solo : « Écart à la parité, par rôle » (ecart, deux lignes par rôle : équipe puis lobby) ; « Ma part par famille de mode » (jaugeDouble par famille, colonnes réelles du mode) ; « Objectif par mode, en valeurs brutes » (grille par famille, une ligne par match, durées en mm:ss). Escouade : « Rapport de force par famille de mode » (ecart, parité 50 %) ; « Ce que mon camp prend de l'objectif » (piste100 par rôle).
Vocabulaire : « Contexte Solo — moi dans mon équipe, et dans le lobby », « Contexte Escouade — mon camp contre le leur » comme intertitres. Les tags de la maquette (« La part du lobby · Solo B », « Une soirée · Escouade A ») étaient de la doc de proposition : NON.

## Démarche imposée
1. Inventaire existant (Go + web) écrit dans ton rapport AVANT de coder : pour chaque donnée de l'artefact, « existe (où) / à calculer (depuis quoi) / non mesurable (preuve) ».
2. Go d'abord (bloc de réponse + tests), puis formes (composants + tests), puis cartes, puis montage, puis captures.
3. Si une donnée est réellement absente du film ou de la base (preuve : table/vue et requête), la carte reste montée avec l'état « non mesuré » explicite (jamais omise en silence) et tu le dis dans le rapport.
4. Vérification visuelle : sans serveur Go de ta branche (mono-process), tu vérifies les formes et les cartes par (a) tests de rendu vitest, (b) une PAGE DE VITRINE temporaire ou le stub Playwright de `/pages/teammates` (route interceptée, réponse = fixture JSON que tu construis à partir des données de l'artefact) sur ton Vite :5185 — captures lues par toi. Le superviseur fera la passe sur données réelles.

Captures APRÈS : la section entière (page Synergies, 1440) sur fixture, et un zoom par forme.
