# LOT G — Match view

Worktree : `C:\Users\Guillaume\Downloads\Scripts\LevelUp-wt-ajust-matchview` · branche `feat/ajust-matchview` · port Vite **5188**.
Lis d'abord `BRIEF_COMMUN.md`. Lot web (les données existent). Match témoin : choisis un match récent de JGtm avec film décodé (rejeu disponible, prises de socle, équipements) — trouve-le en interrogeant l'API :8000 (`/players/JGtm/matches?…`) ou via la page Historique ; note son id dans le rapport. L'onglet Chronologie de la match view porte les blocs équipement/armes spéciales/positions.

## Cadrage utilisateur (verbatim, à respecter à la lettre)
- « Le graphe “Distance par arme” doit être modifié, il vaut mieux afficher des sections par armes, on met les joueurs sous chaque arme mais toujours au sein du même graphe, pas comme c'est aujourd'hui. Et le joueur actif et ses amis doivent avoir une couleur différente. »
- « “Carte de chaleur des positions” est hideux comme graphe et je ne sais pas ce que ça rend, à quoi ça sert ou quel est le narratif. »
- « Dans “Usages d'équipement” j'ai dit que je voulais pas des grenades. Et c'est quoi cette distinction “épisode” et “durée” ? j'ai jamais demandé ça. Je veux pas du texte “Socles de bonus de puissance vidés : … Le propulseur … le geste dure une demi-seconde.” »
- « Pourquoi “Contrôle des armes spéciales” a plusieurs épaisseurs de barres ? et pourquoi il est constamment replié et n'affiche jamais rien par défaut ? Les étiquettes dans les barres sont illisibles, masquées par la hauteur de la barre qui est insuffisante. Virer le texte “34 prises attribuées sur 65 occupations…”. »
- « Le haut des tableaux dans “Tableau des scores” n'utilise pas les couleurs d'équipes choisies par l'utilisateur. »
- « À l'affichage des outils de destruction il faut pouvoir les tronquer, “Marteau antigravité” est trop long et vient tout décaler. Pour les en-têtes aussi : “Rend.” et “Résist.” pour “Rendement” et “Résistance”. »

## Repérage déjà fait (vérifie sur pièces)
- Distance par arme : `features/match-view/MatchKillDistanceSection.tsx` (`:89-131`, un `ChartCard` PAR JOUEUR `PlayerDistanceChart :62-87`), builder `_killDistanceChart.ts:73-138` (bâton flottant min→max + losange moyenne, catégories = armes), couleurs FIXES `chart-series-1`/`perf-tier-2` (`:66-69`), `isMe` seulement en fond de wrapper (`:117-120`). Données `combat_tab.kill_distance_by_weapon: MatchKillDistancePlayer[]` (par joueur → par arme). Amis : `useSettings().friend_gamertags` (`MatchViewPage.tsx:104-105`, prop `friendGamertags`) ; chokepoint couleur joueur `features/match-view/colors.ts` `buildMatchPlayerColors(scoreboard, meXUID, friendGamertags, roster)` `:91-180` (moi → `SQUAD_MAIN_PLAYER_TOKEN`, ami allié → `SQUAD_TEAMMATE_COLOR_TOKENS`, coéquipier → `ALLY_OTHER_TOKENS`, adversaire → `ENEMY_TOKENS`) ; exemple `MatchFragDiffChart.tsx:53-54`.
- Carte de chaleur : `features/match-view/MatchPositionsHeatmap.tsx` (`binPositionsToHeatmap :68-116`, grille 20×20 sur les bornes du match, `Heatmap2DChart` 360 px, filtre équipe), données `useMatchPositions` (`queries.ts:75-88`, `GET …/positions`, `{timeMs,x,y,z,team}` sans joueur), i18n en dur dans le fichier `:40-57`, monté `MatchViewTabChronology.tsx:183-186`. Le REJEU 2D (match-replay) dispose déjà du fond de carte et d'un calque chaleur (`lib/replay/heatPaint.ts`, `drawTacticalHeatmap`) — l'onglet Tactique l'utilise sur fond de plan.
- Usages d'équipement : `features/match-replay/MatchEquipmentUsageSection.tsx` (`:94-173`, `UsageFootnotes :357-389`), colonnes `features/match-replay/model/equipmentUsageColumns.ts` (`usageColumnGroups :162-205` ; grenades `:189-203` clé `grenade.${rank}` ; états actifs `activeEpisodesGroup :212-248` avec `${fam}.count` « (épisodes) », `${fam}.ms` « (durée) », `${fam}.kills`), i18n `features/match-replay/i18n/i18n.ts` (`groupGrenades :379`, `activeCount :368`, `activeDuration :369`, EN `:807-823`, **`notMeasured :393-394` / `:833-834` = le long texte, constante inconditionnelle rendue `:386`** ; lignes data-driven qui le précèdent `:367-385` : « Socles de bonus de puissance vidés », `coverageActiveFmt`, `coverageGrappleFmt`, `unattributedFmt`, `coverageUnknownOriginFmt`). Replié par défaut `:106`.
- Contrôle des armes spéciales : `features/match-replay/MatchPadControlSection.tsx` (DOM/CSS pur, PAS ECharts) — `useState(false)` replié `:95` (décision D3 du 05/09 : l'utilisateur la RÉVOQUE aujourd'hui), `visibleControl` ne garde que `forwardWeapons` ; `PadWeaponRow :239-286` : un bâton de 13 px PAR CAMP empilés verticalement (`flex-col gap-[3px]`, 1 à 3 bâtons par arme = « épaisseurs différentes »), segments par joueur `width: calc(fraction% - 2px)`, étiquette `text-3xs` masquée sous 11 % ; pied `PadControlFootnotes :297-313` (`attributedFmt`, `missingFmt`, `gapFmt.*`, i18n `:410-422` / `:850-862`).
- Tableau des scores : `features/match-view/MatchScoreboard.tsx` (673 L, `eslint-disable max-lines`), en-tête d'équipe `:516-534` + `:542-565` via `teamColorResolver(rows)(teamID, isMyTeam)` (`features/match-view/teamColor.ts:30-52` : couleur OFFICIELLE du jeu d'abord, le jeton utilisateur seulement en dernier recours → sur Infinite le réglage utilisateur n'a JAMAIS d'effet). Réglage utilisateur : `features/settings/AccessibilityTab.tsx:267-288`, appliqué en CSS `--ac-team-ally`/`--ac-team-enemy` par `app/providers/theme-provider.tsx:22-31` ; helper DOM `teamTokenCssVar(ally)` (`features/match-view/teamSeriesColor.ts:63-66`), helper ECharts `teamSeriesColor`. Pattern correct déjà appliqué : `MatchObjectivesSection.tsx:103+`, `MatchPadControlSection.tsx:111-114`, `MatchEquipmentUsageSection.tsx:123-127`.
- Outils de destruction dans le scoreboard : colonne `top_weapon` `MatchScoreboard.tsx:462-474` (`<span className="text-muted-foreground">`, pas de `truncate`, `<td whitespace-nowrap>` `:642`), libellé `t.sbColTopWeapon` (`i18n.ts:523` FR « Outil de destr. » / `:838` EN). « Rendement » / « Résistance » = `useFieldLabel('offensive_conversion')` / `('defensive_resistance')` → `lib/i18n/fieldMappings.ts:155` (dictionnaire canonique servi par le backend ; garde-rail `no-field-label-dictionary.test.ts`) ; colonnes `buildHighlightCols :113-114`. Le graphe « Outils de destruction » de l'onglet Général (`MatchFragCard.tsx`, `FragWeaponBreakdown.tsx`, noms en `yAxis` sans troncature) est l'AUTRE surface du même nom : vérifie sur capture si « Marteau antigravité » y déborde aussi ; si oui, tronque avec ellipse (`axisLabel.width` + `overflow:'truncate'`, nom complet dans le tooltip).

## Items
### G.1 — Distance par arme : un seul graphe, sections par arme, joueurs sous chaque arme
Un seul `ChartCard`. Axe des catégories (vertical, une ligne par (arme, joueur)) groupé PAR ARME : pour chaque arme (ordre = frags mesurés décroissants, toutes équipes), une ligne d'en-tête « Arme ×N » (séparateur visuel : bande `muted`/`markArea` ou étiquette en gras), puis sous elle une ligne par joueur ayant des frags mesurés avec cette arme (bâton min→max + losange moyenne, mêmes primitives que le builder actuel). Couleur du bâton = `buildMatchPlayerColors` (moi = jeton joueur principal, amis = jetons coéquipiers, autres alliés/adversaires = leurs jetons) ; légende = rôles (Moi, Amis, Mon équipe, Adversaires) ou joueurs selon ce qui reste lisible — dis ce que tu choisis. Étiquette de ligne = gamertag (tronqué à ~14 caractères, complet dans le tooltip). Hauteur = fonction du nombre de lignes. Le tooltip dit arme, joueur, N frags, min/moy/max en mètres. Supprime `PlayerDistanceChart` et adapte les tests (`_killDistanceChart.test.ts`, `MatchKillDistanceSection.test.tsx`).
### G.2 — Carte de chaleur des positions
La donnée est une liste de positions match-niveau (sans joueur) sur une grille 20×20 sans fond de carte : l'utilisateur ne sait pas ce que ça rend ni à quoi ça sert. Action : rends-la LISIBLE et UTILE ou supprime-la. Critère : si l'artefact de rejeu du match donne un fond de carte (comme le rejeu 2D / l'onglet Tactique — `drawTacticalHeatmap` + image de fond via la même route que Tactique `/tactical/{map_id}/background.png` ou le fond du rejeu `lib/replay`), alors refais le bloc comme « Où ça se joue » : fond de plan + calque chaleur des positions (grille 0,5 m, échelle quantile p50→p95, cellule non atteinte vide), filtre Tous / mon équipe / adversaires, narratif FR/EN d'une phrase (« Les endroits de la carte les plus occupés pendant ce match, toutes équipes ou par camp. Lecture : plus c'est chaud, plus on y a passé de temps. ») + note de couverture. Si le fond n'est pas disponible pour ce match (preuve), SUPPRIME le bloc (composant, hook, query key, route Go si plus lue par personne, tests, i18n) — pas de « au cas où » — et dis-le. Dans les deux cas, justifie dans le rapport avec la capture.
### G.3 — Usages d'équipement
(a) Retirer le groupe grenades (`equipmentUsageColumns.ts:189-203`, clés i18n `groupGrenades`, hint, `grenadeRankFmt`, `grenadeTypeLabel` si plus utilisé, tests). (b) Supprimer la distinction épisode/durée : pour chaque famille d'état actif, UNE colonne = nombre d'utilisations (le compte), la durée passe dans l'infobulle de la cellule (« N utilisations · durée cumulée mm:ss · N frags sous l'effet ») — plus de colonnes `.ms`/`.kills` séparées ; libellés `activeCount`/`activeDuration` supprimés ou déplacés en tooltip ; tests adaptés. (c) Retirer le paragraphe `notMeasured` (rendu `:386`, clés FR/EN). Conserver les lignes de pied data-driven (socles de bonus vidés, couverture) UNIQUEMENT si elles tiennent en une ligne chacune ; sinon les passer en infobulle du titre. (d) Déplié par défaut ? Non demandé ici — ne change pas. Vérifie sur capture : plus de colonne grenade, plus de « (épisodes) / (durée) », plus le long texte.
### G.4 — Contrôle des armes spéciales
(a) Une SEULE épaisseur : une arme = UNE barre (rail 100 % = occupations du socle nommées), segments = joueurs des deux camps (allié puis adverse, séparés par un fin trait), couleur = camp via `teamTokenCssVar` avec teinte par joueur comme aujourd'hui ; hauteur de barre ≥ 22 px pour que l'étiquette (compte) soit lisible (`text-xs`), étiquette masquée seulement sous 6 % (tooltip garde la valeur). (b) DÉPLIÉ par défaut (toutes les armes, plus de `forwardWeapons` seul) ; garde le bouton pour REPLIER si utile, sinon supprime le repli entièrement (0 code mort). Mets à jour le commentaire de décision D3 (l'utilisateur a tranché le 13/09 : déplié). (c) Retirer le pied « N prises attribuées sur N occupations… » (`PadControlFootnotes`, clés `attributedFmt`, `missingFmt`, `gapFmt.*` FR/EN, tests) ; l'annotation « + N sans nom » par ligne peut rester si elle tient dans la marge droite. Vérifie sur capture : barres d'une seule épaisseur, chiffres lisibles, bloc ouvert au chargement.
### G.5 — Tableau des scores : couleurs d'équipe choisies par l'utilisateur
L'en-tête d'équipe (fond, bordures) doit suivre le réglage Accessibilité (allié/adverse) : utilise `teamTokenCssVar(isMyTeam)` (allié = équipe du joueur de la page) au lieu de `teamColorResolver` pour la TEINTE de l'en-tête (le logo et le nom officiel restent). Vérifie que `teamTintStyles` accepte une `var(--…)` (color-mix) ; sinon adapte. Test : `teamTint.guard.test.ts` reste vert ; ajoute un test qui prouve que l'en-tête lit `--ac-team-ally`/`--ac-team-enemy`. Capture APRÈS avec des couleurs d'équipe non par défaut (pose `localUiPrefs.allyTeamColor/enemyTeamColor` dans le localStorage `levelup-ui-prefs` via `addInitScript`, regarde le format dans `stores/settingsDraftStore.ts`) : l'en-tête doit visiblement changer.
### G.6 — Outils de destruction tronqués, en-têtes courts
Scoreboard : colonne `top_weapon` en `truncate` avec largeur max (~14ch) + `title`/tooltip nom complet ; en-têtes « Rend. » et « Résist. » : ces libellés viennent du dictionnaire canonique `fieldMappings.ts` / backend — trouve la mécanique de libellé COURT (existe-t-il un `short_label` ? sinon ajoute une surcharge locale au scoreboard via `columnMeta` avec le libellé long en tooltip d'en-tête `HeaderLabelTooltip`, FR « Rend. »/« Résist. », EN « Eff. »/« Resist. ») sans casser `no-field-label-dictionary.test.ts` (lis le garde-rail et respecte-le). Graphe « Outils de destruction » (onglet Général) : troncature des noms d'arme avec ellipse si débordement constaté (capture).

Captures APRÈS : match témoin — onglet Général (outils de destruction), onglet Joueurs (tableau des scores avec couleurs personnalisées), onglet Chronologie (usages d'équipement, armes spéciales, carte de chaleur ou son absence), et le graphe Distance par arme.
