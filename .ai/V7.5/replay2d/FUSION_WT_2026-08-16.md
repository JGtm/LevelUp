# Fusion des branches wt/* dans feat/v75 — procedure du 2026-08-16

> Deux lots web ont ete executes en parallele sur des worktrees freres, sans acces a
> `.ai/thought_log.md` ni au registre (edites en parallele sur le principal). Ce fichier
> porte : l'ordre de fusion, les recouvrements connus, les entrees de journal et de registre
> que les deux agents ont FOURNIES et qu'il faut consigner a la fusion, et une ratification
> de golden a faire en connaissance de cause. Contrat `plan-execution`.

## Etat de depart (verifie sur pieces)

- `feat/v75` HEAD `317175a4c`. Base commune des deux branches : `020b95eab`.
- `wt/effets-fiches` : `dd946f604` (mesure Go, distribution des durees camo) · `46f99475a` (verre + or, token `legendary`).
- `wt/drawer-replay` : `59cc6eb94` (assouplissement d'un garde-rail frags, seuil 2 -> 3) · `5dd36609a` (tiroir + filtres).
- Recouvrements mesures (`git diff --name-only 020b95eab X`) :
  - effets ∩ principal : `replay/golden_assembly_test.go`, `replay/golden_inputs_test.go` (effets = commentaires « power-up » corriges ; principal = fixture v5 / schema 8 du grappin) ;
  - drawer ∩ principal : `match-replay/ReplayCanvas.tsx` (drawer = rangee flex + ResizeObserver ; principal = calque grappin) ;
  - effets ∩ drawer : `match-replay/i18n.ts`.
- Fichiers de l'arbre A NE PAS TOUCHER NI COMMITTER (travail d'une autre session) : `.ai/V7.5/README.md` (modifie), `.ai/V7.5/replay2d/PLAN_HABILLAGE_REJEU_2D.md`, `.ai/AUDIT_V7.2.0_MAIN_2026-08-06.md`, `apps/go-api/internal/himap/sonde_bouillie_gamefiles_test.go`. **Jamais `git add -A`** : ajouter des chemins explicites.

## Ordre

1. `git merge --no-ff wt/effets-fiches` (le plus petit recouvrement). Resoudre a la main les deux goldens : garder la structure du principal (schema 8, fixture v5) ET les commentaires corriges (« dash du mode Fiesta », pas « power-up »). Regenerer les goldens si le texte fige en depend, et EXPLIQUER l'evolution.
2. `git merge --no-ff wt/drawer-replay`. `ReplayCanvas.tsx` : la rangee flex du tiroir DOIT contenir le calque grappin ; `i18n.ts` : union des cles des deux lots (parite FR/EN par typage — le typecheck le verifie).
3. **Revue de fusion** (skill `adversarial-review`, contexte frais) sur le diff cumule des deux branches. Point precis a rejuger : `59cc6eb94` releve un garde-rail d'une AUTRE feature (`fragClass.colorSource.guard.test.ts`, seuil 2 -> 3). Alternative a evaluer : une entree d'allowlist CIBLEE (le fichier `replaySound.ts` / la cle `SoundCategoryFilter`) plutot qu'un seuil global releve. Trancher, ecrire pourquoi.
4. **Ratifier le golden `minibobine` de killsource** (`internal/games/halo_infinite/film/killsource`, `-run TestGoldenMiniBobine -update`). Il est ROUGE depuis `020b95eab` : les cles i59 ajoutees a `paramByComponent` font passer `recordStateParam` de 0 a 3. Portee mesuree par le lot alertes : 1 ligne sur 77 change, les 10 lignes publiees et les 3 ancres Theater ne bougent pas. Re-congeler = ratifier ce changement — l'ecrire dans le message de commit et au journal.
5. Consigner au journal et au registre les textes ci-dessous (fournis par les agents), puis les gates complets, puis commit(s).

## Gates de cloture

- Go : `go build ./...`, `go vet ./...`, `go test ./internal/analysis/... ./internal/replaybuild/... ./contracttest/... ./internal/games/halo_infinite/film/...` verts ; `golangci-lint run --new-from-merge-base=origin/main` 0 issue.
- Web (`apps/web`) : purge `node_modules\.tmp` PUIS `npm run typecheck`, `npm run lint`, `npm run test` — exit 0 verifie chacun.
- `git status` propre HORS les 4 fichiers de l'autre session.

## Textes a consigner — journal `.ai/thought_log.md` (deux entrees, verbatim des agents)

### Entree 1 (lot effets-fiches)

    ## [2026-08-16] v7.5 rejeu 2D — effets d'etat actif conformes au cahier des charges (verre + encadre dore)
    **Statut** : Complete (camouflage + surbouclier) — translocateur `[!]` non traite, faute de canal
    d'etat mesure pour le rang 11.
    **Decision technique principale** : le lot d257ba02f rendait le camouflage par une opacite reduite
    (0,4, toute la fiche estompee) et le surbouclier par un anneau au token `info` (celui de la jauge
    de bouclier) — non conforme au cahier des charges Notion 21.1. Remplace (ReplayTeams.tsx) :
    camouflage -> backdrop-filter: blur(6px) + voile color-mix(var(--foreground) 12%, var(--card))
    (teinter avec --card lui-meme aurait ete invisible, 0 contraste) ; surbouclier -> cadre inset 2px
    au nouveau token semantique `legendary` (ajoute a semantic-tokens.ts, 4 palettes, fallback CSS).
    Les deux effets composent (box-shadow accumule, un seul fond). Statiques, prefers-reduced-motion
    respecte par construction. 5 tests ajoutes.
    **Resultats observes** : distribution des durees des 36 episodes camo du golden 000d5950 (nouveau
    test camo_duration_distribution_test.go) : min 700ms, mediane 2700ms, moyenne 5438ms, max 34500ms.
    La prediction « l'essentiel tient en 1-2s » est REFUTEE au niveau de l'episode fusionne (21/36
    chaines) ; ce qui reste vrai et verrouille : le max d'un episode A ACTIVATION UNIQUE (15/36) est
    9600ms, tres sous une duree de power-up Halo (surbouclier du meme lot a 61,6s). Corrige les
    commentaires « POWER-UP de camouflage » dans golden_inputs_test.go et golden_assembly_test.go
    et i18n.ts (estompe/surligne, perime). Gates Go et web verts.
    **Conclusion / prochaine etape** : reste le gate VISUEL utilisateur (temoins 084a804d equipement,
    000d5950 dash-camo). Translocateur non code : aucun canal d'etat mesure (3 lectures i48 d'IDENTITE
    seulement dans le corpus, sur 06dfe6d9). Commits dd946f604 (mesure Go), 46f99475a (rendu web),
    base 020b95eab ; fusionnes dans feat/v75 le 2026-08-16.

### Entree 2 (lot drawer)

    ## [2026-08-16] Tiroir de reglages du rejeu 2D + filtres de sons par categorie
    **Statut** : Complete (branche `wt/drawer-replay`, commits `59cc6eb94` puis `5dd36609a`, base
    `020b95eab` ; fusionnes dans feat/v75 le 2026-08-16).
    **Decision technique principale** : le tiroir POUSSE le canvas (rangee flex : colonne canvas
    + panneau, ResizeObserver deplace de la carte entiere vers la seule colonne canvas) plutot
    que de le recouvrir en overlay (pattern AssetDrawer/FeedbackDrawer, ecarte) - seule facon de
    garantir "n'obstrue jamais la carte" quelle que soit la largeur d'ecran. Regroupe
    l'inventaire phase 0 complet (visee, zones, vitesse - tous useState locaux NON persistes
    avant ce lot - + son on/volume, deja persiste) et ajoute 4 filtres de categorie de son
    NOUVEAUX (armes/grenades/melee/equipements). Persistance unifiee sous replayPreferences.ts
    (patron localStorage try/catch centralise, migre depuis useReplaySound qui le portait seul ;
    garde-rail replayPreferences.guard.test.ts). Le filtre de categorie s'applique A LA
    CONSTRUCTION de buildSoundTimeline (4e parametre optionnel, defaut = tout actif =
    comportement inchange pour tout appelant existant), jamais en aval dans le lecteur.
    `available` (visibilite du panneau son) reste calcule sur la piste SANS filtre pour ne
    jamais faire disparaitre le seul bouton qui permet de tout rallumer.
    **Resultats observes** : typecheck/lint/test verts (427 fichiers de test, 3842 tests, 0
    echec). Decouverte hors perimetre mais bloquant le gate : le garde-rail
    fragClass.colorSource.guard.test.ts (feature frags) faisait un faux positif sur
    SoundCategoryFilter (heuristique >=2 cles parmi shoulder/sidearm/heavy/melee/grenade/
    spartan_ability). Corrige en relevant le seuil a 3 (commit separe 59cc6eb94) — A REJUGER A
    LA FUSION (allowlist ciblee vs seuil global).
    **Conclusion / prochaine etape** : reste le GATE VISUEL UTILISATEUR — panneau qui pousse
    (plutot qu'un overlay), et vitesse/calques desormais persistes entre sessions (comportement
    NOUVEAU). Item registre "Filtre des sons du rejeu par CATEGORIE" marque TRAITE.

## Textes a consigner — registre `.ai/V7.5/REGISTRE_REPORTS.md`

1. Ligne « Filtre des sons du rejeu par CATEGORIE (armes / grenades / equipements) » -> a
   REMPLACER par :

   `| ~~Filtre des sons du rejeu par CATEGORIE (armes / grenades / equipements)~~ | idee utilisateur, 2026-08-16 | — | **TRAITE (tiroir de reglages, 2026-08-16, wt/drawer-replay 5dd36609a, fusionne)** : 4 categories filtrables independamment (armes = tirs+kills d'arme meme table ; grenades = lancers ET explosions ; melee = coup fatal ; equipements = camo/surbouclier), tout actif par defaut (comportement inchange). Cable a la CONSTRUCTION de buildSoundTimeline (SoundCategoryFilter, 4e parametre optionnel), jamais en aval dans le lecteur ; SOUND_MAX_VOICES et la regle silence propre inchanges. Teste (« categorie coupee = aucun son de cette categorie, les autres inchanges ») |`

2. Ligne « Effets plein-fiche NON IMPLEMENTES » (equipement) : marquer camo + surbouclier
   SORTIS (verre + encadre dore, wt/effets-fiches 46f99475a) ; garder le translocateur `[!]`
   avec reprise = « un canal d'etat rang 11 mesure » + la piste spec Notion 21.2 : « i57 porte
   la direction de la capacite (cubemap 24 bits) » — le R(24) de la branche v==1 (valeurs quasi
   uniques, refute comme handle le 16/08) pourrait coder une direction quantifiee, meme famille
   que l'aim_vector 30 bits des fire events — non instrumente.
3. Ligne « camo/surbouclier SORTI » (lot du 16/08 matin) : ajouter « rendu mis en CONFORMITE lot
   effets-fiches, 2026-08-16 » ; remplacer « POWER-UP de camo » par « DASH du mode Fiesta
   (1-2 s, chainable) — PAS un power-up ramasse » ; remplacer « camo estompe, surbouclier
   surligne » par « verre / encadre dore ».
4. `PLAN_EQUIPEMENT_TI37.md` phase 3 §3.1 : la description « estompe opacite 0,4 » / « anneau
   token info » est perimee -> verre / encadre dore.
5. Ligne « golden minibobine ROUGE depuis 020b95eab » (si le lot alertes l'a ecrite) : marquer
   CLOS a la ratification.

## EXECUTION — CLOSE le 2026-08-16

- [x] **Etape 1** — `git merge --no-ff wt/effets-fiches` -> `7db99868c`. Auto-merge sans
      conflit textuel sur les deux goldens ; les deux cotes VERIFIES SUR PIECES apres coup
      (magic `REPLAYINPUTS5`, champ `GrappleReads`, `opt.MapQuant`, `renderGrapple` du
      principal ET commentaires « DASH du mode Fiesta » de la branche). Aucun texte fige ne
      bouge : ces commentaires ne sont pas publies dans le golden d'assemblage, `testdata`
      inchange, aucune regeneration necessaire. Distribution camo re-mesuree sur le fixture
      v5 : identique au chiffre du journal (min 700 / mediane 2700 / moyenne 5438 /
      max 34500 ms, 21/36 chaines, max a activation unique 9600 ms sur 15).
- [x] **Etape 2** — `git merge --no-ff wt/drawer-replay` -> `b26dfb852`. Auto-merge sans
      conflit. `ReplayCanvas.tsx` verifie : `drawGrappleLayer` toujours appele (l. 405-406)
      avec `grappleFx`/`grappleInk` dans les dependances, le canvas qui le recoit est dans la
      colonne mesuree, et le `ResizeObserver` observe cette COLONNE (l. 333-338 sur le `ref`
      de la l. 645), pas le cadre entier. `i18n.ts` : union des cles, parite FR/EN tenue par
      le typage. Controle de completude des deux fusions : les seules lignes d'une branche
      absentes de HEAD sont les lignes SUPPLANTEES (magic v4, alignement pre-gofmt,
      commentaire i18n perime) — rien de perdu.
- [x] **Etape 3** — revue de fusion (`adversarial-review`, deux relecteurs aveugles l'un a
      l'autre, contexte frais, lentilles L5+L3 et L6) -> `1cd57dd98`. 5 constats recevables
      corriges, 42 conditions verifiees qui tiennent, 2 constats consignes au registre sans
      etre traites (decisions de rendu, gate visuel). **Garde-rail frags TRANCHE** : le seuil
      2 -> 3 est ANNULE. Mesure decisive : sous `features/`/`components/`, `shoulder`,
      `sidearm`, `heavy` et `spartan_ability` n'apparaissent nulle part — seules `melee` et
      `grenade` y vivent, donc le maximum atteignable est 2 et la branche (b) du garde ne
      pouvait plus se declencher a 3, laissant passer exactement le defaut de son en-tete (la
      collision melee=grenade, un mapping a DEUX cles). Allowlist ciblee ECARTEE : le
      mecanisme du depot exclut par BASENAME et il faudrait allowlister `i18n.ts`, partage
      par 14 fichiers. Vraie cause : le predicat comptait des CLES sans regarder la VALEUR.
      Corrige — seuil 2 + exigence d'une valeur couleur/token, verifie DANS LES DEUX SENS.
- [x] **Etape 4** — ratification du golden `minibobine` -> `0a416cdfb`. Portee re-mesuree
      avant de re-congeler : 1 ligne sur 77 (la calibration), 10 lignes publiees et 3 ancres
      Theater identiques a l'octet. Canal de detection du garde toujours arme.
- [x] **Etape 5** — journal (3 entrees : les 2 verbatim des lots + celle de la fusion),
      registre (5 mises a jour + 1 ligne neuve pour la vitesse persistee), plan
      `PLAN_EQUIPEMENT_TI37.md` §3.1/§3.3 depoussiere, gates complets.

**Gates de cloture, joues sur l'arbre final** — Go : `go build` 0, `go vet` 0, `go test`
(analysis + replaybuild + contracttest + film) 0 sur 22 paquets, `golangci-lint
--new-from-merge-base=origin/main` **0 issue**. Web : `npm run typecheck` 0,
`npm run lint` 0 (19 warnings pre-existants, 0 erreur), `npm run test:run` 0 —
**428 fichiers, 3853 tests passes, 14 skippes**. `git status` propre hors les fichiers de
l'autre session.

**RESTE** — les gates que la session ne peut pas jouer : GATE VISUEL (fiches verre/or, tiroir
qui pousse le canvas) et GATE D'ECOUTE (filtres de sons par categorie). Aucun push : la
branche reste locale, la CI n'a donc pas tourne sur ces commits.
