# ti=40 (vehicule) — les 16 ecrivains `vehicle-*` nommes (2026-09-16)

> Preparation du lot 3.6, item 3.6.e. Sans production. Source : `HaloInfinite.exe` par Ghidra en
> LECTURE SEULE. Methode : `NOTE_3_6_METHODE_DESCRIPTEURS_2026-09-16.md`.
>
> Instrument / Ghidra lecture seule.

---

## 1. L'etat, colle depuis le depot

`ecs_table.tsv` : **48 composants**, 30 `porte`, 2 `partiel` (`i2
object-forward-and-up-dynamic-precision-component` et `i43 simulation-state-component`), **16
`non_porte`** — aucun des 16 n'avait d'adresse.

`keyframe_closure.golden` ne porte `ti=40` que sur 5 bobines, et la colonne « bloquant » y est
**VIDE** : `111fa685` 0/58, `11de8353` 0/220, `60ae07c4` 0/32, `a521164d` 0/335, `e5adf7b2`
0/132 — **0 ferme sur 777**. `ti=40` appartient donc, comme `ti=42`, a la population « aucune
desynchronisation nommee et aucune fermeture » : le golden ne designe pas de coupable.

C'est une difference importante avec `ti=9`, `ti=11`, `ti=12`, `ti=35` et `ti=43`, ou un
composant est NOMME. Ici, les 16 composants non portes sont des candidats, pas une cause etablie.

## 2. Le releve — 16 ecrivains sur 16

| Composant | Descripteur | Ecrivain | Grammaire relevee | Dependance de config | Statut |
|---|---|---|---|---|---|
| `i30 vehicle-auto-turret-triggers-component` | `0x143d0b668` | `FUN_142f04994` | non relevee | inconnue | a relever |
| `i31 vehicle-auto-turret-aiming-vector-component` | `0x143d0b6b8` | `FUN_14115f33c` | non relevee — un vecteur de visee, donc probablement des `R(16)` dequantifies (`FUN_142ee2194`) | bornes de quantification a verifier | a relever |
| `i32 vehicle-transformed-or-desired-open-state-changed-component` | `0x143d0b618` | `FUN_142f04b70` | non relevee | inconnue | a relever |
| `i33 vehicle-type-state-component` | `0x143d0b3e0` | `FUN_142f02474` | non relevee | inconnue | a relever |
| `i34 vehicle-type-physics-component` | `0x143d0b2f0` | `FUN_142f02498` | non relevee | inconnue | a relever |
| `i35 vehicle-auto-turret-target-component` | `0x143d0b758` | `FUN_142f0496c` | non relevee — une « cible » est probablement une reference d'entite | si reference : largeur selon le domaine | a relever |
| `i36 vehicle-sentry-state-component` | `0x143d0b708` | `FUN_142f04b34` | non relevee | inconnue | a relever |
| `i37 vehicle-emp-timer-component` | `0x143d0b480` | `FUN_142f049dc` | non relevee | inconnue | a relever |
| `i38 vehicle-weapon-set-component` | `0x143d0b4d0` | `FUN_14116d3cc` | non relevee | inconnue | a relever |
| `i39 vehicle-auto-turret-component` | `0x143d0b430` | `FUN_142f04884` | non relevee | inconnue | a relever |
| `i40 vehicle-equipment-turret-parent-component` | `0x143d0b5c0` | `FUN_142f04a00` | non relevee — un « parent » est probablement une reference d'entite | si reference : largeur selon le domaine | a relever |
| `i41 vehicle-seats-override-pitch-component` | `0x143d0b520` | `FUN_142f04a4c` | non relevee | inconnue | a relever |
| `i42 vehicle-seats-override-yaw-component` | `0x143d0b570` | `FUN_142f04ac0` | non relevee | inconnue | a relever |
| `i45 air-drop-flight-component` | `0x143d0b1b0` | `FUN_142f02508` | non relevee | inconnue | a relever |
| `i46 warp-component` | `0x143d0b2a0` | `FUN_142f04bcc` | non relevee | inconnue | a relever |
| `i47 vehicle-low-frequency-component` | `0x143d0b200` | `FUN_142f04a20` | non relevee — un « low-frequency » est la famille des composants larges (cf. `i15 object-low-frequency`, largeur moyenne 570 bits d'apres D1 (1.9.1 bis)) | inconnue, potentiellement grande | a relever, **suspect n 1** |

Note de methode : `i33` et `i34` n'ont pas ete resolus par la chaine automatique (leur chaine de
caracteres a DEUX references, dont une table a `0x143b8c860`) ; ils ont ete resolus a la main
depuis leur accesseur (`0x141177250` et `0x141177260`).

Les deux `partiel` restants portent deja leur adresse au depot : `i2` `FUN_140c5f7ec` (mode
`C=1`, `FUN_142e29bac`, non porte) et `i43` `FUN_142ED6D88` — **le meme
`simulation-state-component` que `ti=35 i60`**, dont le predicat de queue est resolu dans
`NOTE_3_6_TI35_2026-09-16.md`. Porter `i60` pour `ti=35` porte donc `i43` pour `ti=40` dans le
meme geste.

## 3. Ce que le port demandera

1. **Ne pas commencer par les 16.** Le golden ne nomme aucun bloquant pour `ti=40` : avant de
   porter, il faut savoir OU la traversee s'arrete. Le geste le moins cher est celui du lot
   1.9.1 bis (« preuve bornante » : le composant qui precede le premier dont le bit de depart
   depasse deja la frontiere), rejoue sur `ti=40`. Il demande un film — **hors perimetre d'une
   session sans decodage**, consigne ici.
2. Si la preuve bornante designe un des 16, son ecrivain est deja nomme ci-dessus et le relevage
   de sa grammaire ne demande aucun film.
3. `i43` arrive gratuitement avec `ti=35` : a sequencer 3.6.c AVANT 3.6.e.
4. Ordre de suspicion a defaut de mesure : `i47 vehicle-low-frequency-component` d'abord (la
   famille `*-low-frequency` est celle des composants les plus larges du moteur, et D1
   (1.9.1 bis) l'a deja identifiee comme preuve bornante dominante sur `ti=37`), puis `i2`
   (`partiel`, mode `C=1` non porte, et il precede tous les autres dans l'ordre).

## 4. Statut de l'item du plan

3.6.e **PREPARE A MOITIE** : 16 ecrivains sur 16 sont nommes, et `i43` est resolu par ricochet
via `ti=35`. Mais **le bloquant de `ti=40` n'est pas etabli** — le golden est muet, et l'etablir
demande une mesure sur film. Le lot 3.6.e commence donc par une mesure, pas par un port.
