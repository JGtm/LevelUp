# ti=42 (arme au sol) — negatif MESURE : il n'y a aucun composant a porter (2026-09-16)

> Preparation du lot 3.6, item 3.6.d (premiere moitie). Sans production. Source : le depot
> (`ecs_table.tsv`, `keyframe_closure.golden`) et le plan. Aucune decompilation : il n'y a rien
> a decompiler, et c'est precisement le resultat.
>
> Instrument / Ghidra lecture seule.

---

## 1. Le releve

| Composant | Descripteur | Ecrivain | Grammaire relevee | Dependance de config | Statut |
|---|---|---|---|---|---|
| (aucun) | — | — | — | — | **les 21 composants de `ti=42` sont `porte`** |

`ecs_table.tsv`, archetype 42 : **21 composants, 21 `porte`, 0 `partiel`, 0 `non_porte`.**

`keyframe_closure.golden`, les 7 bobines : `111fa685` 0/141, `11de8353` 1/155, `60ae07c4` 2/253,
`a521164d` 2/698, `bcb6d393` 4/437, `e5adf7b2` 1/187, `fb1a1a72` 0/216 — **10 fermes sur 2 087**,
et la colonne « bloquant » du golden est **VIDE** sur les 7 lignes.

## 2. Ce que cela veut dire, et ce que le plan en dit deja

Le lot 3.6 est defini comme « les composants manquants, archetype par archetype ». **`ti=42` n'a
aucun composant manquant.** Sa fermeture de 0,5 % n'a donc pas de cause portable : ce n'est pas
un port qui la corrigera.

Le plan a deja tranche ce point, deux fois, et cette note ne fait que le rattacher a 3.6.d :

- **D8 (1.9.1 bis, pas 2 bis)**, plan l. 4280 : la population a expliquer est de **six
  archetypes** — `ti=13, 21, 37, 38, 41, 42` — « aucune desynchronisation ET aucune fermeture » ;
  ces six portent 51 composants distincts, **aucun n'est commun aux six**, et **la suite se
  traite archetype par archetype, pas par une cause commune**. La piste nommee par le plan est
  precisement `ti=42` : « le plus petit de la classe (B) dont tout le cadre est deja prouve —
  c'est la ou un residu se lit le plus proprement ».
- **D1 (1.9.1 bis)**, plan l. 4268 : `ti=42` ferme **1/2 087** dans la mesure du 2026-09-15, et
  c'est l'archetype dont le depot dit la grammaire « reputee complete ».

## 3. Ce que le port demandera

**Rien a porter.** Ce que 3.6.d demande pour `ti=42` n'est pas un portage mais une INSTRUCTION :
un record de `ti=42` consomme tous ses composants sans desynchroniser et n'atteint pourtant pas
la fin de sa zone d'etat. Les deux hypotheses a departager, dans cet ordre :

1. **Une largeur juste mais un CADRE faux** — la borne de fin du record est mal calculee (c'est
   la famille de D2 (1.9.1 bis) : le ratchet mesure les archetypes objet aux largeurs d'axe
   d'une autre carte). Controle : rejouer la fermeture de `ti=42` avec le decoupage du catalogue
   installe, ce que le plan a deja mesure sans gain sur `ti=37` (3/3 331 dans les deux cas).
2. **Un composant present dans le registre du film mais ABSENT de `ecs_table.tsv`** — la table
   dit « qui existe » d'apres le registre commis, pas d'apres le registre du film mesure. Le
   garde-rail **G2** (`ECS_TABLE_FILM`, `ecs_table_guard_test.go`) est exactement l'instrument
   qui trancherait, et il SKIP proprement sans film.

Les deux controles demandent un film. **Hors perimetre d'une session sans decodage** : c'est
consigne ici, pas traite.

## 4. Statut de l'item du plan

3.6.d (volet `ti=42`) **NE SE PREPARE PAS PAR LA DECOMPILATION** : `[~]` — couvert par D8
(1.9.1 bis, pas 2 bis) du plan, qui le route deja vers une instruction de cadre et non vers un
portage. Le lot 3.6 devrait le dire explicitement au lieu de le ranger avec `ti=43`, qui est un
vrai lot de portage (22 composants).
