# ti=9 (joueur) — le composant bloquant, grammaire COMPLETE (2026-09-16)

> Preparation du lot 3.6, item 3.6.a. Sans production : aucune ligne de `filmdec` touchee,
> aucun film decode. Source : `HaloInfinite.exe` par Ghidra en LECTURE SEULE. Methode :
> `NOTE_3_6_METHODE_DESCRIPTEURS_2026-09-16.md`.
>
> Instrument / Ghidra lecture seule.

---

## 1. L'etat, colle depuis le depot

`ecs_table.tsv` : **10 composants**, 8 `porte`, 1 `partiel`, 1 `non_porte`.
`testdata/keyframe_closure.golden` (ratchet 0.A.3), les 7 bobines :

| Bobine | fermes / total | bloquant nomme |
|---|---|---|
| `111fa685` | 0 / 337 | `i4 managed-player-forge-weather-effect-overrides-component` |
| `11de8353` | 0 / 393 | idem |
| `60ae07c4` | 0 / 240 | idem |
| `a521164d` | 0 / 262 | idem |
| `bcb6d393` | 0 / 144 | idem |
| `e5adf7b2` | 0 / 261 | idem |
| `fb1a1a72` | 0 / 80 | idem |

**1 717 records, 0 ferme, un seul composant en cause.**

## 2. Le releve

| Composant | Descripteur | Ecrivain | Grammaire relevee | Dependance de config | Statut |
|---|---|---|---|---|---|
| `i4 managed-player-forge-weather-effect-overrides-component` | `0x143d08840` (slot du nom `0x143d08858`, chaine `0x143c95460`, accesseur `0x141177e70`) | **`FUN_142ed5bc8`** (`0x143d08880` = descripteur + 0x40) | **`R(32)` ; `R(32)`** — deux champs de 32 bits, ecrits dans cet ordre aux offsets `+0x3c` et `+0x40` de la structure du composant. **64 bits, INCONDITIONNELS** : aucune porte, aucun compteur, aucune boucle, aucun appel virtuel. Releve par les deux `ADD dword ptr [<reg> + 0x2c], 0x20` du desassemblage (`142ed5c70` et `142ed5d20`, plus leurs jumeaux de chemin rapide `142ed5bf9` et `142ed5cb6`) et par les deux seules ecritures de sortie (`142ed5ca1 MOV dword ptr [RDI + 0x3c]`, `142ed5d5d MOV dword ptr [RDI + 0x40]`) | **AUCUNE.** Rien de la largeur ne depend d'une table du jeu, d'une carte ni d'un build : deux largeurs litterales de 32 bits | **prete a porter** |
| `i9 managed-player-custom-input-prompt-widget` | non releve (deja `partiel` dans la table, `FUN_141fcf160`) | `FUN_141fcf160` | portes + `R(32)` + `R(3)` compteur ; si le compteur depasse 0, boucle a etiquette -> desync PROPRE | inconnue | `partiel`, **prochain suspect** |

## 3. Ce que le port demandera

1. Un `case` de plus dans le dispatch de `ti=9` : deux `R(32)` a la suite. C'est la meme forme
   que `i5 managed-player-active-mission-name-component`, deja porte (`FUN_142ed5ab0`, grammaire
   `R(32) + R(32)` dans `ecs_table.tsv`) — le patron existe, il n'y a rien a inventer.
2. La ligne de `ecs_table.tsv` a passer de `non_porte` a `porte`, avec `deser_addr`
   = `FUN_142ed5bc8`, `grammar` = `R(32) + R(32)`, `bits_typ` = `64`, `code_source` pointant sur
   le `case` du meme commit (garde-rails `ecs_table_guard_test.go`).
3. Le ratchet 0.A.3 regenere : la fermeture de `ti=9` doit MONTER sur les 7 bobines. Si elle ne
   monte pas a 100 %, le golden nommera le prochain bloquant — le suspect est `i9`, seul autre
   composant non entierement porte de l'archetype, et sa boucle a etiquette est deja consignee
   comme cause de desync propre.

## 4. Statut de l'item du plan

3.6.a **PREPARE** : il reste UN composant a porter sur `ti=9`, sa grammaire est ecrite ci-dessus
au bit pres, et elle ne porte aucune dependance de configuration. Le port est un `case` de deux
lectures de 32 bits.
