# ti=12 (navpoint) — 18 ecrivains nommes, le bloquant resolu (2026-09-16)

> Preparation du lot 3.6, item 3.6.b (seconde moitie). Sans production. Source :
> `HaloInfinite.exe` par Ghidra en LECTURE SEULE. Methode :
> `NOTE_3_6_METHODE_DESCRIPTEURS_2026-09-16.md`.
>
> Instrument / Ghidra lecture seule.

---

## 1. L'etat, colle depuis le depot

`ecs_table.tsv` : **28 composants**, **2 `porte` seulement** (`i0` et `i14`), **26 `non_porte`**,
et **aucune adresse de deserialiseur** pour ces 26. C'est l'archetype le PLUS depourvu de la
liste des utiles.

`keyframe_closure.golden`, les 7 bobines : `111fa685` 1/15, `11de8353` 0/31, `60ae07c4` 0/204,
`a521164d` 0/298, `bcb6d393` 4/160, `e5adf7b2` 0/60, `fb1a1a72` 0/111 — **5 fermes sur 879**,
bloquant nomme partout `i1 managed-navpoint-flags-component`. Mesure complementaire du lot 1.4
(plan, l. 4689), sur le temoin d'Assaut `c75f33b8` : **569 records, 0 ferme sur 569**, butee
`i1`.

Le bloquant n'est que le PREMIER de la file : `i1` est l'index 1 d'un archetype qui en compte 28
et n'en porte que 2. Fermer `ti=12` demande donc les 26, pas un.

## 2. Le releve — 18 ecrivains sur 26

| Composant | Descripteur | Ecrivain | Grammaire relevee | Dependance de config | Statut |
|---|---|---|---|---|---|
| `i1 managed-navpoint-flags-component` | `0x143d08020` | **`FUN_141094130`** -> `FUN_14109414c` | **`R(8)`**, un seul champ, INCONDITIONNEL, ecrit en `*(param_3 + 0x10) + 4`. Releve : les deux `ADD dword ptr [<reg> + 0x2c], 0x8` (`14109416f`/`141094171` et `1410941e3`) sont les deux chemins d'un MEME champ | **AUCUNE** | **prete a porter** |
| `i2 managed-navpoint-visibility-distance-filters-component` | `0x143d07fd0` | `FUN_140dbde1c` | non relevee | inconnue | a relever |
| `i3 managed-navpoint-visible-offscreen-filters-component` | `0x143d07f80` | `FUN_140dbdf80` | non relevee | inconnue | a relever |
| `i4 managed-navpoint-can-be-occluded-filters-component` | `0x143d07f30` | `FUN_140dbdfac` | non relevee | inconnue | a relever |
| `i5 managed-navpoint-visibility-filter-component` | `0x143d083e0` | `FUN_140dbe194` | non relevee | inconnue | a relever |
| `i6 managed-navpoint-docking-filter-component` | `0x143d08390` | `FUN_140dbdf34` | non relevee | inconnue | a relever |
| `i7 managed-navpoint-docking-order-component` | `0x143d08340` | `FUN_142ed5050` | non relevee | inconnue | a relever |
| `i8 managed-navpoint-docking-group-name-component` | `0x143d082f0` | `FUN_142ed5028` | non relevee | inconnue | a relever |
| `i9 managed-navpoint-formatted-text-component` | `0x143d08520` | `FUN_1410e7b90` | non relevee — le nom annonce la meme forme que `ti=11 i2` (`consumeObjectiveFormattedText`, liste taguee) | probablement une liste a compteur | a relever |
| `i10 managed-navpoint-timers-component` | `0x143d084d0` | `FUN_1410d9040` | non relevee — cf. `ti=11 i0` (`2 x R(7)`, valeur+1) | inconnue | a relever |
| `i11 managed-navpoint-manual-timer-initial-duration-component` | `0x143d08480` | `FUN_142ed5194` | non relevee | inconnue | a relever |
| `i12 managed-navpoint-manual-timer-current-duration-component` | `0x143d08430` | `FUN_142ed512c` | non relevee | inconnue | a relever |
| `i13 managed-navpoint-top-progress` | `0x143d08160` | `FUN_142ed51d8` | non relevee — le jumeau `i14 radial-progress` (PORTE) vaut `R(8)` dequantifie dans `[-1, 1]` | idem `i14` attendu | a relever |
| `i15 managed-navpoint-bottom-progress` | `0x143d080c0` | `FUN_142ed4fe4` | non relevee — meme famille que `i13`/`i14` | idem | a relever |
| `i16 managed-navpoint-override-flags` | `0x143d08070` | `FUN_140ebf834` | non relevee | inconnue | a relever |
| `i17 managed-navpoint-object-marker` | `0x143d082a0` | `FUN_141169e68` | non relevee | inconnue | a relever |
| `i18 managed-navpoint-position-offset` | `0x143d08250` | `FUN_140f04f68` | non relevee — un offset de position, donc probablement un bloc de vecteur (`FUN_142ee2194` = `R(16)` dequantifie dans `[-100, 100]`) | bornes de quantification a verifier | a relever |
| `i19 managed-navpoint-visual-states-component` | `0x143d08200` | `FUN_142ed521c` | non relevee | inconnue | a relever |

## 3. Le cas non resolu : les 8 `visual-state-groups`

`i20` a `i27` sont **huit instances d'un meme composant**, comme les seize
`sub-objective-entities` de `ti=11`. Leurs noms existent individuellement en `.rdata`
(`managed-navpoint-visual-state-groups-component-0` .. `-7`, autour de `0x143d05be8`) mais ils
ne sont PAS atteints par un accesseur de nom : ils sont rassembles dans une **table de
pointeurs de chaines** a `0x143d07f00` (releve : `143d05cc8`, `143d05be8`, `143d05c20`,
`143d05c58`, `143d05c90`, `143d05d00`, ...). La chaine en quatre pas de la note de methode
s'arrete donc au pas 3 pour eux. **NON RESOLU dans cette session** : il faut remonter de cette
table de noms au descripteur commun, ce qui est un autre geste que celui qui a servi aux 18
autres.

Bonne nouvelle : huit instances = UN seul ecrivain a trouver et a porter, pas huit.

## 4. Ce que le port demandera

1. **Un lot 3.6.b `ti=12` ne se fait pas en un `case`** : 26 composants, dont 18 ont desormais
   leur ecrivain nomme et 1 sa grammaire complete. Le dimensionnement honnete est : 18 grammaires
   a relever (une session de decompilation chacune, la plupart triviales au vu de leurs noms),
   plus 1 descripteur a retrouver pour les 8 instances de `visual-state-groups`.
2. L'ordre le moins cher : `i1` (`R(8)`, deja ecrit), puis les jumeaux de composants deja portes
   (`i13`/`i15` a cote de `i14`, `i9`/`i10` a cote de leurs equivalents `ti=11`), puis les
   `*-filters-component` (`i2` a `i6`, tous dans le meme voisinage `FUN_140dbd*`, donc
   probablement la meme forme), puis le reste.
3. Le ratchet 0.A.3 mesurera le progres composant par composant : le bloquant nomme au golden
   AVANCE d'un index a chaque port reussi. C'est l'oracle a utiliser ici, pas la fermeture
   totale, qui ne viendra qu'au dernier.

## 5. Statut de l'item du plan

3.6.b (volet `ti=12`) **PREPARE, ET REDIMENSIONNE** : le plan ecrit « ti=11 et ti=12 ferment a
100 % » comme un seul item ; sur pieces, `ti=11` est un composant a une inconnue et `ti=12` est
**26 composants**. Ce sont deux lots, pas un.
