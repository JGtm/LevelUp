# Registre des reports et re-explorations — pipeline v7.5 et apres

> Tenu par le superviseur, alimente a chaque cloture de lot (regle utilisateur du
> 2026-08-08). Un item entre ici quand il est REPORTE en connaissance de cause :
> bloquant contourne, feature remise, recherche ajournee. Chaque item porte sa
> condition de reprise — un registre sans condition de reprise est un cimetiere.

| Item | Origine | Pourquoi reporte | Condition de reprise |
|---|---|---|---|
| Cinq pictogrammes de mort extraits mais NON cables (`splatter`, `fusion_coil`, `waterfall`, `ricochet`, `player_left`) | lot 7.1, 2026-08-14 | la nature du degat ne descend PAS a ce grain : elle dit ARME / MELEE / GRENADE / VEHICULE / OBJET_EXPLOSIF / DEGAT_GLOBAL, jamais « ecrase par un vehicule » ni « bidon a fusion ». Les brancher au jugement poserait l'icone d'une AUTRE mort — la faute deja refusee au chantier killicon. Les PNG restent extraits et versionnes ; `killicon.NeutralDeath` n'a que DEUX entrees | une donnee MESUREE qui distingue collision et tir sur la classe VEHICULE — piste : `Category.CollisionDamage`, present dans l'enum mais JAMAIS observe sur la population des morts sans revendication (il faudrait d'abord en trouver une occurrence) — ou un champ de dead-state qui nomme l'objet du decor |
| Recherche KOTH (semantique zones/possession, piste Ghidra) | lot 2, 2026-08-08 | decision user : hors v7.5 ; decodeur direct supprime (lot 3), variante B prouvee exacte une fois mais selection de variante aveugle au contexte | post-release v7.5 ; corpus preserve sous `film/testdata/corpus_objectifs/` (minibobine KOTH `0a247154`, oracle 4-2) |
| Peuplement live de la source evenementielle objectifs | lot 1 `[!]`, 2026-08-07 | chantier pipeline film, pas un correctif ; unique ecrivain actuel = `cmd/diag_weapons_v3 -write` | prealable a TOUT score over-time zone/hill/skull et au branchement complet du containment |
| Containment : elargir `clockOffsets` au-dela de -10 s | lot 4, 2026-08-08 | mesure tronquee — 3 films sur 8 piquent a la borne du balayage ; taux 28,6 % a -5 s vs temoin plat | ~1 h de calcul (corpus lourd : LIMIT + plafond RAM, machine libre) ; DECISION USER en attente |
| Containment : oracle de justesse absent | lot 4, 2026-08-08 | on mesure « une zone rattachee », jamais « la bonne » ; seul releve terrain connu = Vagabond, absente du catalogue | **ORACLE DISPONIBLE (lot 5, 2026-08-08)** : Vagabond porte ses 3 zones de Bastion reelles dans `map_objectives.json` (asset `105f5d84`, variante `map.mvar`). Corpus : 3 matchs Strongholds Vagabond au registre. Rejouer le releve terrain du 2026-08-02 avec `cmd/zone-attribution` |
| ~~Alias Fragmentation Heavies dans `NormalizeMapName`~~ | lot 4 | — | **SORTI (lot 5, 2026-08-08)** : suffixe ` heavies` retire, justifie par level_id identique entre base et variante ; +43 matchs (22 Fragmentation, 11 Highpower, 10 Breaker) |
| ~~Highpower au catalogue sans zones Bastion~~ | lot 4 | — | **SORTI (lot 5, 2026-08-08)**, mais en NEGATIF : les 4 variantes des 2 assets Highpower declarent zero `strongholds_zone`, et aucun hash non resolu ne retombe sur un label de zone (recherche murmur3, 45 modes × 26 suffixes + 40 candidats). Voir la ligne « zones de Total Control » ci-dessous |
| Zones de Total Control absentes des variantes de CARTE (Highpower) | lot 5, 2026-08-08 | 25 matchs Total Control sur Highpower au registre, zero zone dans la variante de carte : la donnee est ailleurs | ouvrir la variante de MODE (game variant) — meme surface que `loadGameVariant` ; prealable a tout containment sur BTB |
| Fond de carte de Catalyst | lot 5, 2026-08-08 | **la premiere justification (8,6 % de couverture) etait FAUSSE** — denominateur non comparable, cf. rapport §4 ; remesure a perimetre egal : Catalyst 79,0 % contre 83,1 % pour Cliffhanger. Le vrai motif : `mapstruct-build` ne publie que des AABB, et la belle carte vient de la chaine des TRIANGLES | **DECISION USER 08/08 : portage en Go de la chaine des triangles = PRIORITE.** Plan : `.ai/V7.5/cartes/PLAN_PORT_TRIANGLES_GO.md` |
| Fond de carte de Vagabond (etape 2 : cartes non cuites) | lot 5, 2026-08-08 | le BSP `fo08_wetland` est la TOILE ; la carte vit dans les 4 709 objets Forge du `.mvar`. **Ce n'est PAS « Forge vs pas Forge »** — Cliffhanger aussi a ete concue dans Forge, mais 343 l'a CUITE dans un module dedie (ridgeline : 10 223 instances / 443 objets, contre fo08_wetland : 788 / 4 709) | etape 2 du portage : `type_id -> tag de modele` puis triangles du modele poses par la transformation de l'objet. Faisable, pas une impasse |
| Alias ` sentry defense` dans `NormalizeMapName` | lot 5, 2026-08-08 | meme preuve que ` heavies` (level_id identique) mais +1 match seulement — hors perimetre de l'item L5c | a ajouter au prochain lot qui touche `variantSuffixes` |
| 7 labels de mode non resolus, identifies mais non ajoutes | lot 5, 2026-08-08 | `oddball_exclude`, `extraction_exclude`, `slayer_include`, `firefight_exclude`, `firefight_objective`, `minigame_exclude`, `forge_exclude` ; aucun n'est un role, les ajouter ne change aucune zone | lot hygiene de cloture v7.5 : les verser dans `labelNames` et re-mesurer les non resolus |
| Litteral `film_chunks`/`film_manifests` en dur dans ~7 CLI | lot 4 (dette anterieure) | hors perimetre du lot | factorisation + garde-rail grep (regle des <= 2 copies) ; lot hygiene de cloture v7.5 |
| `loadGameVariant` en echec renvoie 0, nil sans tenter le chemin historique | lot 1, 2026-08-07 | preexistant, hors perimetre du correctif P0 | lot hygiene de cloture v7.5 |
| `steaktacularMedalIDForTitle` vs repli marge de score : lectures divergentes du slug vide | lot 1, 2026-08-07 | deux interpretations du meme cas, aucune fausse isolement | trancher l'interpretation canonique au lot hygiene de cloture v7.5 |
| Rejeu 2D public (piste F) | decision #2, 2026-08-02 | conditionne par la comprehension du CTF (564 tirs perdus vs 44) | verdict de la voie B (recherche CTF en cours) puis DECISION USER |
| match_player_positions : table inutilisable en l'etat | lot 4, 2026-08-08 | 0 ligne, match-level sans xuid (delta-compression), team a -1 ; la vraie source = pipeline rejeu (BuildFromFilm -> Track par xuid, 100 ms) | si un besoin de positions persistees emerge : repartir du pipeline rejeu, PAS de cette table ; sinon la droper (candidat lot hygiene) |
| `MeshResourcePackingPolicy` @186 du tag `rtgo` non porte | cartes/triangles, 2026-08-09 | Reclaimer le lit (`SingleResource` vs `ResourcePerPermutation`) ; nous concatenons les ressources en un blob unique. Aucun symptome mesure : 0 maillage nil, 0 descripteur hors bornes sur les 525 tags de ridgeline | si un tag rend un maillage aberrant ou un descripteur hors du blob : lire le champ et indexer les ressources separement (`Item.ResourceIndex` / `ResourceCount`) |
| Suite complete `go test ./internal/himap/` au-dela du timeout Go de 10 min | cartes/triangles, 2026-08-09 | `TestBalayageDesCartes` rasterise 27 cartes en serie ; la suite entiere echoue sur timeout, pas sur un test rouge. Contourne en ciblant les temoins par `-run` | decouper le balayage (sous-tests + `-short`), ou lui donner son propre tag de build |
| Tranche de jeu `[-12 ; +28]` mesuree sur UNE seule carte | cartes/triangles, 2026-08-09 | `TrancheDeJeuMin/Max` viennent du prototype (`s31_raster.py`) et sont confirmees au pixel pres sur Cliffhanger seule. Sur une carte plus haute ou plus encaissee, un plafond a +28 m coupera de la vraie structure | rejouer la comparaison pixel a pixel sur une 2e carte disposant d'une reference validee ; a defaut, deduire la tranche des ancres ou de la geometrie et verifier que Cliffhanger ne bouge pas |
| Zone jouable : 149 % d'exces (decor hors-jeu dessine) | cartes/triangles, 2026-08-09 | rendu VALIDE par l'utilisateur, mais il couvre une zone bien plus large que la zone jouable. TEMOIN donne par l'utilisateur : gris+rouge de la carte des ecarts = jouable, bleu = decor. REFUTES : tri par module, emprise par les ancres, tranche d'altitude seule | verifier d'abord si la silhouette de la reference est un effet du semis de points du prototype (budget de triangles plafonne a 40 000) ; sinon, regle d'accessibilite (composante connexe de sol praticable depuis les ancres) |
| Trous au milieu de la zone jouee (4,0 % de manquants) | cartes/triangles, 2026-08-09 | signale par l'utilisateur au gate visuel ; il propose un FOND sous la carte plutot que de la geometrie | decision de presentation cote rendu/web, a trancher au moment du branchement du fond de carte |
| Seuil de finesse 0,005 m2 calibre sur UNE carte | cartes/triangles, 2026-08-09 | le filtre « aire mediane de triangle » separe l'arene du decor (accord 38,5 % -> 66,7 %), mais le seuil est mesure contre la seule reference validee, celle de Cliffhanger | valider la REGLE (pas le seuil) sur les 26 autres cartes par l'oracle faible des ancres, comme au §1 ter. Si le seuil doit se retoucher par carte, la regle est mauvaise |
| Nettete du rendu : echelle de couleur par altitude + rendu plus plat | cartes/triangles, 2026-08-09 | demande utilisateur au gate ; l'eclairage actuel (Lambert + ambiante hemispherique 0,25 + 0,75 d) tasse les valeurs vers le blanc | a explorer apres validation de la zone jouable — c'est du rendu, pas de la geometrie |
| Le tri de zone jouable par le GRAIN ne vaut que sur les cartes naturelles | cartes/triangles, 2026-08-09 | REFUTE par Catalyst des la 2e carte : 162 instances ecartees contre 859 sur Cliffhanger. Une carte de roche a un decor grossier, une station spatiale a des abords modelises au meme grain que l'arene. Ce n'est pas un seuil a retoucher, c'est le critere qui ne s'applique pas | chercher un critere insensible au materiau — piste : ACCESSIBILITE (composante connexe de sol praticable atteignable depuis les ancres). La geometrie, elle, transfere sans retouche (19/19 ancres sur Catalyst) |
| Fond de carte Vagabond : etape 2 (cartes non cuites) | cartes/triangles, 2026-08-09 | mesure : `fo08_wetland` porte 13 281 instances sur 3,8 km (la TOILE) et 814 sur 463 m ; la carte vit dans les 4 709 objets Forge du `.mvar`. En prime, Vagabond n'a pas de nom de module exploitable au catalogue (module generique `map`) — ni cadre ni oracle | etape 2 du plan §4 : F1 `type_id -> tag de modele`, F2 poser les triangles par la transformation de chaque objet Forge. NE PAS tenter avec la chaine actuelle |
| Zone jouable par la COLLISION : NO-GO mesure, format `scgt` non decode | cartes/triangles, 2026-08-09 | le bloc `instanced physics instances` se decode entierement (stride 192, typeMask Bullet/Play/InvisibleWall/Render, guid apparie au rendu a 91 %), mais le decor porte lui aussi la collision Play : exces 149,3 -> 129,3 % seulement, et le drapeau Play PERD des positions jouees. Les 552 refs de collision se resolvent toutes en tags `scgt` (132 modules), format non decode (faux positif float32 documente) | porte fermee comme delimiteur de zone ; ne rouvrir le decodage `scgt` que si un besoin AUTRE que la zone jouable emerge (ex. hauteur de collision). Detail : `.ai/V7.5/cartes/INVESTIGATION_PHYSIQUE_ZONE_JOUABLE_2026-08-09.md` |
| Zone jouable par l'ACCESSIBILITE : NO-GO mesure | cartes/triangles, 2026-08-10 | la connexite pietonne depuis les ancres PERD des positions reellement jouees a tous les seuils (0,50 m -> 89,2 % ; 0,85 m -> 91,0 % ; 1,20 m -> 91,9 % contre 95,2 % sans filtre) et ne depasse jamais 40,2 % d'accord. Deux causes structurelles : le rendu est un CHAMP D'ALTITUDE, ou la connexite est presque gratuite (le relief rejoint l'arene par des pentes douces) ; et marcher sous-estime l'accessibilite reelle (chute, saut, ascenseur) | ne retenter que sur le VOLUME avec degagement (`volume.go`), pas sur le z-buffer. La primitive `ComposanteAccessible` est conservee comme instrument, non branchee |
| Zone d'exclusion (mort instantanee / compte a rebours) : DEUX CIBLES NOMMEES | cartes/triangles, 2026-08-10 | conviction utilisateur confirmee par sondage : `sddt` (structure design — kill volumes et soft ceilings dans les Halo precedents) et `pfnd` (pathfinding/navmesh) existent dans la variante `any/` des DEUX cartes et portent des coordonnees MONDE reparties dans la boite du bsp, pas un amas a l'origine. ridgeline : sddt 136 884 o / 19 646 triplets, pfnd 8 576 o / 459. catalyst : sddt 3 240 o / 269, pfnd 5 872 o / 435 | prochaine investigation : decoder `sddt` (volumes de mort) et/ou `pfnd` (surface navigable). Le navmesh est la definition POSITIVE de « ou un agent peut marcher » ; les kill volumes la definition NEGATIVE. Banc de mesure pret (`TestSondePhysiqueRendu`, oracle + accord) |
| ~~Coquille `sddt` comme CADRE universel : NO-GO~~ **CORRIGE le 2026-08-10 : GO** | cartes/triangles, 2026-08-10 | contre-verification de l'investigation : sur Catalyst elle retire 46,4 % de pixels en gardant 19/19 ancres, mais sur Cliffhanger — seule carte a oracle FORT — elle coute 10 points de positions jouees (93,82 -> 84,09 % avec le grain) et fait BAISSER l'accord (66,7 -> 62,4 %). Elle paraissait gratuite sur Catalyst parce que l'oracle y vaut 19 ancres et non 29 221 positions | ne pas l'appliquer aux cartes natives. Reprise possible si un oracle fort (film decode) apparait sur une 2e carte : le verdict pourrait differer selon la forme de la coquille |
| Volumes de mort des cartes FORGE : TROUVES, non exploites | cartes/triangles, 2026-08-10 | `type_id` du `.mvar` = GlobalID d'un tag `food` de `forge_objects-rtx-new.module` (20/20 resolus). Empreinte sur 101 `.mvar` : `-588988541` = volume de mort principal (1 par carte sur 61, aire med. 2 206 m2, 5,5 m sous le sol) — sur Dynasty il tombe au centre, 7 m sous le sol, ce qui COLLE au temoin utilisateur de la fontaine letale. `176825834`/`937132837` = planchers de mort, `-1751270658` = murs de limite | prototype carte Forge : canevas `fo*` + objets `.mvar` moins les volumes de mort. Debloquerait ~100 cartes du catalogue — le plus gros gisement du chantier |
| Run de MASSE backfill-replay (~949 films, ~8 h, artefacts ~2 Go) | lot 6, 2026-08-14 | borne dure de la session executrice : le CLI + le pilote 25 films sont livres, le run complet revient a l'orchestrateur | `go run ./cmd/levelup backfill-replay` (reprenable par SchemaVersion, re-lancable tel quel) ; SERVEUR ARRETE de preference — metadata tenue RW degrade la resolution EN des noms de carte (les map_name UUID tombent en « hors catalogue » au lieu de se resoudre) |
| Re-run backfill-killsource COMPLET (couverture arme-du-kill 0-5 % sur les matchs recents) | lot 6 item 6.5, 2026-08-14 | exige `OpenReadWrite` sur shared = SERVEUR ARRETE ; le serveur air du user tourne et la session a interdiction de le redemarrer | orchestrateur : arreter le serveur (accord user), `go run ./cmd/levelup backfill-killsource`, relancer ; pilote `--limit` a jouer d'abord dans la meme fenetre |
| File durable des jobs replay_build (jobs.json = semantique locale) | lot 6 item 6.2, 2026-08-14 | piste F prod (ouvrier distant + heartbeat) ; hors perimetre v7.5 | activation prod post-tag v7.5.0 (cf. §Hors perimetre du plan parite) |
| ~~Riviere de Cliffhanger absente du fond de carte~~ | cartes/triangles, 2026-08-10 | — | **SORTI (lot riviere, 2026-08-10)** : lecteur `sddt` promu en production (`internal/himap/sddt.go`), eau dessinee sur le fond de carte (PoseEau + TeinteEau), banc inchange a la decimale (accord 66,7 %, positions 93,82 %, terrain compare a l'octet). Balayage : 10/31 cartes installees ont de l'eau sddt — feature generale. Reste le gate visuel utilisateur (images sur le Bureau) |
| Couleur de l'eau non passee au gate visuel | lot riviere, 2026-08-10 | le bleu (`TeinteEau`, 62/112/152 + berge 38/70/96) est un choix de session ; le juge du rendu est l'utilisateur | soumettre `cliffhanger_riviere.png` et `forbidden_riviere.png` (Bureau) au gate visuel ; ajuster la teinte si demande |
| Eau des cartes FORGE absente de `sddt` | lot riviere, 2026-08-10 | balayage 31 cartes : les canevas fo* n'ont AUCUN volume d'eau sddt (12 frontieres generiques seulement). L'eau de Vagabond, si elle est representee, vit ailleurs — probablement dans les objets du `.mvar` | si le prototype Forge (lot 2) rencontre de l'eau : chercher un type_id `food` d'empreinte « surface d'eau » sur les 101 `.mvar` du dump |
| ~~Prototype Forge : 1 139/4 697 objets sans ref rtgo directe~~ **CLOS le 2026-08-13 (lot B)** | lot forge, 2026-08-10 | — | **SAUT LIVRE** : `food -> bloc/scen/mach -> hlmt -> mode` (les 125 hlmt du saut ne referencent AUCUN rtgo — leur geometrie est un tag `mode`/render_model, lu par `NewRenderModelAsset`, offsets Reclaimer `RenderModelTag.cs`). Index elargi aux globals `any` (17 food y resolvaient leurs definitions). Vagabond : 3 558 -> **4 633/4 709 objets dessines (98,4 %)**, ancres 4/4 inchangees, ecart median -0,01 m. Sonde durable : `sonde_forge_saut_gamefiles_test.go`. Reste 38 objets sans modele — ligne dediee ci-dessous |
| Lot B : 38 objets de Vagabond toujours sans modele | lot B, 2026-08-13 | 18 objets (6 type_id) dont le `food` ne reference AUCUN bloc/scen/mach ; 12 objets (1 type_id) sans tag `food` du tout ; ~8 objets d'un modele du saut qui ne s'ouvre pas (77 modeles distincts, 76 ouverts et decodables) | si le gate visuel reclame ces objets : sonder les 6 food restants (histogramme de leurs refs contre l'index complet — groupes candidats : `rtmp`, `foki`, `fosp`) et le modele non ouvrable (id au log debug de la cuisson) |
| Industrialisation des ~99 cartes Forge communautaires | lot B, 2026-08-13 | hors perimetre du lot B (decision superviseur) : la chaine sait cuire UNE carte Forge declaree (`CartesForge`), pas le catalogue entier | chantier dedie : cle de fond -> `map_id` (cf. ligne « Cle d'asset d'une carte FORGE »), extraction d'ancres `map_objectives.json` par asset UGC, et balayage des 101 `.mvar` du dump par l'oracle des ancres |
| Prototype Forge : choix de variante rtgo naif | lot forge, 2026-08-10 | un food dessinable porte PLUSIEURS refs rtgo (liste a offsets reguliers, probablement variantes/etats casses) ; le prototype prend la PREMIERE dans l'ordre du tag | si des objets sont visiblement faux au gate visuel : decoder la structure de la liste de variantes (strides 0xC0 observes) et choisir la variante par defaut explicitement |
| Prototype Forge : toile fo08_wetland non rendue sous les objets | lot forge, 2026-08-10 | le bsp du canevas (13 830 instances) n'est pas dessine — la carte Forge actuelle est faite des seuls objets .mvar ; le sol nature (marais) manque peut-etre | decision au gate visuel utilisateur : si le fond manque, rendre le bsp du canevas (chaine sbsp existante) SOUS les objets, cadre sur les ancres |
| Prototype Forge : chiralite du repere non verifiable par les ancres | lot forge, 2026-08-10 | `Left = Up x Forward` (convention containment.go, validee par l'oracle de containment) ; un miroir global ne changerait pas « du sol sous l'ancre » | gate visuel utilisateur : comparer `vagabond_forge_proto.png` a la carte en jeu (orientation des structures) |
| Mort instantanee vs compte a rebours entre types `food` | cartes/triangles, 2026-08-10 | non distingue par l'investigation | rejeu en cache sur carte Forge avec morts hors-combat horodatees |
| Volumes fins des cartes NATIVES | cartes/triangles, 2026-08-10 | `scnr` n'existe pas dans Infinite (installation entiere balayee) ; `stse` = enveloppe convexe du bsp ; `dwsg`/`hlds`/`cage`/`sred`/`snad` negatifs. Le `.mvar` des cartes natives ne porte que les zones de mode | non necessaire au fond de carte actuel. Reprise : triangulation en jeu, ou diff de groupes de tags non interpretes entre deux cartes aux limites tres differentes |
| Hypothese « environnement ferme » : refutee sur CLIFFHANGER SEULE | cartes/triangles, 2026-08-10 | teste dans le VOLUME (un rendu vu du dessus ecrase les murs sur leur arete, il ne peut pas porter une cloture) : inondation depuis les bords du cadre a hauteur de poitrine. A +0,60 / +1,20 / +2,00 m, la zone enfermee ne garde que 42,3 / 34,8 / 42,1 % des positions jouees et 22 a 32 % d'accord. Cliffhanger n'est PAS clos : a hauteur d'homme l'inondation atteint 70 a 80 % de la carte, le test ne trouve que les salles couvertes. **La frontiere d'une carte native est DECLARATIVE (coquille `sddt`), pas geometrique** | ne pas rejouer. `Volume.ZoneClose` conserve comme instrument, non branche |
| CORRECTION — la coquille `sddt` au NIVEAU DE JEU est un GO | cartes/triangles, 2026-08-10 | le NO-GO precedent portait sur la coquille testee a l'altitude DESSINEE : un rocher haut sort de la coquille, sa cellule est effacee, et la position jouee EN DESSOUS perd son sol. Testee au niveau de jeu : Cliffhanger garde 99 % du cadre a cout NUL (accord 66,7 %, positions 93,82 % inchangees), Catalyst retire 47,1 % du decor en gardant 19/19 ancres | ACTIVE PAR DEFAUT (`Rendu.RestreintALaCoquille`). Gate restant : le balayage des 27 cartes par l'oracle des ancres — une carte qui PERD des ancres est disqualifiante |
| CORRECTION — « environnement ferme » teste sur une seule carte | cartes/triangles, 2026-08-10 | le verdict a ete rendu sur Cliffhanger SEULE, qui est une arene a ciel ouvert sur une falaise — le cas le moins favorable a l'hypothese. Catalyst, station spatiale, n'a jamais ete testee | rejouer `TestClotureCliffhanger` sur Catalyst si le besoin revient. Devenu accessoire depuis que la coquille fonctionne |
| `Coquille()` suppose UN SEUL convexe — 4 cartes amputees | cartes/triangles, 2026-08-10 | balayage des 25 cartes : behemoth (64 plans, 14 ancres -> 0, 100 % efface), forbidden (11 plans, 13 -> 0), fragmentation et sa variante Heavies (18 plans, 22 -> 12 et 20 -> 7). Les 21 autres ne perdent rien. Cause presumee : l'intersection de TOUS les triangles-frontieres comme un seul convexe, alors que ces cartes declarent plusieurs volumes ou une frontiere non convexe — l'intersection de volumes disjoints est vide | grouper les triangles par volume (comme les volumes d'eau, deja lus en liste) et prendre l'UNION. Garde en place en attendant : `CoquilleGardeLesAncres` refuse la coquille qui exclut une ancre — la livraison est sure, mais le decor reste entier sur ces 4 cartes |
| ~~`Coquille()` suppose UN SEUL convexe — 4 cartes amputees~~ **CORRIGE le 2026-08-10** | cartes/triangles | la cause n'etait PAS « plusieurs volumes fusionnes » (le bloc parent n'a qu'UN enregistrement par carte) mais le fait que la frontiere est un MAILLAGE FERME NON CONVEXE : behemoth a 116 triangles pour 64 plans, et l'intersection de ses demi-espaces est vide. Correctif : parite de rayon (`Sddt.ContientFrontiere`) | CLOS. Les 4 cartes sont reparees : behemoth 12,1 % de decor retire, forbidden 36,8 %, fragmentation 27,2 %, heavies 22,9 % — 0 ancre perdue |
| ~~AUCUN asset de carte n'est produit~~ **CLOS le 2026-08-10 (lot A)** | cartes/triangles, 2026-08-10 | — | **17 assets produits** (16 cartes natives + Vagabond), `data/titles/halo_infinite/reference/map_backgrounds/{cle}.png` + sidecar de calage, 4,92 Mo, style `jeu`, 0,0920 m/px, fond transparent. Chaine sortie des tests (`internal/himap/cuisson*.go`, `fond_png.go`), orchestration `cmd/mapfond-build`, chemins `PathResolver.MapBackground*`. Banc inchange (66,7 % / 93,82 %), determinisme verifie au bit. Reste C4 : le GATE VISUEL utilisateur |
| GATE VISUEL utilisateur sur les fonds de carte produits | lot A, 2026-08-10 | la session ne peut pas juger son propre rendu — regle du chantier : les temoins viennent de l'utilisateur | soumettre 3 cartes au choix parmi les 17 (`data/titles/halo_infinite/reference/map_backgrounds/`). DEUX arbitrages attendent : (1) le STYLE — `jeu` est le defaut de production, mais Cliffhanger/Catalyst/Vagabond ont ete validees en `combine`, AVANT que `jeu` existe ; re-cuire sous un autre style = `--style combine` ; (2) la tranche, ci-dessous |
| ~~Tranche de jeu ABSOLUE : deux cartes amputees~~ **TRANCHE le 2026-08-10 par l'utilisateur, sur pieces** | lot A, 2026-08-10 | `[-12 ; +28]` etaient des altitudes ABSOLUES mesurees sur Cliffhanger (sol a -2,2 m). Le niveau joue des 17 cartes va de -136,7 m (`chasm`) a +52,3 m (Vagabond) : `chasm` se reduisait a DEUX TRAITS (5/17 ancres) et `btb_highpower` etait aux deux tiers vide (14/51). L'utilisateur a vu les images et confirme : « Chasm n'affiche rien d'exploitable et highpower pareil » | **BASCULE EN RELATIVE** (`himap.TrancheDeJeu`, une seule expression partagee par la production, la chaine Forge et le banc). Resultat : chasm 17/17, highpower 38/51, Catalyst et Cliffhanger inchanges sur leurs ancres. **Banc RE-BASE par ecrit** : accord 66,7 -> 64,7 %, exces 33,8 -> 39,1 % (la vallee entre dans le cadre), positions jouees 93,82 -> 93,95 %. CLOS. L'EPAISSEUR de 40 m, elle, reste mesuree sur une seule carte (ligne dediee ci-dessus) |
| ~~Decoupage universel des callouts sur sol praticable~~ **CLOS le 2026-08-16 (phase A, PLAN_CALLOUTS_HEATMAP)** | lot 3 parite rejeu, 2026-08-13 | le decoupage n'avait pas besoin de la chaine cartes : le masque praticable est DEJA publie (canal alpha des `map_backgrounds/{cle}.png` + sidecar de calage), donc hors ligne pur et reproductible. `internal/mapdecoupe` + `cmd/mapcallouts-build` | **19 cartes `decoupe`** (18 par la chaine universelle, Ridgeline garde le dump du POC), **3 `brut`** faute de fond publie (`academy_tutorial`, `pve_house`, `sgh_interlock`). Gate A passe : IoU median 0,872 contre le decoupe POC (seuil 0,85), pire perte de positions jouees -1,10 pt sur 7 films de 7 cartes (seuil 2), 3 zones degenerees listees. Contrat OpenAPI inchange, brut conserve au catalogue (625 zones) |
| L'ALTITUDE n'est pas publiee par le fond : pas de test d'ETAGE | phase A callouts, 2026-08-16 | verifie sur pieces : le PNG est un RGBA d'habillage (`TeinteNiveauDeJeu(dz, eclairement)` melange deux inconnues sur 8 bits, l'arete et l'eau ecrasent le reste) et le sidecar ne publie qu'UNE altitude par carte (`stats.playLevelZ`). Le decoupage par etage prevu est donc impossible sur les artefacts versionnes. **La degradation va dans le bon sens** (on ne retire que le vide a TOUTE altitude), mais elle coute : c'est `Enclave` sur Ridgeline, IoU 0,501, ou la chaine garde 97,6 % quand le POC gardait 49,1 % | publier une COUCHE D'ALTITUDE a cote du PNG (PNG 16 bits ou binaire compact, meme calage) ferait remonter l'IoU et permettrait le vrai test d'etage. A peser avec le cout de re-cuisson des 21 fonds |
| Quatre fonds publies PERDENT des ancres — c'est ce qui borne le decoupage | phase A callouts, 2026-08-16 | `btb_highpower` 38/51, `btb_exiled` 30/35, `btb_fragmentation` 36/46, `forest` 14/18 ancres avec sol. Mesure decisive de la phase A : a tolerance nulle, seules 60,11 % des positions jouees de `btb_highpower` tombaient sur du decor publie — le decoupage ne rognait pas trop, le FOND manquait. Compense par une fermeture morphologique de 4 m (etalonnee, pas choisie) | c'est le meme fil que « on ne voit que les TOITS » et que le grain `AireMaxTriangleJouable` : toute amelioration de la cuisson permet de RABAISSER la tolerance, donc de rogner plus juste. Re-jouer `TestOracleTolerance` apres toute recuisson |
| `btb_drydock` : part gardee mediane 75,3 %, seule carte loin de 100 % | phase A callouts, 2026-08-16 | les 17 autres cartes decoupees ont une mediane a 100 % (seules les grandes zones debordent). Sur drydock, la MOITIE des zones perd un quart de son pave : soit les paves debordent vraiment, soit le fond est incomplet | a regarder au gate visuel utilisateur, avec Ridgeline comme temoin |
| `MondeVersPixel` tronque au lieu d'arrondir vers le bas | phase A callouts, 2026-08-16 | `int()` sur une valeur negative tronque vers zero : un point jusqu'a une cellule a GAUCHE ou AU-DESSUS du cadre est rendu comme le pixel 0 au lieu d'etre declare hors cadre. Effet negligeable sur les mesures (le cadre est le voisinage des ancres), mais c'est une erreur de conversion dans le SEUL depositaire de la formule de calage | `math.Floor` a la place de `int()`, plus un test sur un point a une demi-cellule hors cadre. Hors perimetre de la phase A |
| Appariement carte -> dossier PAR LES ANCRES : **NO-GO mesure** | lot A, 2026-08-10 | l'idee etait de remplacer l'heuristique de jetons de nom par une mesure : « le module dont un bsp contient les ancres ». Son temoin (retrouver les appariements deja connus) l'a tuee DEUX fois. (1) Premiere ecriture : « ne tranche pas » partout avec des scores PLEINS — chaque carte declare a cote de son arene un bsp d'HORIZON de plusieurs kilometres, qui contient les ancres de n'importe qui. (2) Departage par le plus PETIT volume : la mesure contredit alors le nom sur ~20 cartes connues (Catalyst -> `ctf_breaker`, Cliffhanger -> `ctf_breaker`, Oasis -> `ridgeline`). **CAUSE STRUCTURELLE : chaque carte Halo est batie autour de SA propre origine, les boites monde se recouvrent, et une position monde ne designe aucune carte** | NE PAS REJOUER. Le code a ete supprime (git garde l'historique). Toute reprise devrait passer par un identifiant, pas par une position — piste : `mapvar.Variant.LevelID`, lu DANS le `.mvar`, deja utilise par `NormalizeMapName` pour rapprocher une variante de sa base |
| ~~« 9 modules du catalogue non installes localement »~~ **AFFIRMATION FAUSSE, corrigee** | lot A, 2026-08-10 | la commande rangeait `deadlock_map`, `oasis_map` et `scarr_map` sous « non installees » alors que leurs dossiers `btb_drydock`, `btb_exiled` et `btb_engine` SONT presents : l'appariement par jetons de nom echouait en silence et fabriquait une explication fausse. Signale par l'utilisateur (« y en a bien plus dans le jeu non ? ») | **CORRIGE** : le depot de variantes porte le lien en clair (`deadlock_btb_drydock.mvar` a cote de `deadlock_map.mvar`). Convention VALIDEE sur les 18 appariements deja connus, 0 manquant. Rend Deadlock (46/46 ancres), Oasis (30/35), Scarr (19/19) et Corpo (4/4, via la chaine FORGE : son canevas `fo11_blank` est vide par construction). **21 cartes publiees.** Reste hors de portee : ce qui n'est pas installe localement |
| Les 4 cartes recuperees n'ont pas de nom AFFICHE | lot A, 2026-08-10 | `btb_drydock`, `btb_engine`, `btb_exiled`, `fo11_blank` sortent avec une colonne « noms » vide : `map_quant_bounds.json` ne les connait pas, et la regle de prefixe ne se declenche pas (`deadlock_map` ne finit pas par `_btb_drydock`). Elles restent identifiables — leur sidecar porte `deadlock_map` dans `mapNames` | la donnee EXISTE deja et est validee : `liensDuDepot` calcule `carte -> module`, son inverse donne le nom affiche. Une ligne dans `ciblesNatives`, puis une re-cuisson. Non fait pour ne pas enchainer une cuisson de 35 min sur de la cosmetique |
| Le lien carte -> module depend d'un depot NON VERSIONNE | lot A, 2026-08-10 | `resolution.go` lit `.ai/re_dump/mapvar/*.mvar`, qui est dans le `.gitignore` au meme titre que l'installation du jeu. Une machine sans ce dump cuira 3 cartes de moins, en silence relatif (elles passent en « non installees ») | si le lien doit devenir robuste : le figer dans une donnee de reference VERSIONNEE (une colonne de `map_quant_bounds.json`, ou un fichier dedie produit une fois), ou le lire dans `LevelID` du `.mvar` plutot que dans son nom de fichier |
| **On ne voit que les TOITS sur les cartes couvertes** | gate visuel utilisateur, 2026-08-10 | verdict sur les 17 cartes : Illusion, Prism (`sgh_crystalcaves`) et Aquarius rendent « la forme globale correcte, mais on ne voit que les toits ou plafonds » ; Forbidden en souffre partiellement (une partie couverte, une partie a ciel ouvert, ce qui est juste) ; Streets et Bazaar sont « un peu rudimentaires ». Cause structurelle : le z-buffer garde la surface la PLUS HAUTE, et sur une arene couverte c'est le toit. **PISTE FAUSSE, verifiee et ecartee** : l'ecart median aux ancres NE PREDIT PAS le defaut — Behemoth (-12,73 m) et Fragmentation (-11,71 m) sont juges nickel, Aquarius (-2,56 m) et Forbidden (-0,35 m) sont touches | prochaine piste : `SurfaceReference` + `CarteParReference` existent deja (`reference.go`) et retiennent « la bande praticable la plus PROCHE du sol de reference » au lieu de « la plus haute » — ecrites pour le `Volume`, jamais branchees sur le `Rendu`. RISQUE A MESURER AVANT : sur Cliffhanger, les rochers hauts font partie de l'identite de la carte et l'utilisateur les valide ; les remplacer par ce qui est dessous changerait une carte jugee nickel. Mesurer sur l'oracle fort AVANT de brancher |
| Streets et Bazaar « un peu rudimentaires » | gate visuel utilisateur, 2026-08-10 | juge moins riche que les autres, sans cause identifiee par l'utilisateur (« je ne sais pas pourquoi »). Les deux ont un ecart median voisin (-4,33 et -4,22 m) et un fort retrait de decor par la coquille (87,8 % sur Streets) | verifier si la coquille de `sgh_streets` retire de la matiere jouee (87,8 % est de loin le plus fort retrait des 25) et si le grain 0,005 ecarte trop d'instances sur ces deux cartes (Streets : 70 instances ecartees seulement, contre 859 sur Cliffhanger — donc probablement PAS le grain) |
| T5 (plan des triangles) : appariement sommets/indices JAMAIS mesure | lot A, 2026-08-10 | l'invariant est bien APPLIQUE — `RuntimeGeoAsset.triangles` ecarte toute face dont un indice depasse le tampon de sommets, pour la bonne raison — mais la mesure qui DEPARTAGE (100 % pour le bon appariement contre 5,1 % pour un LOD voisin) n'a jamais ete jouee. On ignore combien de faces la chaine jette | compter les faces ecartees par tag sur ridgeline, et refaire la mesure avec un LOD voisin comme temoin. Si le taux d'ecart est non negligeable, c'est de la geometrie perdue sur toutes les cartes |
| ~~Cle d'asset d'une carte FORGE = son canevas~~ **CLOS le 2026-08-13 (lot fonds par map_id, phase A)** | lot A, 2026-08-10 | Vagabond etait publiee sous `fo08_wetland` (son module de canevas) ; la condition de reprise (deux cartes sur le meme canevas) est arrivee avec le perimetre v7.5 « cartes manquantes » | **FAIT** : la cle de publication Forge est le `map_id` (`CarteForge{MapID, Nom, FichierMvar, ModuleCanevas}`), le service resout map_id d'abord (`MapKeysForMatch`), Vagabond et Corpo migres (`{map_id}.png/json`), fichiers module-keyes supprimes, garde-rail `TestFondForgeJamaisSousCleModule` |
| Aucun consommateur ne lit encore `map_backgrounds/` | lot A, 2026-08-10 | le lot A produit l'asset et publie son calage ; le brancher sur le rejeu 2D / la web est un autre chantier. Le type et le lecteur existent cote consommateur (`internal/analysis/replay/map_background.go`, `MondeVersPixel`) et sont GPL-propres | chantier d'affichage : poser le fond sous le rejeu 2D. Prealable trivial — le calage est publie et teste. Attention : le calage a une convention Y descendante, ecrite dans le sidecar |
| Fonds de carte versionnes : 4,92 Mo dans le depot | lot A, 2026-08-10 | les assets DOIVENT etre versionnes (la prod n'a pas d'installation du jeu pour les produire), au meme titre que `map_structure/`. 4,92 Mo pour 17 cartes, ~290 Ko de moyenne | si le poids devient genant en ajoutant des cartes : PNG paletise (`image.Paletted`, supporte par l'encodeur standard) — gain attendu 3 a 4x, au prix d'une quantification des couleurs a re-passer au gate visuel |
| Rejeu 2D : attribution fine des tirs orphelins — cas-limite rare | voie B + rondes, 2026-08-11 | la fermeture A du pont place un tir sans position pile a l'instant sur le seul corps proche ; corroboration positive (ancrage + terminalite, `closures.go:bodyExtendsShooter`) apres 2 rondes. RESIDU indiscernable par construction (une vie anonyme n'a pas de proprietaire lisible) : un tir peut, tres rarement, se poser sur le mauvais joueur. Volume quasi nul (fermeture A = 0 attribution sur 64e8adfa apres correctif). Le rejeu est OFF en prod (garde local) | a l'HYPERCARE, quand le rejeu s'allume : evaluer la refonte B-avant-A (faire trancher le fil des morts AVANT la fermeture par corps disponible — recupere le sous-ensemble ou la mort de l'autre joueur est propre, sans fermer le cas indiscernable). Refonte de l'ordre des fermetures, pas une ronde |
| Seuil d'activation du rejeu public = 88 % | voie B, 2026-08-11 | le garde local exige >= 88 % de couverture des tirs par film ; apres le correctif de justesse, `64e8adfa` tombe a 87,39 % (corpus 6/7). MAIS le seuil 88 % avait ete calibre sur une couverture GONFLEE par les fausses attributions maintenant retirees — comparer 87,39 % juste a 88 % gonfle = pommes/oranges | RE-STATUER le seuil A L'HYPERCARE (au moment de l'activation prod reelle), avec la couverture desormais JUSTE comme repere. Ne pas negocier d'exception par film (defaut « choisir ses films », tranche 31/07). Garde en place = rejeu local jusque-la |
| killpos (positions de mort du rejeu) : calcul livre, ECRITURE non branchee | voie B, 2026-08-11 | `analysis/replay/killpos.go` calcule les positions de mort (teste) mais ne les persiste pas : aucun binaire ne pilote BatchBuilder, la seule construction de BatchQueue est dans cmd/server. Alimenter une table partagee hors ligne est une decision de conception sur le chemin anti-ART (ADR 0019/0030) | etape 2bis DATEE **2026-11-08** (echeance posee par l'executeur) : instruire le design d'ecriture offline anti-ART, OU supprimer le producteur si aucun consommateur ne l'exige d'ici la |
| Mystere Fiesta / Husky Raid : la moitie des tirs perdus | voie B, 2026-08-11 | fait solide (meme rythme de tir, deux fois moins d'evenements enregistres), trois explications eliminees, cause inconnue. ~30 % du corpus, AUCUN impact sur les tirs fatals (qui/quand/quelle arme acquis a 99,55 %) | post-v7.5 : ne rouvrir que si un besoin de couverture exhaustive des tirs NON fatals emerge. Trois hypotheses classees au verdict CTF (vague de renouvellement ~747 s, id d'arme hors catalogue, rejets « ambigu » Behemoth) |
| Doublon d'entree journal `teammates.02` (2026-05-02) | merge voie B, 2026-08-11 | l'entree apparait 2x dans `.ai/thought_log.md` — dette PREEXISTANTE (presente des deux cotes avant le merge), pas introduite par l'union | lot hygiene de cloture v7.5 : dedupliquer (bash octets bruts) en meme temps que les autres nettoyages du journal |
| Icones kill feed : l'atlas du kill feed reste NON EXPOSABLE (etape 3 du plan icones) | lot icones, 2026-08-11 | deux liens possibles, les deux refutes. (1) `index.json` porte `tags_weap` sur les entrees `killfeed`, mais ce champ est indexe par `sprite index`, qui ne vaut que pour les atlas d'armes : l'index 0 du kill feed est le Battle Rifle la ou le contour 0 est le fusil d'assaut — le lire donnerait une icone fausse pour CHAQUE arme. (2) le nom interne ne designe jamais l'arme (`heatwave` = Cremateur, `plasma_blaster` = Fusil traqueur, `shade_turret` = un bidon) | rouvrir seulement apres avoir MESURE un lien arme -> index kill feed, comme `sprite index` l'a ete pour l'atlas d'armes. En attendant, la surface produit sert l'atlas `contour`, dont le lien est lu dans le jeu |
| Teinte d'equipe au kill feed : le kill feed ne porte AUCUNE arme (etape 4 du plan icones) | lot icones, 2026-08-11 | le composant d'icone teintee est livre (`components/ui/WeaponIcon.tsx`, masque CSS + token, teste), mais le kill feed de la Match View n'a pas d'arme a teindre : `domain.EventRaw` n'a pas de champ arme, `highlight_events` pas de colonne `weapon_id`, et la seule source de degat par kill (`match_kill_events.source_tag`) est un tag **`jpt!`**, PAS un tag `weap` — 11 lignes sur 114 de classe ARME portent un `[effet weap ...]` exploitable dans `damagetag/labels.tsv` | prealable AVANT toute reprise : soit un chemin MESURE `jpt! -> weap` (a la maniere du dead-state), soit un `weapon_id` par kill dans le contrat de la Match View. Sans l'un des deux, poser une icone sur un kill = inventer le lien |
| `index.json` publie un `tags_weap` FAUX sur les entrees `killfeed` | lot icones, 2026-08-11 | `weapon-icons-build/main.go:220` ecrit `tagsParIndex[idx]` pour les TROIS styles alors que `weaptags.go` ne le calcule que depuis le `sprite index` des atlas d'armes. Le consommateur s'en protege (filtre sur le style `contour`, verrouille par `TestSeulLAtlasContourEstLu`), mais l'artefact versionne porte un champ trompeur | corriger `weapon-icons-build` PUIS regenerer `index.json` — donc sur une machine avec Halo Infinite installe. A grouper avec la prochaine re-extraction d'icones |
| ~~Revue visuelle des nouvelles icones d'armes~~ **PASSEE le 2026-08-11** | lot icones, 2026-08-11 | — | **CLOS** : gate tenu par l'utilisateur sur l'artefact arme par arme (42 etiquettes, avant/apres, bascule de theme clair/sombre et de teinte) — verdict « tout est parfait ». Le rendu `currentColor` des grandes images est donc VALIDE ; il n'y a plus d'arbitrage ouvert sur la lisibilite |
| **[FIN DE v7.5, avec cartes v2] Armes qui partagent une icone : ne garder que la variante de base** | decision user, 2026-08-11 | 5 index d'atlas servent 13 etiquettes. **Le jeu LUI-MEME ne les distingue pas** : leur `sprite index` est identique (index 16 va meme plus loin — un SEUL tag `weap` pour 3 etiquettes produit). Chercher une image specifique dans cet atlas est donc sans objet. Mesure : #14 Energy Sword / Duelist / Elite Bloodblade / Infected (4) · #16 Gravity Hammer / Diminisher of Hope / Rushdown Hammer (3) · #23 Shock Rifle / Shock Rifle (Ranked) (2) · #33 M392 Bandit / Bandit Evo (2) · #35 Sandwich / Mythic Sandwich (2) | lot de fin de v7.5 : chercher une image specifique AILLEURS que dans l'atlas d'armes (skins/variantes ont peut-etre leur propre `bitm`) ; a defaut, **ne presenter que la variante de BASE** sur les surfaces de catalogue (tiroir d'assets) plutot que N lignes a l'icone identique. Arbitrage user au vu des trouvailles |
| **[FIN DE v7.5] Grenades : verifier qu'il n'existe VRAIMENT aucune image** | decision user, 2026-08-11 | dernier trou consequent : 5 etiquettes sur 42 (sentinelle grenade + Frag/Plasma/Dynamo + sentinelle melee) sont servies par les 2 PNG dessines a la main, faute d'entree dans l'atlas d'armes. Etabli a ce stade : les grenades ne sont pas des `weap` (elles vivent en `eqip`+`proj`, declarees par le tag de globals `gggl`) et n'ont donc pas de bloc « UI display info » a lire — le balayage de TOUS les groupes de tags ne les fait pas sortir | lot de fin de v7.5 : chercher l'icone la ou l'objet est declare (`eqip`/`proj`/`gggl`), et non dans le `weap`. L'atlas du kill feed porte des grenades (indices 46-51 par famille) mais reste non keyable. Arbitrer selon les trouvailles : si rien, les 2 PNG a la main restent le choix assume |
| **[CONTRAINTE] La teinte alliee/ennemie ne concerne QUE les icones du kill feed** | decision user, 2026-08-11 | precision donnee au compte rendu du lot icones : les « grandes » images (atlas `contour`, listes d'armes, tiroir d'assets) ne prennent PAS de couleur d'equipe. Etat livre CONFORME : aucune couleur d'equipe n'est appliquee nulle part ; les grandes images sont rendues en masque `currentColor` pour la seule raison qu'elles sont blanches a 100 % et seraient invisibles en theme clair | quand la teinte d'equipe arrivera (kill feed, une fois l'arme par kill disponible) : passer le token `team-ally`/`team-enemy` a `WeaponIcon` UNIQUEMENT sur cette surface. A confirmer au gate visuel : le rendu `currentColor` des grandes images convient-il, ou faut-il une autre solution de lisibilite ? |
| ~~Icones kill feed : l'atlas du kill feed reste NON EXPOSABLE (etape 3 du plan icones)~~ **CLOS le 2026-08-11 (lot arme du kill)** | lot icones, 2026-08-11 | les deux liens refutes l'etaient a bon droit — MAIS un TROISIEME existait, deja versionne et jamais consulte : la passe humaine `.ai/V7.5/icones/NOMMAGE_GATE_2026-08-09.tsv`, qui donne `index kill feed -> weapon_key` pour 26 vignettes et un libelle humain pour les autres. Elle CORRIGE le piege des noms internes (index 22, nomme `heatwave` dans le jeu, porte `hinf_cindershot`), et `config/titles/halo_infinite/mappings/weapon_names.toml` la corrobore independamment (Cremateur = Cindershot, Calcineur = Heatwave) | CLOS. Le pont vit dans `internal/games/halo_infinite/film/killicon` (36 regles versionnees). Le garde-rail `TestChaqueRegleEstCorroboreeParLeRegistre` rejoue la corroboration a chaque CI |
| ~~Teinte d'equipe au kill feed : le kill feed ne porte AUCUNE arme (etape 4 du plan icones)~~ **CLOS le 2026-08-11** | lot icones, 2026-08-11 | le prealable exige etait « un chemin MESURE `jpt! -> weap` ». Il n'en fallait pas : `jpt! -> NOM` est deja resolu a 97,6 % par `damagetag`, et le nom suffit a designer une vignette. Le detour par le tag `weap` etait une contrainte que la mesure ne demandait pas (11/114 lignes seulement le portent) | CLOS. `MatchHighlightEvent` porte `weapon_key`/`weapon_label`/`weapon_image_url`/`weapon_image_tinted` + `actor_team_id` ; le feed est rendu en DOM (`MatchKillFeed.tsx`), icone teintee a la couleur d'identite de l'equipe du tueur |
| **La donnee de l'arme du kill est EN RETARD : 0 a 5 % de couverture sur les matchs d'avril a juillet 2026** | lot arme du kill, 2026-08-11 | mesure sur la base de production, 152 009 kills du feed : 34,3 % recoivent une icone. Repartition des 65,7 % restants — 47,1 % ont une passe de film NON publiable ligne par ligne (BTB) ou sans source mesuree, 18,5 % n'ont AUCUNE ligne dans `match_kill_events`. Par mois : 55 a 72 % jusqu'a mars 2026, puis 5,5 % en avril, 0 % en mai et juin, 4,4 % en juillet — les matchs les plus recents n'ont pas une seule ligne. **Ce n'est pas un defaut du pont** : c'est `backfill-killsource` qui n'a pas ete rejoue | rejouer le backfill de la source de degat sur les matchs recents — operation de production, HORS PERIMETRE de ce lot (et hors des interdits qui lui etaient poses). A arbitrer avec l'utilisateur : sans ce rejeu, la feature est invisible sur les matchs qu'il regarde le plus |
| Le nom du tueur n'apparait qu'au SURVOL dans le fil des frags | lot arme du kill, 2026-08-11 | choix de densite : une lane porte jusqu'a ~80 kills sur ~700 px ; N etiquettes permanentes se chevaucheraient et rendraient le fil illisible. Le nom est colore a la couleur d'equipe des qu'il apparait, et l'attribut `title` porte « tueur — arme — instant » pour le clavier et le lecteur d'ecran | a re-arbitrer au GATE VISUEL si l'utilisateur attend les noms en permanence. Alternative connue : passer le fil en liste de puces chronologiques (chaque entree `[icone] Nom`), au prix de l'alignement sur l'axe temporel du graphe |
| ~~« Les kills par vehicule n'ont pas de chassis identifiable »~~ **AFFIRMATION FAUSSE, corrigee le 2026-08-11** | lot arme du kill, 2026-08-11 | l'executeur avait ecarte les 89 lignes de classe VEHICULE au motif que « labels.tsv ne nomme pas le chassis ». Litteralement vrai (pas de colonne `nom`), mais la conclusion sautait une etape : **78 lignes sur 89 citent la racine de banque sonore du chassis** dans leur champ `detail` (`sb_010_veh_cv_ghost`, `sb_010_tur_un_machinegun`...), exactement comme les grenades citent leur entree `gggl` — mecanisme que le meme lot exploitait deja pour les grenades. Signale par l'utilisateur (« ca avait ete verifie et confirme ») | **CORRIGE** : genre de regle `BANQUE` (13 regles), applique UNIQUEMENT quand la ligne cite UNE racine et une seule. 36 lignes couvertes sur 89, **472 des 574 kills de vehicule (82,2 %)**. Couverture globale des sources connues : 90,7 % -> **98,0 %**. Garde-rail neuf : `TestUnEffetPartageParPlusieursChassisNObtientAucuneIcone` |
| Vehicules : 53 lignes restent sans icone, et c'est mesure | lot arme du kill, 2026-08-11 | un meme tag cite plusieurs chassis quand l'effet leur est PARTAGE (`vehi 0000d4ff` cite Scorpion, Chopper, Banshee, Wasp et tourelle Shade). Deux exclusions ciblees en plus : `tur_cv_shadeturret` (deux vignettes candidates, index 39 « Banished Shade Turret » et 40 « Shade turret ») et `tur_bt_gatlingmortar` (CONFLIT : nom interne `gatling_mortar` contre libelle humain « Scrap cannon » — l'humain fait foi, cf. Cindershot) | rouvrable si un besoin apparait : departager la tourelle Shade demande une source de plus (le gate visuel utilisateur peut trancher entre les vignettes 39 et 40 en les regardant). Le mortier gatling demande de re-arbitrer le libelle humain de l'index 14 |
| ~~Tourelle Shade : deux vignettes candidates~~ **TRANCHE PAR L'UTILISATEUR le 2026-08-11** | lot arme du kill, 2026-08-11 | l'executeur avait presente ce point comme un arbitrage a rendre alors que la passe humaine l'avait DEJA tranche et ecrit : `NOMMAGE_GATE_2026-08-09.tsv` nomme l'index 39 << Banished Shade Turret >> et l'index 40 << Shade turret >>. Rappel de l'utilisateur : « la banished c'est la 39 » | **CLOS** : regle `BANQUE tur_cv_shadeturret -> killfeed-39` (toutes les racines `cv` d'Infinite designent des engins Banished : ghost, banshee, wraith, phantom). +10 lignes VEHICULE couvertes (46/89), +2 kills. **Lecon : relire le TSV avant de reposer une question a l'utilisateur** |
| ~~Aucun consommateur ne lit encore `map_backgrounds/`~~ **CLOS le 2026-08-12 (piste F)** | piste F, 2026-08-12 | le fond de carte est servi par l'API (`GET .../replay/background` pour le calage, `.../replay/background.png` pour l'image, tous deux derriere le garde local du rejeu) et dessine SOUS le rejeu, a la place du sol reconstruit. Chaine de resolution `match -> nom de carte -> module` fondee sur le catalogue de bornes existant, sans seconde regle de nommage | CLOS. Oracle `TestMapBackground_TousLesModulesDuCatalogue` : 15/15 cartes du catalogue livrent calage et image |
| ~~6 fonds de carte cuits sont INATTEIGNABLES par un rejeu~~ **SORTI (lot bornes, 2026-08-13)** | piste F, 2026-08-12 | — | **CLOS** : les 6 cartes (Deadlock 93 matchs, Oasis 67, Scarr 57, Recharge 55, Prism 48, Corpo 2) sont au catalogue de bornes, chacune etablie par la methode level_id (unicite 1/1 sur les 64 modules de `any/levels` + `ds/levels`), preuve MATERIALISEE en test durable `TestPreuveLevelIDCartes` (`internal/himap/sonde_levelid_gamefiles_test.go`). Les 15 entrees preexistantes sont bit-identiques au diff. Preuve de service bout-en-bout : `GET .../replay/background` passe de 404 a 200 sur les 6 cartes (serveur local). Commit du lot sur `feat/v75` |
| Le fond de carte est REFUSE s'il ne recouvre pas la zone jouee | piste F, 2026-08-12 | `coversPlayedArea` ecarte un fond dont le cadre ne contient pas les bornes du rejeu : un fond qui ne contient pas le terrain signale que les deux reperes ne sont pas le meme (mauvaise carte, ou bornes de dequantification d'une autre carte), et poser les joueurs a cote de leur terrain serait pire que pas de fond. AUCUN cas connu aujourd'hui (le seul artefact disponible passe) | si un fond disparait a l'ecran alors que la carte en a un : c'est ce garde qui a parle, et il faut comparer les bornes de l'artefact au cadre du sidecar AVANT de toucher au rendu |
| Le worktree `LevelUp-wt-replay2d` n'a PAS les bases de donnees | piste F, 2026-08-12 | son `shared_matches_v2.duckdb` fait 12 Kio (artefact de test). Or le kill feed du rejeu depend de la Match View, donc de la base — un serveur lance depuis le worktree affichera le rejeu sans kill feed et sans fond (la carte du match ne peut pas etre nommee) | pour tout gate visuel sur cette branche : lancer le serveur en pointant la racine de donnees du depot principal (`LEVELUP_REPO_ROOT`), ou depuis le depot principal une fois la branche fusionnee. A dire AVANT de demander un verdict visuel |
| `PalmaresRelationsPage.test.tsx` instable sous charge | piste F, 2026-08-12 | le test « badge cross-jeu » expire a 5 s quand la suite complete tourne (406 fichiers), et passe en 5 s isole. Sans rapport avec la piste F | si l'instabilite se reproduit en CI : relever le `testTimeout` de ce fichier ou alleger son rendu. Ne pas le desactiver |
| **[POST-v7.5 / ACTIVATION PROD DU REJEU] Delocalisation du traitement rejeu 2D (ouvrier local OU 2e VPS) + monitoring des jobs de bout en bout** | plan master (decision #5, §1/§2bis/§4bis), rappele par l'utilisateur 2026-08-12 | le rejeu est OFF en prod en v7.5 (garde local) : tant qu'il n'est pas active en prod, personne ne produit d'artefacts cote serveur, donc l'ouvrier + le monitoring des jobs ne sont PAS requis. C'est un chantier d'infrastructure d'ACTIVATION, hors du perimetre local v7.5. L'essentiel de la plomberie EXISTE DEJA (contrat `AsyncJobStatus` dans `internal/domain/job.go` ; `JobStore` ring memoire ; route `GET /admin/monitoring/jobs` ; dashboard admin sync/logs/system ; scheduler ADR 0027). Ce qui MANQUE = un type de job « construire un rejeu », une FILE PERSISTANTE (le ring memoire perd les jobs au redemarrage), un HEARTBEAT d'ouvrier. L'ouvrier est un ROLE (poste, 2e VPS, ou rien) : le VPS web ne decode jamais, il met en file et sert ; l'ouvrier tire le travail en HTTPS, sans secret Halo (morceaux via CDN pre-signe), sans base -> l'ETAT vit cote web, donc le monitoring voit le distant. Artefact reproductible a l'octet (J3.5) -> ou on le construit = question de cout, pas de justesse | APRES le tag v7.5, GATE sur la decision d'activation du rejeu en prod (retrait du garde local ; seuil re-statue a l'hypercare, cf. ligne « Seuil d'activation du rejeu public = 88 % » ci-dessus). Point d'entree : `.ai/PLAN_MASTER_FILM_KILLFEED_REJEU.md` decision #5 (L1027) + §1 ouvrier externe (L1573), §2bis jobs+monitoring dans l'app (L1661), §4bis monitoring distant/heartbeat (L1729). RE-VERIFIER sur pieces l'etat des briques existantes (`domain/job.go`, `/admin/monitoring/jobs`) AVANT de scoper — docs .ai potentiellement perimes |
| **[POST-v7.5] Calque drops / armes au sol du rejeu 2D (Phase 2 des fiches enrichies)** | fiches enrichies rejeu 2D, Phase 2.1, 2026-08-12 | **decision utilisateur : report, 2.2/2.3 ANNULEES.** La POSITION des armes au sol (`ti=42`) est INDECODABLE offline-pur, mesure sur `000d5950` : temoin FANTOME de meme cardinalite (899 slots jamais vus en image-cle) rend 493 slots peuples / 10 950 echantillons contre 661 / 44 498 pour la vraie bande — la presence d'une position n'informe presque rien ; discriminant spatial (une arme posee ne bouge pas) : 3,3 % seulement des 458 slots reels a >=3 echantillons tiennent dans 0,5 u, 62,4 % s'etalent au-dela de 20 u (fantome 0,5 % / 82,4 %). Confirme le verdict du 2026-07-28 (`.ai/SUIVI_REPLAY_2D.md`, « objets au sol — bloque, et proprement ») que le plan de Phase 2 avait contredit. Causes structurelles : au keyframe l'offset d'i0 suit le default-state de l'archetype, absent pour ti=42 (`defaultStateDeserByTI`) ; sur la voie delta le record ne porte aucun typeIndex et la bande de slots comblee est contaminee. **L'IDENTITE, elle, est ACQUISE et livree** : 196 lectures portant une famille connue, 22 armes nommees, 169 vies (slot,gen), 0 fuite d'alias sur 172 paires (`filmdec/keyframe_ground_weapons.go`) | condition de reprise : **default-state de `ti=42` resolu** (une entree dans `defaultStateDeserByTI`, ce qui donne l'offset d'i0 dans le record de keyframe) **OU** evenements de cycle de vie d'entite decodes offline (piste Phase 3, cf. @acurtis166). Reprise possible SANS l'un ni l'autre sous une forme **NON SPATIALE** : publier la liste « quelles armes gisent au sol a l'instant lu » (sans dire ou), cadence ~20 s avec age — option produit a arbitrer, le decodeur d'identite est deja la. Refutation reproductible : `GW_FILM=<film> go test ./internal/analysis/replay/ -run GroundWeapon -v` |
| Affinements des fiches joueur du rejeu 2D (liste non fournie) | gate fiches, 2026-08-12 | gate visuel VALIDE « a ce stade » par l'utilisateur ; il signale « des choses a affiner » sans les nommer — on ne code pas contre une liste inconnue | retour utilisateur post-v7.5 avec la liste des affinements |
| Live Fire : module PROUVE, bornes INDISPONIBLES | lot bornes, 2026-08-13 | le lien Live Fire -> `sgh_interlock` est etabli par level_id (1253388187 / 0x4AB52F9B, unicite 1/1, rejoue par `TestPreuveLevelIDCartes`), mais le module ds ne porte AUCUN tag sbsp (`himap.ErrAucunTagSbsp`, meme exception que la cuisson — handoff cartes §1 ter) : la source declaree des bornes (`world bounds` du sbsp) n'existe pas pour cette carte. 68 matchs restent sans dequantification ni fond ; `cmd/replay-build` refuse, c'est le comportement voulu (pas de coordonnee devinee) | trouver ou vivent les bornes monde de `sgh_interlock` (sa geometrie n'est pas la ou celle des 26 autres cartes se trouve : autre groupe de tags a instruire), OU les capturer en live comme Cliffhanger (`DAT_14462cbe0` sur un film Live Fire, preuve externe deja admise par la doctrine de `cmd/mapquant-build`) |
| Variantes « Sentry Defense » / « Firefight » non gerees par `NormalizeMapName` | lot bornes, 2026-08-13 | `map_quant_bounds.json` ne resout pas `Oasis Firefight` (1 match), `Oasis Sentry Defense` (1) ni `Highpower Sentry Defense` (1) : seuls les suffixes « - Ranked » et « Heavies » sont retires, et la regle exige une PREUVE meme-level_id avant d'ajouter un suffixe (cf. commentaire `variantSuffixes`). `Vallaheim Firefight` (2 matchs) resterait de toute facon hors catalogue (carte Forge non cataloguee) | rejouer la preuve Heavies du 2026-08-08 : lire le level_id de `oasis_firefight_map.mvar` / `oasis_sentry_defense_map.mvar` / `highpower_sentry_defense_map.mvar` dans le depot `.ai/re_dump/mapvar` et, s'il egale celui de la base, ajouter les suffixes a `variantSuffixes` avec un cas dans `TestNormalizeMapName` (mutation jouee : casser, rouge, revert). 3 matchs regagnes |
| GATE VISUEL utilisateur du lot TOITS (9 cartes re-cuites) | lot toits, 2026-08-13 | la session ne peut pas juger son propre rendu — regle du chantier. Le lot a re-cuit les 9 cartes COUVERTES (part de matiere cachant un sol praticable > 1/3, mesure `INVESTIGATION_TOITS_2026-08-13.md`) : les 5 visees (ctf_illusion 38,5 %, sgh_crystalcaves 61,2 %, ctf_aquarius 66,7 %, ctf_forbidden 35,1 %, chasm 37,2 %) ET 4 detectees par la regle SANS verdict utilisateur prealable : btb_engine 46,1 %, va_launchsite 35,9 %, fo08_wetland 58,5 % (Vagabond, VALIDEE au lot A — a re-juger en priorite), fo11_blank 45,4 % (Corpo). ridgeline/catalyst/va_behemoth IDENTIQUES AU BIT | soumettre les 12 paires avant/apres de `C:/Users/Guillaume/Desktop/gate_cartes_v75/toits/` ; si une carte re-cuite est jugee moins bonne, arbitrage : relever `SeuilCarteCouverte` (btb_engine et launchsite sortent a 0,50) n'affecte PAS les 5 visees ; un retour carte par carte n'est pas prevu par la regle (aucun reglage par carte) |
| Seuil `SeuilCarteCouverte = 1/3` valide sur les 8 cartes JUGEES seulement | lot toits, 2026-08-13 | la separation mesuree (validees <= 28,4 %, defectueuses >= 35,1 %) repose sur les 8 cartes ayant un verdict utilisateur ; les 13 autres tombent ou elles tombent (4 basculent couvertes). La marge est de 4,9 points sous le seuil et 1,8 au-dessus | si le gate visuel du lot toits contredit une carte : re-mesurer la marge avec le verdict ajoute ; le tableau des parts est publie dans chaque sidecar (`coveredShare`) |
| Fonds Forge : reliquat <9 matchs non cuit (lot fonds par map_id) | fonds map_id, 2026-08-13 | 37 fonds Forge publies sous cle map_id (2 migres + 2 pilotes + 33 de masse, 0 echec, rapport RAPPORT_FONDS_MAP_ID_2026-08-13.md) ; restent les cartes Forge jouees <9 matchs avec `.mvar` au depot : Ecotone (8), Insolence (7), Solution (7), Flood Gulch (6), Threshold (5), Solitude - Ranked (5), Thunderhead (4), Pharaoh (3), Credence (2), Disciple (2), Merchant's Square (2), Urban Raid (2), et <=1 match (Nadair, Outlook, Rat's Nest, Ronin, Scarlett's Landing, Warehouse, Dawnbreaker, Cole Protocol, Lattice - Ranked, 944396dd) | la MEME chaine s'applique telle quelle, carte par carte : etendre `preuvesLevelID` (sonde) + `mapModule` (mapquant-build), `mapobj-build --from-file .ai/re_dump/mapvar/<carte>_map.mvar --map-id <uuid>`, declaration `CartesForge`, `mapfond-build --natives=false --maps <uuid>` ; map_id et comptes au rapport |
| Assets UGC SECONDAIRES d'une carte Forge deja cuite | fonds map_id, 2026-08-13 | Dynasty 90cd321d, Houseki 6439625e, Shiro 2962c4e0, Shogun 8f51ccb9, Salvation f633db01, Starboard 50771a22 (1 a 3 matchs chacun) : meme nom de carte, autre asset — le depot ne porte qu'UN `.mvar` par carte, on ne sait pas laquelle des deux versions il decrit. Leur fond rend l'absence propre (jamais la carte d'un autre) ; les bornes, elles, se resolvent par NOM et marchent deja | si un de ces assets devient joue : re-telecharger son `.mvar` par map_id (`mapobj-build --map-id <uuid> --save-mvar`, reseau) puis meme chaine ; ne JAMAIS reutiliser le `.mvar` de l'autre version sans preuve objects_n |
| ~~GATE VISUEL utilisateur du lot TOITS~~ | lot toits, 2026-08-13 | — | **VERDICT RENDU (user, 2026-08-13)** : « excellent travail », lot VALIDE — dont btb_engine, va_launchsite et fo11_blank (les 3 basculees sans verdict prealable). DEUX exceptions a revoir, voir la ligne suivante |
| Vagabond (fo08_wetland) et Catalyst a revoir — « cas particuliers » (user 13/08) | gate toits, 2026-08-13 | Vagabond : re-cuite par la regle 1/3 a 58,5 % de matiere couverte, jugee a revoir ; cran disponible = relever `SeuilCarteCouverte` a 0,50 (la sortirait AVEC btb_engine et va_launchsite, pourtant validees — un cran ne suffit donc PAS, il faudra un critere plus fin ou une exception en DONNEE). Catalyst : PNG INTACT AU BIT par le lot toits — le grief est ANTERIEUR (ligne « +11,5 % de matiere » de la bascule de tranche du 10/08, deja au registre) | decision user : « on y reviendra ». Reprendre les deux ensemble : re-mesurer Vagabond avec `coveredShare` du sidecar + rejouer la tranche de Catalyst sur artefact visuel avant/apres ; aucun reglage par carte dans le code, exception declaree en donnee si necessaire |
| **GATE VISUEL lot map_id : MASSE REFUSEE (user 13/08) — BLOQUANT AVANT TAG** | fonds map_id, 2026-08-13 | verdict utilisateur sur les 35 PNG : pilotes Starboard/Dredge « plutot ok » mais « on voit encore trop les plafonds et toits » ; masse 33 cartes « aucune n'est exploitable — bouillie au milieu, forme patatoide gribouillee » (Goliath tolerable a la rigueur, Domicile trop de toits). L'oracle des ancres (438/441) n'a rien vu — il ne juge PAS l'aspect (piege deja au registre). Hypothese de tete a MESURER : cartes communautaires = boites FERMEES (coque de blocs dans le vide) -> le z-buffer rend la coquille ; la regle des toits (seuil 1/3, reference par ancres) ne mord pas assez sur ces cartes | investigation lancee 13/08 (diagnostic quantitatif validees-vs-refusees + prototypage de regle sur TEMOINS : Domicile, Goliath, 2-3 patatoides, Starboard, Vagabond en garde) ; les 33 fonds RESTENT publies sur feat/v75 (pas en prod) mais NE PASSENT PAS le tag en l'etat : re-cuisson apres regle corrigee + nouveau gate, ou retrait |
| ~~Equipement actif : i54 « mobility-action » = evenement DATE mais NON identifiable comme usage d'equipement~~ **TRANCHE le 2026-08-16 (phase D, PLAN_ETAT_ACTIF_EQUIPEMENT) : `i54` N'EST PAS un usage d'equipement** — croisement i54 x i48 PAR VIE sur les 12 films : 0,55 episode/vie (mobilite) contre 0,45 (autres rangs), ratio 1,2, autres > mobilite sur 4 films ; temoin `0014603f` (0 identite i48) porte quand meme 631 flag1==1. L'etat actif est trouve AILLEURS : camo = i28 queue[1] (phase A), surbouclier = i5 q>64 (phase B) | plan parite rejeu 2D, lot 1 item 1.4 `[!]`, 2026-08-13 | mesure offline sur 000d5950 (instrument `apps/go-api/internal/analysis/filmdec/i54_research_test.go`, garde `I54_FILM`, ~6 s) : 171 851 records delta biped, 2 819 portent i54 au masque, flag1==1 sur 2 753 ; **67 EPISODES discrets** (salves de ~36-40 records, duree quasi fixe ~0,6-0,7 s), 1 a 3 par vie sur 39/99 vies, et 3/67 seulement a moins de 2 s du spawn — signal d'EVENEMENT date en cours de vie, PAS du bruit de mouvement continu, PAS une action d'apparition. MAIS rien offline n'etablit que l'episode est un USAGE D'EQUIPEMENT (le corps d'i54 porte positions et vecteurs — compatible escalade/mantle autant que grappin), et AUCUNE identite d'equipement ne voyage avec (surbouclier / camouflage / translocateur indistinguables) — or la decision 3 du plan exige l'identite pour choisir le rendu. Verdict : INDECIDABLE offline-pur, pas d'effet simule (regle du plan) | reprise a l'une OU l'autre : (1) croiser i56 « spartan-ability-energy » (masque 3 bits + 7 bits par charge armee, aux images-cles) avec les episodes i54 — une chute de charge au voisinage d'un episode signerait l'usage et l'identite equipee se lit dans i48/i57 ; (2) verite terrain Theater sur un match RECENT date (doctrine du chantier) avec usages d'equipement notes. L'instrument re-derive les 67 episodes en une commande |
| Transport de l'ARTEFACT depuis l'ouvrier vers le web | lot 8 (file durable + protocole ouvrier), 2026-08-14 | hors du perimetre ferme du lot (4 items : file durable, protocole, battement, vue admin — le transport n'y figure pas). Aujourd'hui `POST /internal/build-queue/complete` rend un RESUME JSON (module, pistes, octets, chemin) ; les ~2 Mo d'artefact restent sur le disque de l'ouvrier. Tant que l'ouvrier est le poste, c'est sans consequence (meme disque que le cache de rejeu) ; des qu'il est une AUTRE machine, l'artefact n'arrive jamais au serveur qui doit le servir | lot suivant : route de depot d'artefact (multipart ou corps binaire) + ecriture par `PathResolver.ReplayArtifactPath`, avec verification de taille/forme avant d'ecraser un artefact existant. Prealable a tout ouvrier reellement distant |
| Branchement de la file au fil de l'eau post-sync | lot 8, 2026-08-14 | `internal/sync/replayartifacts` construit toujours EN LOCAL (etape conditionnee LOCAL, lot 6) ; rien ne met en file automatiquement. Seule la route admin `POST /admin/actions/replay-build/enqueue` alimente la file. Volontaire : basculer le fil de l'eau vers la file sans transport d'artefact produirait des jobs dont le resultat n'arrive nulle part | apres le transport d'artefact ci-dessus : remplacer l'appel local par une mise en file quand un ouvrier est configure, en gardant le chemin local quand il n'y en a pas (degradation deja ecrite) |
| `priority` de la file exposee mais jamais positionnee | lot 8, 2026-08-14 | la colonne existe et l'ordre de service la respecte (priorite decroissante puis anciennete), mais aucun appelant ne passe autre chose que 0. Le champ est la parce que l'ordre de service en depend et qu'il ne se rajoute pas apres coup dans une table append-only deployee | quand un rattrapage de masse coexistera avec des demandes admin unitaires : donner une priorite superieure aux secondes. Aucune migration necessaire |
| Resolution du manifeste par les tokens du premier joueur disponible | lot 8, 2026-08-14 | `EnqueueReplayBuild` passe par `haloTokensForDrain` (helper existant du drain catalogue), qui prend le premier joueur au token valide. Le manifeste de film n'est pas porte par un joueur, donc la resolution est correcte ; mais l'echec est celui du POOL, pas du match, et le message d'erreur ne le distingue pas | si un cas d'echec ambigu remonte : nommer le joueur dont le token a servi dans l'erreur de mise en file |
| Index de capacite **7** OBSERVE mais NON NOMME | plan capacites actives, item 2.1, 2026-08-14 | balayage de couverture (instrument versionne `apps/go-api/internal/analysis/replay/i48_ability_width_test.go`, garde `ABILITY_FILM_ROOT`) : sur 14 films du cache, 6 rendent des lectures, 742 au total. Cinq d'entre eux donnent le jeu connu {3,4,5,6} ; le film **`00ba2e1c`** donne l'index **7 sur ses 85 lectures, et LUI SEUL** — les huit joueurs y portent le meme equipement (mode a dotation unique). Couverture des noms : 657/742 = **88,5 %**. AUCUNE entree n'a ete ajoutee a `config/titles/halo_infinite/mappings/replay_labels.toml` : l'index est observe, il n'est pas identifie, et la regle du depot est qu'un nom approchant se lit comme une certitude. Il garde son numero a l'ecran | UNE observation suffit et elle est bon marche : ouvrir **`00ba2e1c` en Theater** et lire l'equipement de n'importe quel joueur. Le nom obtenu entre tel quel en `"7" = { en = ..., fr = ..., icon = "hud/..." }`. SECOND TEMOIN cherche exprès et trouve : `9fe88ec4` (BTB:Fiesta CTF sur Insolence, 2025-11-13) rend 7 sur ses 63 lectures, et lui seul ; un BTB non-Fiesta recent (`4f77afc1`, BTB:CTF sur Flood Gulch, 2026-07-24) rend {3,4}. Les deux porteurs de 7 sont des **BTB Fiesta**. Cibles Theater, par ordre de recence : `9fe88ec4-...` (13/11/2025) puis `00ba2e1c-1a70-4060-98fc-f29d53d402c0` (BTB:Fiesta Slayer sur Obituary, 25/07/2025). CAVEAT de la doctrine du chantier : Theater exige un match RECENT — si ces deux films n en sont plus, jouer un BTB Fiesta et relancer l instrument dessus. Aucun code a changer — la table est deja une donnee de titre |
| Surbouclier et camouflage TOUJOURS absents de la table | plan capacites actives, item 2.3, 2026-08-14 | les deux equipements que la maquette veut mettre en scene (fiche doree, effet de verre) n'apparaissent sur AUCUN des 15 films mesures : les index observes sont {3,4,5,6} et {7}. Sans index observe, il n'y a rien a nommer — et sans nom, l'etape 4 ne peut pas choisir un rendu plutot qu'un autre | releve Theater de l'utilisateur sur un match qui les contient (ils sont des ramassages de carte, pas une dotation : un film d'arene ou quelqu'un prend le surbouclier suffit), OU balayage de couverture elargi une fois le cout du balayage regle (voir la decouverte « ne passe pas a l'echelle » du plan) |
| **Etat actif d'une capacite : la reprise n°1 (croisement `i56`) est EPUISEE, mesuree** | plan capacites actives, item 3.3, 2026-08-14 | instrument versionne `apps/go-api/internal/analysis/filmdec/i56_energy_test.go` (garde `I56_FILM`, 8 s). Critere pose AVANT la mesure : le taux de coincidence episode `i54` / chute d'energie `i56` du meme slot (±1 s) doit ECRASER les memes episodes decales de ±5 s. **Resultat : reel 3/67 = 4,5 % · temoin +5 s 0,0 % · temoin -5 s 3,0 % — le reel n'ecrase rien, la correlation N'EST PAS ETABLIE.** Cause mesuree, et elle n'est pas seulement l'absence de relation : `i56` n'est transmis que **176 fois sur 171 851 records delta (0,10 %)**, sur 78 slots, d'ou **28 chutes lisibles pour 67 episodes** — une relation parfaite plafonnerait a 41,8 %. Le detecteur de records ne voit que les masques explicites de <= 7 composants, donc 176 est un PLANCHER. ACQUIS au passage, par une voie independante du decompile : les 176 valeurs sont toutes multiples de 16 (quartet bas nul, quartet haut 0 a 4) et les 28 chutes portent toutes sur le quartet haut — l'encodage discret de RECETTE_LOADOUT §9 est CONFIRME, `i56` est bien un compteur de charges. Ce qui manque est sa frequence, pas sa semantique | deux voies, dans cet ordre de cout : (1) rendre `i56` lisible sur les records a masque DENSE (> 7 composants), ce qui leverait le plancher de 176 — prealable a toute nouvelle tentative de correlation ; (2) verite terrain Theater sur un match RECENT et date, avec les usages d'equipement notes a la seconde (doctrine du chantier). La voie `i57` reste morte : bit 0 = 1 sur 386/386 |
| Effets plein-fiche (dore / verre / bordure animee) — **CAMOUFLAGE et SURBOUCLIER SORTIS le 2026-08-16, TRANSLOCATEUR `[!]`** | plan capacites actives, etape 4, 2026-08-14 | le GATE 3 interdisait l'etape 4 depuis le 2026-07-31 (« rien de tout ceci ne s'affiche avant que l'etape 3 soit passee »). Double blocage : pas de source d'ETAT ACTIF mesuree, et pas d'IDENTITE non plus. **LEVE POUR DEUX FAMILLES SUR TROIS** (lot effets-fiches, `wt/effets-fiches` 46f99475a, fusionne dans feat/v75 le 2026-08-16) : le camouflage rend la fiche VITREUSE (`backdrop-filter: blur(6px)` + voile `color-mix(var(--foreground) 12%, var(--card))`) et le surbouclier l'ENCADRE D'OR (cadre inset 2px au nouveau token semantique `legendary`, 4 palettes + fallback CSS) — conforme au cahier des charges Notion 21.1, en remplacement de l'opacite 0,4 et de l'anneau au token `info`. Effets statiques, `prefers-reduced-motion` respecte par construction, composables. **CONSTAT DE REVUE DE FUSION, NON TRAITE** : les deux tokens du `color-mix` etant OPAQUES, le voile se peint par-dessus le `backdrop-filter` — le flou est un no-op mesure, l'effet « verre » visible est le VOILE seul ; ni sa magnitude ni son sens ne sont contraints par un test (`CAMO_GLASS_VEIL_PCT` a 0 ou 100 passerait). C'est une decision de RENDU, elle appartient au gate visuel. Le TRANSLOCATEUR reste `[!]` non code : aucun canal d'etat mesure pour le rang 11 (3 lectures i48 d'IDENTITE seulement dans le corpus, sur 06dfe6d9) — alimenter un effet par une source non mesuree reste l'interdit du chantier | **Camo/surbouclier : reste le GATE VISUEL utilisateur** (temoins 084a804d equipement, 000d5950 dash-camo), qui tranchera aussi le constat verre/voile ci-dessus. **Translocateur : un canal d'ETAT rang 11 mesure.** Piste ouverte, spec Notion 21.2 — « i57 porte la direction de la capacite (cubemap 24 bits) » : le R(24) de la branche v==1 (valeurs quasi uniques, REFUTE comme handle le 2026-08-16) pourrait coder une direction quantifiee, meme famille que l'aim_vector 30 bits des fire events. NON INSTRUMENTE a ce jour |
| **Le calque d'objectifs du rejeu 2D EN PRODUCTION est decale de `originMs` (3,6 s a 50,8 s)** | lot containment zones, decouverte 2026-08-14 | meme defaut que celui etabli a l'etape 1 du plan containment, mais du cote SERVI : `buildObjectiveActions` (`internal/analysis/replay/objectives.go`) pose `t = TimeMS / interval` alors que `TimeMS` est date depuis le PREMIER PAQUET DU FILM et que la grille de frames compte depuis le PREMIER PAQUET DE POSITION ; l'ecart entre les deux zeros est exactement `doc.originMs`. Le client ne compense PAS : `buildObjectivePulses` (`apps/web/src/features/match-replay/objectivesLayer.ts`) consomme `a.t` tel quel, seul `killFeedLogic.ts` applique `originMs`. Consequence visible : les pulses d'action d'objectif s'allument 3,6 a 50,8 s trop tard SELON LE MATCH, et sont poses sur l'element le plus proche du joueur au MAUVAIS instant — donc frequemment sur la mauvaise zone. Mesure de l'ampleur : l'appartenance stricte passe de 9,9 % a 40,9 % quand la correction est appliquee. NON CORRIGE : hors du perimetre ferme du plan containment (regle « zero fix opportuniste ») | correction a un seul endroit — retrancher l'origine dans `buildObjectiveActions`, qui doit donc la recevoir. ATTENTION, la correction touche un document SERVI : `go test ./contracttest/` OBLIGATOIRE (compte gele des champs de `ReplayDocument`), et verifier qu'aucun consommateur web ne compense deja (aujourd'hui aucun ne le fait). Repartir des evenements identifies et non des actions deja posees : `buildObjectiveActions` jette ce qui depasse la derniere frame, c'est-a-dire les actions de fin de match que la correction ramene dans la fenetre (+11 actions sur 525 au corpus d'origine). Recette et mesure : `cmd/zone-attribution/measure.go`, fonction `correctedActions` |
| ~~11 films sur 31 : ecart vertical de ~1 270 m entre positions du film et zones du catalogue~~ | lot containment zones, decouverte 2026-08-14 | — | **SORTI (PLAN_BORNES_CANEVAS_FORGE phase 3, 2026-08-16)** : cause trouvee et corrigee — le catalogue de bornes servait l'AABB du DECOR LOINTAIN des canevas Forge au lieu de celle de l'ARENE (`mapquant-build` retenait le plus gros tag sbsp ; le critere est desormais la REGION 0 du tag de niveau, `himap.BSPQuantification`). Re-mesure sur le film meme du report, `go run ./cmd/zone-attribution -match 1b1e380f` (Solitude) : ecart vertical median **+1 240 a +1 300 m -> +0,1 m** (p25 +0,0 · p75 +1,2), distance mediane au volume **1 279-1 438 m -> 1,0 m** (p75 4,5 · max 15,1), appartenance **0,0 % -> 39,0 % a 0 m** (59,3 % a 2 m · 76,3 % a 5 m · 100 % a 20 m), `zone_captures` 21/70 et `zone_secures` 2/14, concordance inter-joueurs 19/19 groupes a 100,0 % contre 66,7 % pour le temoin decale. Les volumes viennent de `map_objectives.json` (le `.mvar`), source independante des bornes : le controle n'est pas circulaire. **Le containment est RE-OUVRABLE sur ces 11 films** — leurs 675 actions (36 % du corpus mesure) redeviennent exploitables une fois les artefacts re-cuits |
| Containment : 16 des 48 films mesurables non mesures | lot containment zones, 2026-08-14 | mesure jouee sur 32 films ; les 16 restants ne peuvent PAS renverser le gate de persistance, et c'est demontre plutot que suppose : a 760 attributions sur 1 860 actions, atteindre 80 % exigerait 3 640 actions supplementaires TOUTES attribuees, soit 227 actions/film sur 16 films quand le maximum observe est 110 et la moyenne 60. Borne haute realiste (16 films a 100 %) : **61,0 %**. S'ajoute la contrainte machine : le poste de l'utilisateur a redemarre trois fois pendant le lot. Cout mesure pour information : **105 s et 216 Mo de pic par film**, un process a la fois (l'outil n'est donc PAS la bombe RAM du balayage corpus — il decode un film par process) | si le containment est repris : `for m in <16 ids>; do go run ./cmd/zone-attribution -match $m; done`, un film a la fois, ~28 min au total. Films restants : a349fea8 6bc544a1 47149c49 acf6ad8b d39926cb 2b2c5aa3 b77dffd4 83fe35b3 dec3e8e6 ff4f7e7b 1e26f641 7ddb5c8d d4230304 bdd4b8f5 353367d6 696a9d7c. N'a d'interet QU'APRES la correction du defaut de repere ci-dessus — sinon la moitie du complement tombera a 0 % pour la meme raison |
| Containment des zones : PAS DE PERSISTANCE, gate 4.1 non franchi | lot containment zones, 2026-08-14 | critere ecrit AVANT mesure et non ajuste : justesse >= 95 % sur l'oracle ET taux >= 80 % sur le corpus elargi. Taux mesure : **40,9 %** (64,1 % en excluant les 11 films au repere decale) — echec net. Justesse : **aucun oracle de zone n'existe**, le releve terrain du 2026-08-02 ne nommant sa base que par une LETTRE qu'aucune donnee decodee ne porte ; le substitut le plus fort (concordance inter-joueurs) donne 97,2 % sur Vagabond et 94,0 % sur le corpus, mais ne mesure pas la meme chose. Le lot leve la 2e des trois objections du 08/08 — la correction d'horloge est desormais ETABLIE ET LUE, plus estimee — et laisse les deux autres debout | reprise conditionnee, dans cet ordre : (1) defaut de repere ~1 270 m corrige (11 films, 36 % du corpus) ; (2) complement des 16 films ; (3) si le taux depasse alors 80 %, un oracle de JUSTESSE reste a construire — la seule voie connue est un releve Theater sur un match RECENT et date, avec la zone notee par lettre, ce qui donnerait du meme coup la lettre A/B/C que le chantier classe hors de portee du hors-ligne |
| **Capacites 3 et 6 : un SECOND releve Theater les renommerait** | lot equipement actif, etape 1, 2026-08-14 | `3 = mur portatif` et `6 = capteur de menace` reposaient sur UNE observation isolee chacun (releve Theater du 2026-07-27, slots 517 et 515 de `000d5950`), et la palette `sofd` famille A les contredisait. La mesure du 14/08 retire le contradicteur (le champ de 3 bits ne PEUT PAS etre le rang `sofd` : largeur 3 bits vs rangs 8/9/11/23, valeurs 0/1/2 jamais vues sur 1702 lectures, rang 3 « pas une capacite » lu 270 fois) mais ne fournit pas de preuve de rechange. Les deux noms sont donc RETIRES : 605 lectures sur 1702 (35,5 %) affichent desormais leur numero | **donnee que seul l'utilisateur possede** : ouvrir en Theater UN film autre que `000d5950` ou un joueur porte l'equipement lu 3 ou 6, et nommer cet equipement. Deux observations concordantes = controle de groupe = les deux lignes reviennent dans `replay_labels.toml` (une ligne chacune). Films candidats riches en 3 et 6 : `00502e52` (3:29 6:50), `07aa428d` (3:39 6:33), `0762ae7c` (3:33 6:37) |
| **Lecteur `i48` de PRODUCTION : le rang de palette par vie** | lot equipement actif, etape 2, 2026-08-14 | la mesure du 14/08 etablit que le rang de palette voyage dans `i48` (R(6) apres porte), **lu a 100 % quand le masque l'annonce** (748 lectures, 0 illisible, 8 films), une fois par vie environ. Le canal actuel (champ de 3 bits des images-cles) est STRUCTURELLEMENT AVEUGLE aux rangs hors 16-23 : il ne verra jamais le camouflage (8), le surbouclier (9) ni le translocateur (11), pourtant mesures dans le corpus. Non fait ici : c'est un lecteur de production + une palette determinee par film + une table indexee par (palette, rang), pas une ligne de TOML | lot dedie : exporter un `ScanFilmAbilityRanks` depuis `filmdec` (le corps existe dans `i48_rank_test.go`), determiner la palette du film par la signature de ses rangs, puis nommer par (palette, rang). Debloque d'un coup : les index 3, 6 et 7 aujourd'hui sans nom, ET l'identite des trois capacites de l'etape 4 |
| **`i57` : quelle valeur signifie « actif » ?** | lot equipement actif, etape 3, 2026-08-14 | `i57` est bien plus disponible qu ecrit (1 414 lectures sur le film temoin, 1 257 changements de valeur, ~14 par vie) et ses transitions coincident avec les episodes `i54` a **2,1 fois les temoins decales** (72,2 % contre 34,2 % et 32,9 %). Mais un temoin a 33 % dit qu une transition est proche une fois sur trois a n importe quel instant : afficher « actif maintenant » se tromperait environ une fois sur quatre. Le decompile donne `etat[3] = v - 1` (valeurs -1, 0, 1, 2) et RIEN dans le flux ne nomme ces etats | **donnee que seul l utilisateur possede** : un releve Theater DATE ou l on note l instant d une utilisation d equipement (n importe laquelle), sur un film du cache. Trois usages dates suffisent a trancher quelle valeur d `i57` porte l etat actif — l instrument `i57_reach_test.go` publie deja la valeur horodatee par slot |
| Deploiement du 2e VPS (provisioning, service systemd, secrets d'infra) | lot transport, 2026-08-14 | le plan livre le BINAIRE et le PROTOCOLE, pas l'installation — hors perimetre declare | activation du rejeu en production ; a ecrire au runbook (`docs/RUNBOOK_GO_LIVE*`) avec le jeton `LEVELUP_BUILD_WORKER_TOKEN` et le reglage `replay_build_location=worker` |
| La production n'enfile RIEN tant que `LEVELUP_BUILD_WORKER_TOKEN` est absent | lot transport, 2026-08-14 | garde volontaire : `worker` sans jeton degrade en `off`. Enfiler quand aucun ouvrier n'existe resoudrait un manifeste Halo par match a chaque cycle et gonflerait la base de monitoring pour rien. Consequence : ce lot n'active rien en prod | poser le jeton sur le VPS web au deploiement de l'ouvrier — c'est le SEUL geste qui ouvre le fil de l'eau (le reglage par defaut vaut deja « ouvrier » en production) |
| L'ouvrier distant ne purge pas SES artefacts (ni ses films si `--work` vaut le cache du depot) | lot transport, 2026-08-14 | il supprime ses morceaux de film apres chaque job, mais seulement quand `--work` designe un dossier a lui ; l'artefact local (~2 Mo/match) reste. Sur un poste de developpement c'est voulu (cache reutilisable, archive film intouchable) ; sur un 2e VPS ca s'accumule | installation du 2e VPS : `--work` sur un dossier jetable + purge de ses artefacts (ou un `--no-keep-artifact`), a trancher a l'ecriture du runbook |
| Plusieurs ouvriers en parallele : jamais mesure a plusieurs | lot transport, 2026-08-14 | la mecanique le permet (prise atomique sous queueMu + lease dblease, bail de 5 min, reprise du travail d'un ouvrier disparu) et le refus d'un artefact venant d'un ouvrier qui ne detient pas le job est teste — mais aucun test ne fait tourner DEUX ouvriers en meme temps. Ne pas le promettre | un second ouvrier reel, ou un test qui lance deux binaires concurrents sur une file de N jobs et verifie qu'aucun match n'est traite deux fois |
| Rang 10 de la famille A : observe 67 fois, NON nomme | lot rang-capacite i48, 2026-08-14 | l'identifiant de chaine du `sofd` n'est pas inverse (le §13 le disait deja des rangs 3, 10, 13-22, 24-26). C'est le rang non nomme le PLUS FREQUENT du corpus : 21 lectures sur `00ba2e1c`, 34 sur `06dfe6d9`, 10 sur `00162144`, 2 sur `084a804d` — a lui seul il explique l'essentiel des 21,3 % de lectures sans nom | inverser le murmur3 de son identifiant, ou un releve Theater nommant l'equipement d'un joueur dont la lecture i48 vaut 10. Le second est bon marche et suffit s'il porte sur DEUX joueurs |
| Rangs 19 et 22 de la famille B : une observation isolee chacun | lot rang-capacite i48, 2026-08-14 | le releve Theater du 2026-07-27 donne 19 = mur portatif et 22 = capteur de menace, mais sur UN joueur chacun. « Un controle de groupe vaut, un temoignage isole ne vaut pas ». Cout mesure : 19 et 22 pesent 34/82 des lectures de `000d5950`, 40/65 de `00502e52`, 19/42 de `07aa428d` | un SECOND releve Theater nommant l'equipement d'un joueur dont la lecture vaut 19 ou 22, sur un autre film que `000d5950`. Deux observations concordantes font un controle de groupe, et la couverture de la famille B passerait de ~50 % a 100 % |
| Nommage de la famille A adosse a UNE seule chaine (murmur3 du `sofd`) | lot rang-capacite i48, 2026-08-14 | DECISION PRISE ET DISCUTABLE : l'inversion murmur3 est admise comme source de nommage bien qu'elle soit unique, au motif qu'elle est d'une autre NATURE qu'une observation d'ecran (elle lit la table du jeu) et que c'est elle qui a fait RETIRER deux noms le 2026-08-14. Seul le rang 23 a deux chaines (murmur3 + banque sonore). Si la barre monte, la famille A ne garde que le 23 et l'item 4.2 tombe | DECISION UTILISATEUR. A defaut, un releve Theater sur un film de famille A confirmerait d'un coup plusieurs rangs (le controle de groupe y est gratuit : 8 joueurs par releve) |
| Couverture de nommage mesuree sur 7 films, pas sur le corpus | lot rang-capacite i48, 2026-08-14 | garde-fou de ressources (le poste a redemarre trois fois cette semaine sur un lot de decodage) : un balayage i48 des 40 films du cache coute ~15 min de decodage sequentiel, un film a la fois. Non lance de ma propre initiative | DECISION UTILISATEUR : machine libre + accord explicite. L'instrument existe (`filmdec/i48_rank_test.go`, garde `I48_FILM`) et n'exige aucun code neuf |
| Effet plein-fiche des capacites actives (fiche doree, effet de verre, bordure animee) | lot rang-capacite i48, 2026-08-14, item 4.1 | l'IDENTITE est resolue et publiee, l'ETAT ACTIF ne l'est pas : `i57` est lu sur 0,82 % des records et son association aux episodes `i54` vaut 72,2 % contre 34 % de temoin — une erreur sur quatre. Une fiche doree fausse une fois sur quatre est pire qu'une fiche sobre | une source d'etat fiable : rendre `i56` lisible sur les records denses, ou une verite Theater datee a la seconde. Quand elle arrive, l'effet se code sans rien inventer — la palette est resolue et l'identite publiee. **MISE A JOUR 2026-08-16 : la source d'etat EXISTE pour DEUX familles** (PLAN_ETAT_ACTIF_EQUIPEMENT) — camo : `i28` queue[1] binaire 0/4095, transitions EXCLUSIVES aux vies rang 8 (39 contre 0 sur 574 autres vies, 3 films) ; surbouclier : `i5` non clampe, regle `q > 64` exclusive aux porteurs (0 faux positif sur 113 000 mesures hors porteurs). **LIVRE le 2026-08-16 pour ces deux familles** (lot equipement : `equipmentEpisodes` schema 7, camo ESTOMPE la fiche, surbouclier la SURLIGNE, sons Activate/Deactivate) — les autres familles restent sans etat mesure, donc sans effet |
| Plafond de voix simultanees a 8 alors que TOUS les tirs sonnent | plan effets de tirs, 2026-08-15, item 1.2 | l'utilisateur a demande « tous les sons, je verrai si c'est trop ensuite ». Mesure du jour : sur le corpus des 23 artefacts, 4 897 sources refusees pour 17 068 (28,7 %) ; 46 refus sur le film temoin. Le plafond n'a PAS ete releve — c'est une decision d'oreille, pas de code, et le relever est un changement d'un chiffre (`SOUND_MAX_VOICES`, replayAudio.ts) | ECOUTE DE L'UTILISATEUR sur un echange nourri : soit « c'est trop » (et on baisse, ou on filtre), soit « il en manque » (et on releve le plafond) |
| Duree de l'eclair de bouche non reglee a l'oeil | plan effets de tirs, 2026-08-15, item 2.4 | la fenetre reste `SHOT_HOLD_MS` = 600 ms. **AMENDE le 2026-08-15 (lot visibilite)** : l'extinction n'est plus au carre mais en puissance 3/2 — mesure de rasterisation, l'eclair ne tenait que 3 images sur 6 en theme sombre et 0 sur 6 en theme clair ; il en tient 5 sur 6 dans les deux. La DUREE elle-meme reste un choix d'oeil | GATE VISUEL : verdict de l'utilisateur sur un echange nourri, a 1x et a 2x |
| Deux eclairs qui se recouvrent ne s'ADDITIONNENT plus | lot visibilite de l'eclair, 2026-08-15 | la composition passe de `lighter` a normale, parce que l'additif rend l'eclair MATHEMATIQUEMENT invisible en theme clair (mesure : 0 pixel change sur 6 images, toutes familles, toutes teintes). Le prix est la superposition : deux tirs simultanes au meme endroit ne brulent plus. Aucun cas n'a ete juge a l'oeil | si l'utilisateur trouve les rafales trop plates : additif RESERVE au theme sombre (il faudrait alors faire descendre le theme jusqu'a `FxInk`, qui l'ignore aujourd'hui) |
| ~~L'EXPLOSION de grenade compose elle aussi en additif — degradee en theme clair~~ | lot visibilite de l'eclair, 2026-08-15, decouverte hors perimetre | — | **SORTI (lot visibilite de l'explosion, 2026-08-15)** : les quatre emplois de `lighter` juges un par un et passes en `source-over`, plateau de 30 % ajoute au degrade du flash. Pixels changes en theme clair, pire cas sur 2 fonds x 3 types : flash 0 -> 1484, boule de feu 0 -> 379, onde 0 -> 313, eclats 0 -> 43. Garde-rail neuf `e2e/replay-explosion-raster.spec.ts`, PHASE PAR PHASE (une mesure globale laissait passer : a 320 ms sur fond clair, les 209 px changes venaient TOUS de la poussiere) ; verifie a l'envers, il rougit sur 24 combinaisons de la version d'avant |
| Les BRAISES et les ECLATS d'une explosion ne s'ADDITIONNENT plus entre eux | lot visibilite de l'explosion, 2026-08-15 | meme arbitrage que pour l'eclair de bouche (ligne ci-dessus) : ce sont les deux seuls sites ou l'additif apportait quelque chose (12 disques et 10 trainees qui se recouvrent), et ils y perdent le sur-eclat blanc de leurs recouvrements. Prix mesure sur fond sombre : boule de feu 235 -> 194 px, eclats 47 -> 46. Le flash et l'onde, eux, ne perdent RIEN (une seule primitive chacun, rien a accumuler). Aucun cas juge a l'oeil | si l'utilisateur trouve les explosions plates en theme sombre : additif RESERVE au fond sombre — meme prealable que pour l'eclair (faire descendre le theme jusqu'a l'encre, qui l'ignore aujourd'hui), et les deux se traitent alors ensemble |
| Le garde-rail de rasterisation ne tourne qu'en E2E | lot visibilite de l'eclair, 2026-08-15 | seul un navigateur reel rasterise : jsdom (donc vitest) ne peint rien, et `@vitest/browser` n'est pas installe. Le job Playwright de la CI ne se declenche que sur PR vers `main` — le garde-rail est donc un gate de MERGE, pas de commit | si le rejeu gagne d'autres calques peints, envisager `@vitest/browser` (provider playwright) pour ramener la preuve pixel dans le gate rapide |
| Melee A MAINS NUES : aucun son, et aucune source sonore | question utilisateur, 2026-08-15 | mesure sur les 23 artefacts : 1 025 kills, dont 37 au marteau et 11 a l'epee — ceux-la SONNENT deja (cles au manifeste, WAV presents). 442 kills (43,1 %) n'ont AUCUNE cle d'arme (residu connu de la source de degat) et restent muets ; la melee sans arme est dans ce lot. Le pack ne porte pas de fichier de coup | l'utilisateur fournit un son de melee, comme pour les 4 armes muettes (Bandit EVO, MA5K Avenger, SPNKr a combustible, carabine Vestige) |
| `PalmaresRelationsPage.test.tsx` rouge en suite COMPLETE, vert seul | lot visibilite de l'eclair, 2026-08-15, decouverte hors perimetre | 1 echec sur 3 803 tests en `npx vitest run` complet ; le meme fichier passe (37 tests) lance seul. Aucun rapport avec le lot (qui ne touche que `match-replay` et un fichier E2E neuf). **2e OBSERVATION le 2026-08-16 (lot equipement, qui ne touche pas non plus palmares)** : timeout 5 000 ms sur « rend le badge cross-jeu » en suite complete (1/3821), vert seul en 2,9 s (14 tests) — le motif est CONFIRME sur deux lots independants | pattern confirme (2 passes) : chercher la fuite d'etat entre fichiers (mocks de query non reinitialises) — lot hygiene, PAS un correctif opportuniste d'un lot rejeu |
| La teinte FORERUNNER (violette) n'a AUCUN tir dans le corpus | plan effets de tirs, 2026-08-15, item 2.6b | le Rayon de Sentinelle est la seule arme Forerunner du jeu et aucun des 23 artefacts locaux n'en porte un tir (0 sur 17 904). La teinte est declaree, cablee et testee, mais elle n'a jamais ete VUE a l'ecran | un film portant un tir de Rayon de Sentinelle. A defaut, la verifier en forcant temporairement la teinte d'une arme frequente — a NE PAS committer |
| Le rendu de la grenade Spike : explosion `needle` ou halo ? | plan effets de tirs, 2026-08-15, item 3.3 | le plan laisse ce type « a trancher a l'oeil ». Choix pose : elle DETONE (teinte cristal), parce qu'elle se plante puis projette ses pointes et que c'est le seul type dont la fin de vol est certifiee `at-rest` (15 vols lies sur 21). 49 vols Spike lies dans le corpus | VERDICT DE L'UTILISATEUR au gate 3. La ramener au halo tient en une ligne de `restKindOf` (grenadeFx.ts) |
| `internal/analysis/weaponv3` depasse le timeout du gate LOCAL | plan effets de tirs, 2026-08-15, decouverte hors perimetre | `make go-api-test` impose `-timeout 60s` ; le paquet met ~63 s et part en panic de timeout. Il passe en 63,6 s sans la borne. HORS PERIMETRE de ce plan : le paquet ne depend que de `internal/analysis`, qu'aucun commit de ce lot ne touche. La CI Linux reste le juge d'autorite | mesurer le paquet en CI ; s'il y est aussi lent, alleger son jeu de donnees ou relever le timeout de la cible locale — jamais desactiver le test |
| Rejeu 2D : distinguer tir NORMAL / CHARGE / CONTINU (son + effet visuel) | demande utilisateur, 2026-08-15 | intention exprimee pour « a terme », pas un lot ouvert. Le lot effets du jour ne differencie que par FAMILLE d'arme (7 formes) et par TEINTE de decharge (7 natures) : un tir charge et un tir normal de la meme arme sont aujourd'hui identiques a l'oeil ET a l'oreille | **DEUX MOITIES D'AMBITION INEGALE, a ne pas confondre.** (1) CONTINU = propriete de l'ARME, pas du tir (Rayon de Sentinelle, Crematier) : realisable SANS aucune donnee nouvelle, par une colonne du manifeste du titre a cote de `[shot_effects]` / `[shot_tints]`, plus une boucle de son tenu au lieu d'un declenchement par tir. (2) CHARGE = etat par TIR : la donnee EXISTE (`AmmoSlot.Gauge`, union `i30` branche fraction, deja publiee) mais elle est lue aux IMAGES-CLES, pas aux instants de tir — il faudrait croiser la jauge avec l'horodatage du tir, ce qui n'a jamais ete fait, et MESURER si la resolution temporelle suffit. **PIEGE DEJA DOCUMENTE : la jauge compte le CONSOMME, pas le restant** (2 temoins concordants, 2026-07-31) — un client qui l'affiche tel quel montre une barre inversee. **Limite dure** : le DENOMINATEUR de la jauge est propre a l'arme et ABSENT du film (table hors film requise). Piste distincte pour la surchauffe : `i32`/`i35` `weapon-state-overheated` (f32 7 bits @+0x88 + 2 bits de drapeaux @+0x82), jamais instruit. Sources : `RECETTE_LOADOUT_2026-07-27.md` §10, `inventory.go:38-53` |
| Tir CONTINU (faisceau tenu) : **NO-GO mesure, RIEN IMPLEMENTE** | lot mode de tir, 2026-08-15 | la mesure de cadence sur les 23 artefacts (instrument `cadence_corpus_test.go`, garde `REPLAY_CORPUS`) refute la premisse sur TROIS points. (1) **L'unique arme a faisceau tenu du jeu ne tire pas dans le corpus** : le Rayon de Sentinelle est PORTE 49 fois et produit **0 tir**, la ou ses voisines de meme frequence de port produisent 13 a 77 tirs (Empaleur 57/13, Cremateur 58/67, Bulldog 53/52, Dechiqueteur 52/77). Il se comporte comme les armes de MELEE (marteau 72/0, epee 54/0). (2) **Les deux candidats nommes sont REFUTES** : Cremateur/Cindershot 67 tirs, ecart median **1 400 ms**, 0 % d'ecarts d'une frame, p90 17,8 s ; Calcineur/Heatwave 23 tirs, 800 ms. (3) **Le plancher de mesure est la frame de 100 ms**, et le MA40 AR l'occupe deja (82,9 % de ses ecarts consecutifs y sont) : meme avec des tirs, la cadence a ce quantum ne separerait pas un faisceau d'une automatique rapide. Consequence : une table `[shot_modes]` n'aurait AUCUN membre observable, et le rendu tenu ne se declencherait sur aucun film connu — c'est-a-dire du code mort par construction (regle 7). **Note** : la famille `light` de `[shot_effects]` n'est PAS un substitut — son seul membre jamais vu (Calcineur) n'est pas continu | un film portant des TIRS de Rayon de Sentinelle. La condition est verifiable en une commande : `REPLAY_CORPUS=... go test ./internal/games/halo_infinite/replaylabels/ -run TestCadenceDesArmesSurLeCorpus -v` — la ligne « hinf_sentinel_beam portee N fois, M tir(s) » repond seule. Si M > 0 : la declaration `[shot_modes]` se pose sur le patron de `[shot_tints]` (valideur Go a liste fermee + garde-rail de vocabulaire cote client), et le son se coalesce par rafale au lieu de se declencher par tir |
| Tir CHARGE : **NO-GO mesure, RIEN IMPLEMENTE** | lot mode de tir, 2026-08-15 | la jauge existe (`AmmoSlot.Gauge`) mais elle ne peut pas qualifier un tir, et deux mesures independantes le disent. (1) **RESOLUTION TEMPORELLE** : sur 14 238 tirs d'armes ayant porte une jauge, l'ecart au releve de jauge le plus proche du meme tireur a une mediane de **8 900 ms**, un p90 de **42 300 ms**, et **3,03 %** seulement passent sous 500 ms. **73 tirs sur 14 238 (0,51 %)** sont ENCADRES par deux lectures, et **8 (0,06 %)** montrent une jauge qui bouge entre l'avant et l'apres — soit 0,04 % des 17 904 tirs du corpus. Un rendu bati la-dessus qualifierait un tir sur deux mille. (2) **L'ATTRIBUTION jauge -> arme est elle-meme refutee** : l'appariement `Inventory.Am[k]` <-> `Loadout.W[k]` place 98 des 307 lectures de jauge sur le **MA40 AR** et 38 sur le **marteau antigravite**, deux armes qui n'ont aucune charge dans le jeu. On ne sait donc meme pas de quelle arme parle une jauge. Le pistolet a plasma, arme a charge par excellence, ne totalise que **4 tirs** dans tout le corpus | DEUX prealables, dans cet ordre. (a) FIABILISER l'appariement emplacement -> arme (la mesure ci-dessus est le temoin : une jauge sur le MA40 doit disparaitre) ; (b) une source de charge lue aux INSTANTS DE TIR et non aux images-cles — piste jamais instruite : `i32`/`i35` `weapon-state-overheated` (f32 7 bits @+0x88, 2 bits de drapeaux @+0x82). Le denominateur de la jauge reste absent du film : toute barre en pourcentage exigerait une table hors film |
| 150 tirs du corpus portent une famille d'arme ABSENTE du registre | lot mode de tir, 2026-08-15, decouverte hors perimetre | 8 familles, dont `0x1833A5A8` a elle seule 118 tirs — cadence mediane 100 ms et 77,4 % d'ecarts d'une frame, soit la signature d'une automatique (le MA40 est a 82,9 %), pas d'un faisceau. Les 7 autres pesent 32 tirs. Aucune n'est connue de `weaponv3.KnownWeaponHigh32` : elles n'ont ni nom, ni son, ni effet, et le rejeu les dessine en trait neutre — ce qui est la regle, mais en silence | les nommer exige une source (tourelles et armes de vehicule sont les candidats naturels, jamais observes au registre). Chiffre a re-mesurer si le registre d'armes gagne des entrees : meme commande que la ligne « tir CONTINU » |
| Son du coup de melee NON FATAL (`Melee - Hit` du pack) : **non branche, fichier non livre** | lot sons explosions + melee, 2026-08-15 | le document de rejeu ne porte AUCUN flux de degats ni d'impact : ses seuls evenements dates sont les tirs (`Shots`), les lancers (`Grenades`), les trajectoires (`Tracks`) et le fil des MORTS (mesure sur `replay/document.go`, 40 champs). La seule trace d'un coup non fatal serait une baisse de bouclier lue sur `Point.sh` — mais un bouclier baisse pour n'importe quelle source, donc le brancher la reviendrait a INVENTER un coup de melee. Le WAV n'est deliberement pas verse sous `static/sounds/` : un asset que rien ne joue casse le garde-rail (0 asset mort) | un flux de degats PAR COUP dans le document de rejeu. La piste existe et est deja instruite : `project_damage_stream_per_hit_opportunity` (per-hit 0xD2). Le jour ou un evenement d'impact est date et attribue, `Melee - Hit` se branche en une ligne de `KILL_SPRITE_SOUND_STEMS`-bis cote impacts |
| Grenades a etiquette AMBIGU : 2 tags sur 17 restent MUETS — **COUT MESURE NUL le 2026-08-16** | lot sons explosions + melee, 2026-08-15 ; quantifie par `PLAN_TYPE_GRENADE_PAR_LANCER.md` | `damagetag/data/labels.tsv` marque AMBIGU deux effets de grenade generiques traversant plusieurs entrees `gggl` (`31e8d17e` : entrees 0+1 ; `88f1034c` : entrees 0+1+3). Non publiables, donc sans vignette, donc sans son. C'est le comportement VOULU : le type n'est pas etabli, et sonner l'explosion d'une voisine serait un mensonge inaudible. **CE QUI EST NOUVEAU : le denominateur, qui n'avait jamais ete compte.** Sur les 23 artefacts (868 morts), les 63 morts de classe GRENADE (7,26 %) se repartissent sur 8 tags TOUS VALIDE — aucune n'est ambigue. Sur **150 films du cache (11 583 morts, 696 par grenade, 13 tags)** : **1 seule mort ambigue, `88f1034c`, film `13b00e35`** — soit **0,14 %**, borne haute 95 % (Wilson) **0,81 %**. `31e8d17e` n'est jamais observe. Le silence de ces deux tags coute donc une mort tous les ~700 morts par grenade, soit ~1 match sur 150 | inchangee sur le fond (une mesure qui desambiguise les deux effets), mais **la priorite tombe** : la population est quasi vide et le cout est chiffre. Rejouer quand la table de nommage change de saison : `FILM_SWEEP=150 go test ./internal/replaybuild/ -run TestPhase0Bis -v -timeout 90m` |
| Type d'une grenade par le LANCER plutot que par le tag : **NO-GO mesure, RIEN IMPLEMENTE** | `PLAN_TYPE_GRENADE_PAR_LANCER.md`, 2026-08-16 | **les deux gates du plan echouent, independamment.** (0) La cible est QUASI VIDE : **1 mort sur 696 morts par grenade (150 films, 11 583 morts) = 0,14 %, borne haute 95 % 0,81 %**, sous le seuil de 1 %. Le corpus d'artefacts seul (0 sur 63) n'aurait PAS tranche : sa borne haute vaut 5,75 %. Le balayage large a en revanche trouve une vraie occurrence (`88f1034c`, film `13b00e35`) — le negatif porte donc sur le POIDS, pas sur l'existence. (1) La jointure lancer -> mort atteint bien le seuil d'accord — **96,00 % a 2 s, 96,43 % a 5 s**, couverture 86,2 % puis 96,6 %, cas confondants 1 a 5 — **mais le temoin (a) ne s'effondre pas** : un AUTRE joueur tire au hasard obtient 60 a 80 % d'accord, parce que **66,7 % des morts par grenade sont des frags** (un predicteur constant « toujours frag » scorerait 66,67 % sans rien lire). Le temoin (b), mort decalee de +10 s, s'effondre lui a 0,00 % jusqu'a 5 s. Decision n°4 du plan : pas de repli, le seuil ne se rebaisse pas apres coup | DEUX conditions, l'une OU l'autre. (a) Que le POIDS des deux tags franchisse 1 % — le compte se rejoue en une commande et l'instrument nomme les films porteurs. (b) Que le temoin (a) puisse s'effondrer : il faudrait une population de morts par grenade moins dominee par la frag, ou un temoin mieux peuple (il ne trouve un lancer que sur 4 a 19 morts sur 58). Instruments versionnes et gardes : `internal/replaybuild/grenade_corpus_test.go`, `grenade_join_corpus_test.go` (`REPLAY_CORPUS`), `grenade_ambigu_sweep_test.go` (`FILM_SWEEP`). **DECISION UTILISATEUR 16/08 : sujet CLOS** (l'impact ne justifie aucun fallback ; le kill feed du JEU resout ces cas par l'objet source ECS au runtime, valeur non serialisee dans l'evenement de kill du film — RE §4). Si reouverture : la recette serait lancer (96 % a 2 s) **+ grenades EQUIPEES comme CONTRAINTE** (i22/i47 : un tueur ne portant qu'UN type candidat n'est plus une inference, c'est une elimination) — evaluee le 16/08, non retenue seule (plus faible que le lancer : le decrement i22 EST le lancer, et en Fiesta plusieurs types portes) |
| `equipment-charges-remaining` (i27) et `equipment-energy-delay-ticks-left` (i26) de ti=37 : jamais lus | etape 5 PLAN_EQUIPEMENT_ACTIF, 2026-08-15 | hors de la liste prescrite du lot (les QUATRE champs i20/i21/i23/i24). Le recensement au masque publie par le meme lot les designe pourtant comme le gisement : sur 12 films et 1 097 619 records ti=37, **charges-remaining est annonce 16 125 fois (3,1x l'energie, 7,6x `activated`)** et energy-delay-ticks-left 10 608 fois. Un compteur de charges qui decroit date une utilisation par construction ; un compte a rebours en ticks porte une duree | les deux sont **DEJA PORTES** par `consumeByName` (traverse.go) : il ne manque qu'un hook, exactement comme pour les quatre d'aujourd'hui. Reprise : ajouter deux champs a `EquipmentField` (`equipment_state.go`), rejouer `equipment_state_test.go` sur les 12 memes films, et publier transitions + decroissances par vie d'objet. C'est le premier geste du prochain lot equipement. **CONFIRME le 2026-08-16 (phases C/E, PLAN_ETAT_ACTIF_EQUIPEMENT)** : la datation des deployables a echoue par `activated` (1 transition sur 3 films), par le R(24) d'i57 (pas un handle — valeurs quasi toutes uniques) et par les naissances ti=37 (densite 4,7-5,3/s, temoins au niveau du reel) — `charges-remaining` reste LA voie candidate, toujours EN RESERVE (decision user du 16/08) |
| ~~Identite de l'objet ti=37 : quel equipement est cette entite ?~~ **SORTI le 2026-08-18** | etape 5 PLAN_EQUIPEMENT_ACTIF, 2026-08-15 | les quatre champs mesures ne portent AUCUNE identite. L'archetype s'appelle `item-*` autant qu'`equipment-*` (i18 `item-at-rest`, i19 `item-ignore-player`) : 9 043 trajectoires sur 12 matchs, c'est bien plus que des equipements de joueur — bonus au sol et socles y sont probablement melanges. La question du plan (« ti=37 couvre-t-il les SEPT equipements ou seulement les deployables ? ») **n'est pas tranchee, et la mesure ne pouvait pas la trancher** : elle compte des entites, pas des equipements | trouver dans le record de CREATION de l'entite (record NEW, pas delta) la reference de definition de l'objet — meme voie que la famille high-32 des armes au sol (`keyframe_ground_weapons.go`). Sans elle, aucun nommage, et donc aucun rendu | **REPONDU, et la reponse est chiffree** : le mot de 32 bits inconditionnel du bloc `object-multiplayer-properties` du record de CREATION est le GlobalID du tag `eqip` de l'objet — 21 valeurs observees sur un corpus de 11 films, 21 resolues dans le groupe `eqip` du jeu (148 097 tags parcourus). L'archetype melange bien equipements de joueur et objets du monde, et la mesure les SEPARE : le poseur le plus proche est a 0,52-0,60 m median pour un equipement pose, contre 11-36 m pour le temoin, et les quatre identifiants PLATS (`0x0f5716ff`, `0xbcabbe43`, `0xcaaadcb0`, `0xaada07f3`) sont les objets du monde — sans poseur, et c'est la mesure qui le dit.
| ~~`SetWorldObjectPrecisionFromLayout` n'est appelee par AUCUN chemin de production~~ **CLOS le 2026-08-15** | etape 5 PLAN_EQUIPEMENT_ACTIF, 2026-08-15 | **TRAITE par `PLAN_PRECISION_OBJETS_MONDE.md`.** L'impact a ete MESURE avant de corriger, sur 7 films / 6 cartes / 6 valeurs de largeurs, critere = part d'echantillons dans l'emprise du nuage des bipedes du meme film en coordonnees normalisees de l'AABB. **ti=41 (chemin de production) : 0,09 / 0,51 / 28,46 / 31,31 / 65,21 % avec le defaut contre 98,96 a 99,79 % avec les largeurs du catalogue** ; sur Cliffhanger, temoin, **92,11 % des deux cotes et 580 trajectoires identiques**. La source des largeurs n'est PAS `DetectI0Layout` mais l'entree de catalogue `MapQuantEntry.AxisWidths` — la meme qui portait deja les bornes ; `DetectI0Layout` sert de CONTROLE (accord 7/7). Correctif : `replay.Options.WorldRange` devient `Options.MapQuant` (un seul champ : bornes et largeurs ne se dissocient plus) et `BuildFromFilm` installe/restaure sous `LockProcessDecode` | **CLOS.** Garde-rails poses et VUS ROUGES : `TestBuildFromFilmWiresWorldObjectPrecision`, `TestInstallWorldObjectPrecision`, `TestWorldObjectPrecisionReadersAreAllowlisted` (allowlist datee). Instrument de mesure versionne : `filmdec/world_object_precision_test.go` |
| ~~`traverseComponentLoop` lit `WorldObjectPrecision` hors `BuildFromFilm` — le decodeur de trames de `killsource` reste aux largeurs de Cliffhanger~~ **CLOS le 2026-08-16 sur un NEGATIF MESURE** | PLAN_PRECISION_OBJETS_MONDE phase 2, 2026-08-15 | **MESURE (PLAN_ALERTES_REPLAY_PARTOUT phase 1) : l'ecart est NUL.** `killsource.Decode` rejoue AVANT/APRES installation des largeurs de catalogue sur 3 films — Bazaar `[17 17 16]`, Illusion `[18 18 17]`, temoin Cliffhanger `[13 13 14]` — rend **0 ligne changee sur 95 / 91 / 93**, couverture 100 % des couples reels des deux cotes, marche appariee 89/90, 83/84, 84/86 inchangee, divergences inchangees, alertes vides. Une **sonde d'absurde `[6 6 6]`** (26 bits de moins par record) ne deplace rien non plus : le negatif n'est pas un instrument muet. Le desalignement, lui, EXISTE et est mesure — aux largeurs de la carte la marche lit plus de records et **les dead-states lus derriere un objet du monde s'effondrent (85 -> 6 sur Bazaar, 65 -> 14 sur Illusion)** : ce sont des dead-states FABRIQUES par le desalignement, que le filtre de credibilite rejetait deja. Cabler l'entree de catalogue jusqu'a `Decode` ajouterait un parametre, trois appelants et un garde-rail pour un effet nul sur la sortie | **CLOS.** Instrument versionne et rejouable : `internal/games/halo_infinite/film/killsource/world_precision_test.go` (`TestKillSourceWorldPrecisionImpact` + `TestKillSourceWalkArchetypes`, gardes par `KSPREC_FILM`). ROUVRIR SI le filtre de credibilite de la marche est relache (plage de slots de bipede elargie, roster incomplet, categorie non contrainte) : c'est lui qui absorbe l'ecart aujourd'hui |
| ~~**Golden `minibobine` ROUGE sur `feat/v75` depuis `020b95eab`**~~ **CLOS le 2026-08-16 par RATIFICATION (fusion, commit `0a416cdfb`)** | decouverte PLAN_ALERTES_REPLAY_PARTOUT, 2026-08-16 | le lot grappin a ajoute les cles i59 a `paramByComponent` (traverse.go) sans re-congeler `minibobine.golden` : `recordStateParam` passe de 0 a 3 et la croissance de 1.000 a 1.002. **Le garde a fait exactement son travail** — sa doc dit que son canal de detection EST la ligne de calibration. Portee mesuree non destructivement (`-update` puis `git checkout`) : **UNE SEULE ligne differe sur 77**, les 10 lignes publiees et les 3 ancres Theater ne bougent pas | **CLOS.** Portee RE-MESUREE a la fusion avant de re-congeler (`-update` puis `git diff` : 1 insertion, 1 suppression, la seule ligne de calibration ; les 10 lignes publiees, les 3 ancres Theater, la couverture, le controle negatif, les voies et la sante identiques a l'octet). Le balayage `calibrateRSP` ne choisissait 0 que faute de discriminant (profil plat, x1.000) ; il choisit 3 avec x1.002 depuis que la queue R(3) d'i59 est lue. Re-congeler NE DESARME PAS le garde : son canal reste cette ligne, une prochaine derive du decodeur la deplacera comme celle-ci l'a deplacee, et le plancher de 10 lignes publiees est inchange |
| ~~`i59` tag==3 = evenement du GRAPPIN — portage du corps `FUN_142f25e90` JUSTIFIE comme ANCRE~~ **SORTI (PLAN_GRAPPIN_LIGNE, 2026-08-16) : porte, prouve, publie (schema 8), trace** | phase E, PLAN_ETAT_ACTIF_EQUIPEMENT, 2026-08-16 | 210 occurrences tag==3 sur 3 films famille B (57+103+50), toutes datees et attribuees a leur slot : **115/117 lectures a porteur i48 identifie tombent sur des vies RANG 20 (grappin)**, 0 sur un autre rang identifie (2 exceptions [19], vraisemblable trou de couverture i48), 93 sur des vies sans identite ; motif en PAIRES a ~0,15 s. AUCUNE co-datation avec les signaux deployables (naissances ti=37 : reel ≈ temoins decales aux fenetres fines ; `activated` : 0 co-datation). Le corps non porte lit position + quaternions : le candidat naturel est le POINT D'ANCRAGE du tir de grappin. Portage NON execute (interdit du plan de mesure) | **EXECUTE (PLAN_GRAPPIN_LIGNE, phases 0-2)** : grammaire MESUREE dans le flux (le decompile n'avait que les feuilles justes ; l'enrobage etait faux, la position est aux LARGEURS D'AXE DE LA CARTE) ; marche a l'ecart ZERO sur les 3 films (48/48, 72/72, 33/33) ; controles (a) emprise 203/203, (b) decroissance accroche 85/90 contre temoins effondres, fixite 0,05-0,07 u ; publie `grappleLines` (schema 8) + couverture ; ligne blanche (`--foreground`) au canvas. Restes au registre : drapeaux != 000 et vecteurs du corps lourd (lignes dediees ci-dessous) |
| ~~Camo et surbouclier LISIBLES cote film, NON publies au document rejeu~~ **SORTI (lot equipement, 2026-08-16)** | phases A/B, PLAN_ETAT_ACTIF_EQUIPEMENT, 2026-08-16 | — | **PUBLIE** par les phases 1-4 du `PLAN_EQUIPEMENT_TI37.md` restreintes aux deux familles (decision user 16/08) : `equipmentEpisodes` au document (schema 7, episodes dates par vie, fin mesuree ou fermee a la mort via `endRead`), couverture par famille dans `Coverage.equipment`, effet pleine fiche (**rendu mis en CONFORMITE lot effets-fiches, 2026-08-16 : verre / encadre dore**, cf. la ligne « Effets plein-fiche » ci-dessus — il disait « camo estompe, surbouclier surligne ») et sons Activate/Deactivate. DECOUVERTE au golden : sur `000d5950` (Fiesta) c'est le **DASH du mode Fiesta (1-2 s, chainable) — PAS un power-up ramasse** qui allume i28 queue[1] (698 lectures strictement binaires, transitions sur vies rangs 19-22 ; enseignement utilisateur du 2026-08-16, ce mode ne pose aucun equipement au sol). Mesure qui le verrouille (`camo_duration_distribution_test.go`) : 36 episodes, mediane 2700 ms, mais un episode a ACTIVATION UNIQUE (15/36) ne depasse pas 9600 ms, tres sous une duree de ramassage (surbouclier a 61,6 s). i28 est l'etat de l'UNITE, l'exclusivite rang 8 etait la validation du canal sur des films sans autre source. `Point.sh` reste clampe, inchange. Restent les gates VISUEL et ECOUTE utilisateur |
| ~~Filtre des sons du rejeu par CATEGORIE (armes / grenades / equipements)~~ | idee utilisateur, 2026-08-16 | — | **TRAITE (tiroir de reglages, 2026-08-16, wt/drawer-replay 5dd36609a, fusionne)** : 4 categories filtrables independamment (armes = tirs+kills d'arme meme table ; grenades = lancers ET explosions ; melee = coup fatal ; equipements = camo/surbouclier), tout actif par defaut (comportement inchange). Cable a la CONSTRUCTION de buildSoundTimeline (SoundCategoryFilter, 4e parametre optionnel), jamais en aval dans le lecteur ; SOUND_MAX_VOICES et la regle silence propre inchanges. Teste (« categorie coupee = aucun son de cette categorie, les autres inchanges ») |
| Vitesse de lecture PERSISTEE mais plus affichee hors tiroir (rejeu 2D) | constat de revue de fusion, 2026-08-16 | **COMPORTEMENT NOUVEAU introduit par le tiroir** : la vitesse etait remise a 1x a chaque montage, elle est desormais relue de `localStorage` (`replay-speed`), et la rangee de vitesse a quitte la barre pour le tiroir. Consequence mesurable : choisir 4x, fermer le tiroir, revenir plus tard ou ouvrir un AUTRE match -> le rejeu demarre a 4x sans que rien a l'ecran ne le dise, et comme `SOUND_MAX_SPEED = 2` aucun son ne sort alors que l'interrupteur est ON. L'indice `soundFastHint` (« le dire, pas le cacher ») n'est porte que par `ReplaySoundControls`, monte uniquement quand le tiroir est OUVERT. NON TRAITE : afficher la vitesse en cours dans la barre (ou ne pas persister la vitesse) est une decision de RENDU | **DECISION UTILISATEUR au gate visuel du tiroir.** Trois options, sans preference imposee : (a) une pastille « 4x » dans la barre a cote du bouton Reglages ; (b) la vitesse redevient volatile (seuls calques et son restent persistes) ; (c) statu quo assume. La correction est locale a `ReplayCanvas.tsx` / `useReplaySettings.ts` dans les trois cas |
| Corps i59 tag==3 a DRAPEAUX != 000 : grammaire non etablie (8 records / 210 corpus famille B) | PLAN_GRAPPIN_LIGNE phase 0, 2026-08-16 | les 3 bits apres la valeur interne sont des drapeaux de presence : a 000 (202/210) la grammaire est mesuree et prouvee ; les valeurs 001/100/110 changent la forme (tailles singleton 37/92/114/142/189-247 bits, dont deux INDEPENDANTES de la carte) et rendent `ported=false` — desync propre, comptee (`BodyBroken`), jamais une largeur devinee. Idem valeur interne 4 (1 record). Perte pratique : ~4 % des lectures de grappin | un corpus ou ces variantes sont denses ; rejouer `TestI59AnchorBodyDump`/`Template` (i59_anchor_test.go) dessus — l'instrument lit la grammaire tout seul quand les classes ont des effectifs |
| Semantique des DEUX vecteurs dir24+mag12 du corps LOURD d'i59 (et du R(24)+R(9) de queue) | PLAN_GRAPPIN_LIGNE phase 0.3, 2026-08-16 | l'ancre est la POSITION ABSOLUE du corps — les vecteurs n'ont pas eu a etre identifies pour le rendu. Le fit par fixite inter-membres est IMPOSSIBLE en l'etat : le corps leger (tir) ne porte jamais de vecteur, donc jamais deux lectures appariees. Candidats : velocite du porteur, direction du cable ; la plage (min, max) de la magnitude log/exp n'est pas etablie (DAT_143cd839c non relevee) | relever DAT_143cd839c dans l'exe (statique, projet `HI.rep`) OU correler la direction cubemap (exacte, sans plage) au deplacement du porteur apres l'accroche — les quanta sont deja publies par le hook |
| Test web FLAKE : `PalmaresRelationsPage` (1 echec isole, 2 passes completes vertes ensuite) | gates PLAN_GRAPPIN_LIGNE, 2026-08-16 | hors perimetre du lot (page Relations, aucun rapport avec le rejeu) ; non reproduit sur deux suites completes consecutives (424/424) | au prochain echec : isoler avec `vitest run <fichier> --retry=0` et regarder le setup asynchrone de la page |
| ~~Bornes de canevas Forge REFUTEES : `fo05_desert`, `fo11_blank`, `fo13_frost` — 18 entrees deja cataloguees, 276 matchs servis avec de FAUSSES bornes~~ | PLAN_ALERTES_REPLAY_PARTOUT phase 3, 2026-08-16 | — | **SORTI (PLAN_BORNES_CANEVAS_FORGE, 2026-08-16)**, et le perimetre etait PLUS LARGE que ce que cette ligne disait : le balayage des 29 modules porteurs de sbsp de l'installation rend **SIX** canevas ou le plus gros tag n'est pas l'arene, pas trois — `fo03_space` (**Starboard**, 24 matchs) et `fo06_deepsea` (**Dredge**, 16) portaient la meme faute sans avoir jamais ete controles, `fo10_deadland` est le sixieme (aucune carte cataloguee). Portee reelle : **21 entrees / 316 matchs**. Critere retenu, STRUCTUREL et celui du moteur : le BSP de dequantification est la **region 0** du bloc structure-BSP du tag de niveau (`levl`), lu par `himap.BSPQuantification` — lisible sur les 29 modules, confirme sur les 29 par un controle croise independant (le plus petit AABB), verrouille par `himap.TestBSPQuantificationTousModules` qui nomme les six canevas. Catalogue regenere : 21 modifiees ([18 18 18] -> [15 15 17]), 14 ajoutees, 0 retiree, 43 inchangees. Controle film : **342 couples (film, carte) · 66 cartes · 342 accord · 0 desaccord · 0 illisible** (`filmdec.TestControleBornesFilms`, instrument neuf dont la liste de couples est une DONNEE tiree du registre — c'est la lacune du controle a la main qui avait laisse Starboard et Dredge dehors) |
| ~~14 cartes Forge prouvees mais NON cataloguees (bornes de canevas refutees)~~ | PLAN_ALERTES_REPLAY_PARTOUT phase 3, 2026-08-16 | — | **SORTI (PLAN_BORNES_CANEVAS_FORGE phase 2, 2026-08-16)** : les 14 sont au catalogue, aux bornes d'arene, une fois la ligne ci-dessus corrigee — Ecotone, Threshold, Pharaoh, Credence, Disciple, Nadair, Warehouse (`fo11_blank`) · Solution, Flood Gulch, Dawnbreaker, Vallaheim Firefight (`fo05_desert`) · Outlook, Lattice - Ranked, 944396dd-5661-4a16-b1d8-a6053f762c55 (`fo13_frost`). Controle film joue carte par carte, toutes ACCORD. DEUX n'ont aucun film en cache — **Vallaheim Firefight** et **944396dd** : elles entrent sur le controle de leur CANEVAS (`fo05_desert` 66 films, `fo13_frost` 22 films, tous ACCORD), exactement comme Cole Protocol au lot precedent, les bornes etant celles du MODULE et non de la carte. +41 matchs au perimetre constructible |
| Detachment (25 matchs) et Argyle (21) : module INCONNU | PLAN_ALERTES_REPLAY_PARTOUT phase 0, 2026-08-16 | ce ne sont PAS des cartes Forge, et pourtant leur module n'existe ni dans l'installation locale (31 dossiers sous `deploy/{pc,any,ds}/levels/multi`) ni au depot `.mvar` (seuls `detachment_map.mvar` et `argyle_map.mvar` existent — aucun fichier-lien). Ensemble : **46 matchs, plus que les 41 des 14 cartes Forge refutees**. Premier poste de non-constructibilite apres Live Fire | lire le `level_id` de `detachment_map.mvar` / `argyle_map.mvar` (`mapvar.Parse`) et le chercher dans une installation COMPLETE du jeu — l'installation locale n'a que 31 dossiers de cartes, le jeu en compte davantage. Si le module y est, la chaine s'applique telle quelle |
| **Re-build de MASSE des artefacts de rejeu deja cuits sur les six canevas Forge** | PLAN_BORNES_CANEVAS_FORGE phase 3, 2026-08-16 | un artefact deja ecrit garde les bornes avec lesquelles il a ete cuit : le correctif ne les rattrape pas. Mesure sur le cache local (28 artefacts) : SIX vivent sur les canevas corriges, TROIS ont ete re-cuits ici (`a32ee8d2` Cliffside, `c0a82e88` Corpo, `06dfe6d9` Threshold) et TROIS restent faux — `83ee3f9f` (Domicile), `084a804d` (Fortitude Heavies), `0f9550e5` (Snowbound, emprise X[-40,71 388,24] Y[-987,43 -640,65] Z[1292,99 1310,08], soit 429 x 347 m a +1 293 m). Le re-build de masse est un **lot ops a fenetre utilisateur** declare hors de ce plan (cout machine, cache partage avec le serveur qui tourne) | lancer le re-build sur les 316 matchs des six canevas quand la fenetre est ouverte ; le prealable (bornes justes) est LEVE. Controle de recette : emprise du joueur dans l'artefact — un Z au-dessus de 1 000 m signe un artefact perime |
| Threshold (`06dfe6d9`) : couverture de calques PARTIELLE, cause pont slot -> joueur | PLAN_BORNES_CANEVAS_FORGE phase 3.3, 2026-08-16 | l'artefact est construit (254 pistes, 7 532 frames, 5,87 Mo, emprise 101 x 103 m a Z[163,84 186,82] — plausible) et son controle de BORNES est vert (5 films Threshold sur 5 ACCORD), mais ses calques ne suivent pas : tirs **245 rattaches sur 2 223** (1 954 sans slot), verdict « partiel : moins des deux tiers rattaches », pont « non publiable : un slot change de porteur ». Ce n'est PAS un fait de bornes — c'est le pont slot -> joueur sur un match de 254 vies, et le melanger aux bornes ferait perdre les deux diagnostics | reprendre le pont sur les matchs a forte rotation de slots (254 vies ici contre 52 sur Cliffside) ; le film et les bornes sont acquis, seul le rattachement est a instruire |
| Son du kill au repulseur | lot R2.4, 2026-08-16 | la vignette existe (`killfeed-56`, `nom_jeu repulsor`, `tags_weap 692390e9`) mais AUCUNE source de degat mesuree n'y mene : 0 des 473 lignes de `damagetag/labels.tsv` ne nomme le repulseur, les 114 lignes nommees sont toutes de classe ARME, les 6 lignes d'effet `eqip` sont INCONNU sans nom (dont trois sont des entrees `gggl`, donc des grenades). La seule cle ecrivable, CLASSE INCONNU, poserait le son sur 206 sources sans rapport | identifier le `jpt!` du repulseur — piste concrete : l'atlas rattache `killfeed-56` au tag `weap` 692390e9 ; retrouver la chaine d'effet qui mene a ce tag depuis un `jpt!` (meme surface que les 11 lignes ARME qui portent deja un `[effet weap ...]`), ou verite-terrain Theater sur un kill au repulseur date. Le fil d'alerte est pose (`replaySoundAssets.guard.test.ts`) : il devient rouge des qu'une regle killicon mene a `killfeed-56`. Son deja choisi : `EQUIPMENT/Repulser - Activate (On Object)`, 1,85 s |
| Carte de chaleur « jusqu'au curseur » (rejeu 2D) | phase B PLAN_CALLOUTS_HEATMAP, 2026-08-16 | le calque est statique sur tout le match. Mesure : la cuisson (accumulation + lissage gaussien + quantiles) coute 6,8 ms sur une arene de 64 m et 19,1 ms sur 250 m pour 72 000 positions, soit 40 % a 115 % du budget d'une image a 60 Hz avant le remplissage — et lissage comme quantiles sont GLOBAUX, aucun chemin incremental par image | un algorithme incremental (accumulation par fenetre + etalonnage fige sur le match entier) OU une demande utilisateur explicite qui accepte un recalcul a la pause seulement |
| Deux vues de chaleur d'un match coexistent : calque du rejeu 2D (canvas, lisse, sur fond) et `features/match-view/MatchPositionsHeatmap.tsx` (ECharts 20x20, endpoint `/positions`, sans fond) | phase B PLAN_CALLOUTS_HEATMAP, 2026-08-16 | decouverte en fin de lot ; hors perimetre | arbitrage PRODUIT : garder les deux (usages differents) ou remplacer la vue de la page match par un lien vers le rejeu avec la carte de chaleur active |
| Hauteur du canvas du rejeu figee a 480 px alors que la rangee `fil \| carte \| fiches` lui laisse la colonne centrale | PLAN_HABILLAGE_REJEU_2D §4, 2026-08-17 | hors perimetre du lot d'habillage (la hauteur commande `fitWidth`, les calques cuits hors ecran et la hauteur de rangee des colonnes laterales) | retour du gate visuel user sur la rangee : si la carte parait « perdue » dans sa colonne a 1920 px, rendre `CANVAS_HEIGHT` fonction de la largeur de colonne (borne 480-620) |
| `floorOf` rend l'etage 1 (pas 0) sur un document a amplitude verticale nulle (`altitudeRatio` -> 0,5 « si plate ») : anneau d'etage permanent | PLAN_HABILLAGE §5 decouverte 1, 2026-08-16 | hors perimetre ; le repli « milieu » est juste pour une opacite, faux pour un compteur d'anneaux | quand un artefact sans bornes verticales est observe a l'ecran : `floorOf` -> 0 si `minZ == maxZ`, test unitaire |
| Fichiers du rejeu au seuil : `ReplayCanvas.tsx` 779 L (dette gelee, +48 malgre 3 extractions), `ReplayTeams.tsx` 488 L, `replayMarkers.ts` 493 L | PLAN_HABILLAGE §5 decouvertes 3-5, 2026-08-16 | extraire hors lot = fix opportuniste | **HONORE pour `ReplayCanvas.tsx` le 2026-08-18** (lot correctif revue poses, F3) : le lot des poses avait ajoute 81 L sans extraire ; la sortie des quatre cuissons hors ecran (`useReplayStaticLayers.ts`) le ramene a 860 L, sous le niveau d'avant, avec un cliquet de taille (`placementFamily.guard.test.ts`). `PlayerCard` de `ReplayTeams.tsx` et `drawProjectilesLayer` de `replayMarkers.ts` restent a extraire a leur prochaine addition |
| Suite web complete non deterministe sur ce poste : timeouts 5 s isoles sous charge (`langSegmentInheritance.test.ts` gate 0, `PalmaresRelationsPage.test.tsx` fin lot B), verts relances seuls | PLAN_HABILLAGE §5 decouverte 6, 2026-08-16 | fichiers jamais touches par le lot | si un 3e fichier flanche : `testTimeout` cible ou isolation du montage routeur ; la CI Linux reste le gate d'autorite |
| Tiroir de reglages du rejeu touche par DEUX lots : habillage (bascule « Noms », fusionne) et R1 de la planche (passage en overlay, `wt/retours-planche`, en cours) | PLAN_HABILLAGE §7, 2026-08-17 | ordre de fusion tranche : habillage d'abord (complet, gates verts), R1 se realigne | a la fusion de R1 : `git merge feat/v75` dans `wt/retours-planche`, garder les deux intentions dans `ReplaySettingsDrawer.tsx`/`useReplaySettings.ts` (patron du realignement `2c956ac9a`) |
| ~~`equipmentPlacements` (poses d'equipement au rejeu, schema 9)~~ **SORTI le 2026-08-18 (PLAN_POSES_EQUIPEMENT_PUBLICATION) : calibre, publie, temoins re-cuits** | PLAN_IDENTITE_TI37 phase 2, 2026-08-17 | **LE DIAGNOSTIC DU 17/08 ETAIT INCOMPLET, et c'est ce qui bloquait.** Les 3 bits manquants n'etaient pas portes par le SEUL premier champ du bloc MPP : DEUX champs inconditionnels du chemin minimal peuvent les porter — le premier (`R(9)`) et le champ inline `R(5)` qui SUIT l'identifiant de 32 bits. Les deux donnent le meme TOTAL, donc la meme position, donc le meme accord avec un oracle de position ; ils ne donnent PAS la meme lecture. Forcer `6/5` (l'hypothese du 17/08) rendait 19 a 37 poses par film BTB, toutes portant UN SEUL identifiant — un artefact de decalage de 3 bits, qui aurait ete publie comme une identite. Le decoupage MESURE est `9/5` sur les films Quick Play et **`8/3`** sur les films Big Team Battle : 537, 892 et 922 poses sur les memes films, 12 a 14 identifiants, et le pic du default-state tombe alors sur 57 bits exactement | **SORTI.** `filmdec.CalibrateMPPWidths` calibre le DECOUPAGE COMPLET par oracle de position (>= 12 accords ET >= 3x la concurrente, arret anticipe) : 11 films sur 12 tranchent, le 12e (`0014603f`, 41 ancres / 70 vies) publie une liste vide + `slog.Warn`. 21 identifiants sur 21 resolus dans le groupe `eqip` du jeu. Publie schema 9 + `coverage.placements` (qui porte le decoupage : sans lui, zero pose par absence et zero pose par calibration refusee seraient indistinguables)
| Mur et capteur de la palette famille A : mesures mais NON NOMMES (diagonale sous le seuil) | PLAN_POSES_EQUIPEMENT_PUBLICATION gate 1, 2026-08-18 | le manifeste ne nomme que `0x8e2dc574` (mur, 88,0 %) et `0x72199cba` (capteur, 87,5 %), tous deux de la palette famille B. Les identifiants que la famille A emploie echouent DE PEU au seuil de 85 % pose avant la mesure : `0x72b63d69` rang 1 (82,0 %, 41/50), `0x2974c233` rang 2 (77,8 %, 42/54), `0x686b40c9` rang 2 (75,0 %, 21/28) ; s'y ajoute `0x528fce46` rang 19 famille B (81,5 %, 22/27), vraisemblablement les panneaux deployes du mur. Consequence MESUREE : sur les quatre films Big Team Battle du corpus, les 537 a 922 poses publiees sont TOUTES en famille `other` — avec leur position, leur fenetre, leur poseur et leur identifiant, mais sans nom | le denominateur est le frein, pas le signal : sur ces films 46 a 70 % des poseurs n'ont AUCUN rang `i48` lu (84 a 157 slots a rang connu sur 209 a 256 bipedes), ce qui dilue la diagonale. Reprise : elargir le corpus a des films de famille A dont la couverture `i48` est meilleure, ou croiser par un second canal (le rang de la fenetre d'image-cle 16..23). Le seuil NE SE REBAISSE PAS — c'est lui qui a empeche de publier l'identifiant de decalage `0x10c64ad2` le 17/08 |
| Le DECOUPAGE du bloc MPP correle avec la liste (Quick Play 9/5, Big Team Battle 8/3) sans qu'aucun mecanisme ne l'explique | PLAN_POSES_EQUIPEMENT_PUBLICATION gate 0, 2026-08-18 | separation parfaite sur 11 films (7/7 et 4/4), mais la liste n'est PAS dans le film (donnee du registre de matchs) et le proxy cote film — taille de la bande de slots, nombre de bipedes — n'a pas de frontiere observee (rien entre 138 et 161 slots bipede). Surtout, DEUX champs retrecissent, de 1 et de 2 bits : aucune quantite en log2 n'explique cette paire. Deriver une regle reviendrait a inventer un seuil | la calibration par oracle fait le travail (11/12, marges de 12 contre 0 a 28 contre 1) et coute 15 a 60 s par film. ROUVRIR si un film tombe entre les deux classes, ou si un troisieme decoupage apparait : ce serait le signe qu'une quantite continue le fixe, et il faudrait alors la trouver plutot que d'elargir la liste des candidats |
| NEGATIF MESURE — `ability-enabled-id` et `entity-ref-index5` du default-state de ti=37 ne portent RIEN | PLAN_IDENTITE_TI37 phase 0, 2026-08-17 | portes FERMEES sur 503 records de creation sur 503 (274 sur `000d5950`, 229 sur `00162144`), alors que le record est lu bit-exact (profil de bits concordant champ par champ, distance en-tete -> masque = 24 + 60 + 1 sur 91 records localises par un oracle de position independant). Le film ne transmet donc AUCUNE reference de createur a la naissance | ne pas rejouer ces deux champs. L'identite est ailleurs dans le MEME record : le mot de 32 bits inconditionnel du bloc MPP = le GlobalID du tag `eqip` (11/11 resolus). Le createur, lui, se mesure par la geometrie (0,575 m median contre 14,127 m de temoin) |
| `i57` v==1 comme controle temporel de la pose d'equipement | PLAN_IDENTITE_TI37 item 1.3, 2026-08-17 | la double chaine etait deja etablie par le croisement `eqip` x rang `i48` (90 et 92 %) et par la geometrie + temoin ; un 3e canal n'aurait pas change le verdict et aurait ouvert le nommage d'`i57`, qui est un autre sujet | si le nommage d'`i57` redevient une question : co-dater son passage a v==1 avec le premier record des objets `eqip` deja nommes — le pont existe maintenant dans les deux sens |
| Les GlobalID `eqip` ne sont PAS lisibles en clair dans les palettes `sofd` | PLAN_IDENTITE_TI37 phase 0.3, 2026-08-17 | balayage des 119 tags `sofd` de `any/globals/globals-rtx-new.module` : 0 reference `eqip` trouvee par recherche de mot de 32 bits. Les references de tag Halo Infinite passent par la table de dependances du tag, pas par le GlobalID brut | si le pont rang -> `eqip` doit venir du JEU plutot que de la mesure du film : decoder l'en-tete de tag (table de dependances) — meme surface que la chaine `food -> bloc/scen/mach` du lot forge |
| UI poses — l'infobulle est au POINTEUR seul | PLAN_UI_POSES_EQUIPEMENT, 2026-08-18 | le canvas est une balise unique, sans sous-element focusable : l'identite d'une pose n'est pas atteignable au clavier | quand une couche d'accessibilite sera posee sur le canvas du rejeu (elle manquerait aussi aux marqueurs joueurs et aux zones) |
| UI poses — pendant la LECTURE, une pose qui expire sous un pointeur IMMOBILE garde son infobulle jusqu'au prochain mouvement | PLAN_UI_POSES_EQUIPEMENT, 2026-08-18 | l'image courante est lue dans une reference ; publier la frame a React 60 fois par seconde couterait le budget d'animation | si le survol devient un besoin en lecture continue |
| `ReplayCanvas.tsx` : le cadrage `{bounds, width, height, pad}` etait recopie SIX fois ; un est memoise (`canvasView`, partage par le dessin et le survol), cinq copies restent dans les effets de cuisson | PLAN_UI_POSES_EQUIPEMENT, 2026-08-18 | hors perimetre ; le fichier fait 938 lignes (dette de taille gelee, +76 par ce lot ; 4 copies du patron « cuire un calque hors ecran » notees par le lot heatmap) | **PARTIELLEMENT CLOS le 2026-08-18** (lot correctif revue poses, F3) : les QUATRE cuissons hors ecran vivent dans `useReplayStaticLayers.ts`, avec une seule amorce (`cookLayer`) et le cadrage partage `canvasView` — quatre des cinq copies restantes disparaissent. `ReplayCanvas.tsx` : 952 -> 860 L, sous les 861 L d'avant le lot des poses, et un CLIQUET le tient (`placementFamily.guard.test.ts`). RESTE : la cinquieme copie du cadrage et les 4 copies du patron notees par le lot heatmap |
| **La DISPARITION d'un objet d'equipement n'est PAS lisible dans le film** | lot correctif revue poses, F1, 2026-08-18 | `t1` (`filmdec/equipment_placements.go:61`, `replay/equipment_placements.go:65`) est le dernier point de la vie DECODEE, c'est-a-dire la MISE AU REPOS de l'objet — le balayage ne suit que les records porteurs d'`object-position-component`, et un objet pose cesse d'emettre sa position. Mesure (`filmdec/equipment_life_end_test.go`, films 000d5950 / 00ba2e1c) : mur `0x8e2dc574` med 0,68 s (n=13), capteur `0x72199cba` med 2,09 s (n=19). Le recensement des keyframes PROUVE que l'entite survit (101 poses sur 295, et 228 sur 537, encore recensees > 1 s apres la fin du flux de position). Trois pistes de fin explicite instrumentees, trois negatifs : record DEL balaye bit a bit = 78 090 et 158 098 candidats pour 477 et 993 vies ; queue de records de la meme cle sans contrainte de position = 98,0 % et 99,4 % des cles SANS aucune vie en portent ; recensement keyframe vrai mais espace de 20,0 s. La coupure `AtRest` (i18) heritee du projectile n'y est POUR RIEN : `ti=37 i18` est `item-at-rest-component` et les distributions sont identiques avec et sans elle | une source de fin DATEE. Deux pistes non instrumentees : (a) une marche SEQUENTIELLE des records (`frame_records.go`) rendrait le record DEL sans balayage bit a bit — elle desynchronise encore aujourd'hui (« au moins une faute dans le CORPS des records », note de `DefaultPacketPreambleBits`) ; (b) `equipment-energy-component` (i24, R(14)) decroit peut-etre jusqu'a la mort de l'objet, mais il n'est lu que sur les records porteurs d'i0 — donc jamais apres l'immobilisation. En attendant, le rendu ne referme rien : le capteur se tient a sa duree OFFICIELLE (15 s), les autres poses vont jusqu'a la fin du rejeu (`placementEndFrame`, `equipmentPlacementsLayer.ts`) |
| Les artefacts de rejeu DEJA CUITS restent au schema <= 8 : la re-cuisson de MASSE du schema 9 n'a pas ete lancee | lot correctif revue poses, F10, 2026-08-18 | le lot des poses n'a re-cuit que TROIS temoins. Tout artefact plus ancien est servi sans `equipmentPlacements` ni `coverage.placements` : le calque des poses est alors vide, et rien a l'ecran ne distingue « ce match n'a pas d'equipement pose » de « cet artefact est perime ». Le champ `schemaVersion` du document porte deja l'information, mais aucun chemin ne la lit pour declasser l'artefact. **Ce lot n'a lance AUCUN run de masse** (hors perimetre, et c'est une operation de production) | rejouer la cuisson sur le corpus d'artefacts, en filtrant sur `schemaVersion < 9`. A arbitrer avec l'utilisateur comme le backfill `killsource` : sans ce rejeu, les poses n'existent que sur les trois temoins |
| Le nommage d'une famille de pose par DIAGONALE est fragile, et les quatre identifiants « plats » ne sont pas des equipements poses | lot correctif revue poses, note de revue, 2026-08-18 | (a) la diagonale croise l'identifiant `eqip` avec le RANG DE CAPACITE du poseur — deux mesures en serie, et le rang manque a 46-70 % des poseurs sur les films BTB : un identifiant peut echouer au seuil pour un defaut de DENOMINATEUR, ce qui est le cas des quatre identifiants a 75-82 % ; (b) `0x0f5716ff`, `0xbcabbe43`, `0xcaaadcb0`, `0xaada07f3` sont vraisemblablement les quatre GRENADES du jeu (groupe `gggl`) ramassables au sol — un objet du monde sans poseur est plat par construction, et son echec au seuil ne mesure rien. **Le seuil et le critere N'ONT PAS ETE MODIFIES** : c'est une decision produit, hors de ce lot. La note est entree au manifeste (`config/titles/halo_infinite/mappings/replay_labels.toml`, section `[[equipment_objects]]`) | decision produit de l'utilisateur : garder la diagonale en la dotant d'un denominateur minimal (n de rangs CONNUS), ou lui adjoindre un second canal (rang de la fenetre d'image-cle 16..23). Les quatre plats, eux, se confirmeraient par le groupe de tags `gggl` plutot que par une diagonale |
