package migration

// steps_metadata_drop_playlists_catalog_indexes.go — drop_playlists_catalog_secondary_indexes
// a été migré vers internal/games/halo_infinite/migrations/steps.go avec la famille
// catalogue (même table playlists_catalog, atomique) (Phase 1.5 b12, voie B). Le nom
// reste dans internal/migration/order.go (canonicalOrder).
