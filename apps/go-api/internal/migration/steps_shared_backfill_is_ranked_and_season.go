package migration

// steps_shared_backfill_is_ranked_and_season.go — shared_backfill_is_ranked_and_season
// (ALTER match_registry +season_id + backfill is_ranked/season_id) a été migré vers
// internal/games/halo_infinite/migrations/steps.go (Phase 1.5 b18, voie B). Le nom reste
// dans internal/migration/order.go (canonicalOrder).
