package migration

// steps_player_rebuild_career_progression.go — rebuild_career_progression_defeat_art_corruption
// (+ helper markRebuildDone + const careerProgressionRebuildMetaKey) a été migré vers
// internal/games/halo_infinite/migrations/steps_player_repairs.go (Phase 1.5 b21, voie B).
// Le nom reste dans internal/migration/order.go (canonicalOrder).
