/**
 * CareerHubPage — page Carrière : progression de rang, LUSR/CSR, top matches, encounters.
 * Les citations ont leur propre page dédiée (/citations).
 */
import { CareerProgressionTab } from './CareerProgressionTab'

export function CareerHubPage() {
  return <CareerProgressionTab />
}
