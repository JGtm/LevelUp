[Unit]
Description=LevelUp Docker prune (images/containers/networks/build cache > 24h)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/docker system prune -af --filter "until=24h"
